{
  "date": "2026-09-13",
  "task": "wie-doc-contract-divergence-has-no-alarm-measure-first",
  "title": "문서↔계약 어긋남 경보 — 판정 «만들지 않는다(⒥)»",
  "summary": "「울지 않는다」는 참이다(잡는 기계 0). 그러나 만들 값이 없다: ★CI 러너가 그 계약 파일을 볼 수 없고(7/7 GitHub 호스티드 · self-hosted 0), ★참조 대상의 버전 추적성이 없으며(한 파일은 untracked · 다른 하나는 커밋본 63줄 ↔ 라이브 1,146줄), ★지켜야 할 «값 복사» 자리가 2곳뿐이고 그 둘은 오늘 9ef24b43 이 이미 고친 같은 불릿에 있다. 더 싼 대안(값을 복사하지 말고 정본을 가리켜라)이 이미 착지해 표면 자체를 없앴다.",
  "changes": [
    "코드·검사기 구현 0 — 판정과 근거 수만 기록한다(docs/report/0109 + 이 worklog + STATE.md 완료 1항)"
  ],
  "verification": [
    "ⓐ 잡는 기계 0: repo 검사기 9종 전수에서 repo 밖 경로를 읽는 것 0 · ~/orchestrator/bin/ 에서 AGENTS.md 를 읽는 도구 0",
    "ⓑ 값 복사 2곳(등재 사실 · 전략 값 — 둘 다 같은 불릿) · 규칙 재진술 3 · 포인터 3",
    "ⓒ CI 제약: .github/workflows 7종 전건 GitHub 호스티드(ubuntu-latest/matrix) · self-hosted 0 ⇒ ~/orchestrator 접근 불가 ⇒ 게이트 불가",
    "ⓒ 추적성: ~/orchestrator@live HEAD 13b88a0c 는 origin/main 보다 2,473 커밋 뒤 · templates/merge-ticket.tpl 커밋본 63줄('4-A' 0건) ↔ 라이브 1,146줄('4-A' 11건) · contracts/upstream-sync-repos.conf 는 그 트리에서 untracked",
    "ⓓ 대안 비교: 가리키기(9ef24b43 착지)는 비용 0 이고 값 복사 자체를 없앤다 ↔ 검사기는 로컬 전용이라 «매 회차가 기억해야 하는 것»을 하나 더 만든다"
  ],
  "limits": [
    "이 판정은 «오늘의 수» 위에 선다 — 값 복사 2곳·러너 0 self-hosted·계약 파일 untracked. 셋 중 하나가 바뀌면 다시 열어야 한다(재개 조건 3종을 리포트에 박았다)",
    "사람 규율 한 줄은 «어디에 둘지»만 정했고 쓰지 않았다 — AGENTS.md 재수정이 이 티켓의 범위 밖이다(아래 제안 #p0)",
    "남의 정본(contracts/·templates/)은 읽기만 했고 만지지 않았다"
  ],
  "adoptedProposals": ["2026-09-13-merge-section-points-at-the-canon#p0"],
  "proposals": [
    {
      "title": "「외부 계약은 가리킨다, 복사하지 않는다」를 «기계가 안 잡는 것» 절에 한 줄로 박아라",
      "plainSummary": "설명서가 다른 곳의 규칙을 그대로 베껴 적으면 조용히 어긋납니다. 베끼지 말고 원본을 가리키라는 한 줄을, 이미 '기계가 못 잡으니 사람이 지킨다'고 모아 둔 자리에 넣자는 제안입니다.",
      "userBenefit": "오늘 9일간 살아 있던 종류의 모순이 다시 생기지 않습니다 — 그 모순은 매 회차가 스스로 알아차려야 했고, 두 회차가 각자 알아차려서 겨우 막았습니다.",
      "why": "이 회차의 판정이 ⒥(검사기 만들지 않는다)이므로 남는 방어선은 사람 규율뿐이다. 자리는 AGENTS.md §Constraints 의 「Held by you, not by a machine」 — 「No game bytes」·「Secrets are referenced, never embedded」가 이미 같은 성격(기계가 안 잡는 것 + 왜 안 잡히는지)으로 사는 절이다. 문안도 그 문형을 따른다: 「An external contract is referenced, never copied — point at the canon (path + section) and let the ticket's frontmatter bind. Nothing machine-checks this: CI cannot see ~/orchestrator.」",
      "tradeoff": "★규율은 기계가 아니다 — 읽지 않는 회차에는 무력하다. 그리고 이 파일은 이미 길다(한 줄이 더해지는 값보다 «길어서 안 읽힌다»는 비용이 클 수 있다). ★그래서 «새 절»을 만들지 말고 기존 절에 한 줄만 얹는 형태여야 한다. 이번 회차가 직접 쓰지 않은 이유는 AGENTS.md 재수정이 티켓 범위 밖(오늘 9ef24b43 으로 닫힌 자리)이기 때문이다.",
      "effort": "S",
      "target": "AGENTS.md §Constraints → 「Held by you, not by a machine」"
    }
  ]
}
