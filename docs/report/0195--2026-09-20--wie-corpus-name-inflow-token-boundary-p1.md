## [2026-09-20] 「도구를 안 부른 회차」 검사기 — 재고 «짓지 않기로» 했다. 오탐 3, 진탐 0 (wie-corpus-name-inflow-token-boundary-p1)

채택 제안 `2026-09-20-corpus-name-inflow-token-boundary#p1` 의 이행. 제안은 「기계가 알아채게 할지 **정하라**」였다.
★**결정: 짓지 않는다.** 제안 자신이 적은 대안(「아무것도 하지 않고 «절반만 닫혔다»를 기록으로 남기는 편이 맞을 수 있다」)이 맞았고,
이 회차가 더한 것은 그 판단을 **수로 세운 것**과 **되열 조건**이다.

### ⒝ 먼저 — 이미 해소됐나

아니다. `origin/main` 에서 `corpus-name-inflow` 를 참조하는 **`.github/`·`scripts/` 파일은 그 도구 자신뿐**이고,
`AGENTS.md` 산문 한 문단 + 회차 문서들이 전부다. 열린 PR 중 이 자리를 고치는 것도 없다(#230 은 형제 `#p0` = 모집단 건).

### ① 구조적 사실 — ★**실행은 흔적을 남기지 않는다**

`scripts/corpus-name-inflow.mjs` 의 파일 쓰기(`writeFile`·`appendFile`·`createWriteStream`) **0건**.
⇒ ★**「이 회차가 도구를 불렀는가」는 사후에 관측할 수 있는 사실이 아니다.** 어떤 검사든 둘 중 하나여야 한다:

| 형태 | 무엇을 보는가 | 문제 |
|---|---|---|
| ⒜ 문구 프록시 | 회차 문서에 특정 문자열이 있는가 | 제안이 이미 경고했다 — 문구를 바꾸면 샌다(`pr-subject-tag`·`worklog-coverage` 선례) |
| ⒝ 새 산출물 | 도구가 `--record` 로 남긴 파일 | «있는가»는 CI 가 보지만 «참인가»는 못 본다(코퍼스가 없다 · Constraint 9) ⇒ 손으로 써 넣으면 통과 |

★⒝는 «규율을 다른 규율로 바꾸는 것»이고, 새 원장 파일 하나 + 새 공유 삽입점 하나를 만든다
(이 저장소가 `STATE.md` §완료에서 값을 치르고 없앤 바로 그 형태다).

### ② 문구 프록시를 «실제로 돌려 봤다» — 오탐 3 / 진탐 0

도구가 착지한 뒤(`e2c00a25`, 2026-09-20) 유입 수를 보고한 회차 문서 **12개**를 전수로 재고,
가장 자연스러운 후보 술어 — ★**「유입/BOUNDED 를 적었으면 리터럴 `corpus-name-inflow` 도 있어야 한다」** — 를 적용했다.

| 회차 | 문서 | 후보 검사 | 실제로 돌렸나 |
|---|---|---|---|
| `4acb1631` | `docs/report/0190` | OK | ✔ |
| `4acb1631` | `…census-runner-carrier-qualified-keys.json` | ★**FLAG** | ✔ 「도구를 커밋 «후»에 실행했다」 |
| `4886130b` | `docs/report/0188` · 그 worklog | OK · OK | ✔ |
| `71b6743c` | `docs/report/0189` | ★**FLAG** | ✔ 「도구를 «실행해서» 적는다」 |
| `71b6743c` | `…ktf-image-sweep-tool-to-scripts.json` | ★**FLAG** | ✔ 「도구를 실행해서 적었다」 |
| `e2c00a25` | `0170`·`0173`·`0174`·`0187` + worklog 2 | 전건 OK | ✔ |

⇒ ★**FLAG 3건 · 전건 준수 · 진탐 0.** 셋 다 «자기 말로» 도구를 인용했을 뿐이다.
회차 단위로 보면 유입 수를 보고한 **3회차 중 2회차**가 걸린다 — **오탐률 66.7%**.

★**표본이 작다는 것을 «인정»하는 것이 아니라 그것이 곧 논거다**: 이 저장소의 규율은
「표본 1건에 굳히면 오탐이 굳는다」이고, 여기서는 굳힐 진탐이 **하나도 없다**.

### ③ 병의 기저율 — ★**0. 그리고 «다른 병»의 기저율은 4다**

도구 착지 이후, 유입 수를 보고한 회차 **3/3 이 도구를 돌렸다**(미준수 0).
★그런데 그 리니지에서 **실제로 틀린 수가 커밋된 일은 4번 있었고, 4번 다 착지 전에 잡혔다**:

| 회차 | 무엇이 틀렸나 | 무엇이 잡았나 |
|---|---|---|
| `0187` | 「BOUNDED 16회/8쌍」(실측 29회/17쌍) | 게이트② → `-fix` |
| `0188` worklog | 「PREFIX 3회/2쌍」 | 게이트② → `-fix` |
| `0189` worklog | 「3파일 · PREFIX 0회/0쌍」(커밋 «전»에 쟀다) | 회차 내 자체 재측 |
| `0190` worklog | 근거로 든 파일에 그 이름이 0회였다 | 게이트② 필수 ② → `-fix` |

⇒ ★★**관측된 실패형은 한 번도 「도구를 안 불렀다」가 아니다 — 언제나 「불러 놓고 그 뒤 문장을 더해 수를 무효화했다」이다.**
그것은 형제 제안 `#p2`(「적힌 수 == 지금 출력」)의 질문이고 **자기 티켓이 있다**. 여기서 풀지 않는다.
★**이 회차의 실질 산출 중 하나가 이 재구성이다** — `#p1` 의 전제에는 관측이 **0**, `#p2` 의 전제에는 **4**가 있다.

### ④ 왜 지금인가 · 대가 (각 한 줄)

- **왜 지금인가**: 제안이 「절반만 닫혔다」고 스스로 적었고, 그 나머지 절반이 **지을 수 있는 것인지**를 아무도 재지 않았다 — 위 ①②가 그 답이다.
- **대가**: ★**짓지 않는 대가는 「다음 회차가 같은 제안을 다시 낸다」**이다. 그래서 판단을 산문에 남기지 않고
  **`AGENTS.md` 의 그 도구 항목 «안»에 측정과 함께** 박고, 되열 조건을 명시했다(이 저장소가 `--expect-last-frame`·
  `worklog 의무화`·`cargo audit` 에서 반복해 쓰는 형태 그대로).

### ⑤ 되열 조건 — ★두 개, 그리고 둘 다 «관측»이다

1. ★**이 리니지 «밖»의 회차가 유입 수를 보고할 때.** 위 준수 3/3 은 **도구를 만든 사람들의 회차**이고,
   그것은 있을 수 있는 가장 약한 표본이다. 습관이 리니지를 넘어가는지는 아직 아무도 모른다.
2. ★**어떤 회차가 수를 «손으로 재유도한 것»이 발견될 때** — 그 순간 진탐이 1이 되고, 위 ② 표의 분모가 의미를 갖는다.

★**이 조건은 산문이고 아무도 집행하지 않는다.** 그것을 감추지 않는다 — `AGENTS.md` 의 `WIE_BASE` 두 행이
이미 같은 형태(검사되지 않는 의무)를 지고 있고 그 파일이 그것을 명시해 두었다.
★**되열 조건을 기계화하면 같은 문구 프록시를 한 단 위에서 다시 짓게 된다**(「유입 수를 보고했는가」를 기계가
알아야 하는데, 그것이 바로 ②에서 틀린 그 판정이다).

### 계약 2 — 개악 양방향

★**해당 없음 — 검사기도 가드도 «만들지 않았다».** 이 회차의 산출은 **판정과 기록**이다.
★한 방향만 보이는 개악 실험을 지어내 붙이지 않는다(그것이 공허하다는 것이 계약의 취지다).
코드 변경 **0줄** · `scripts/` 무접촉 · `.github/` 무접촉.

### 한계

- ★**분모가 12파일 / 3회차다.** 이 결론은 「검사기가 영원히 틀린다」가 아니라 **「오늘 지으면 오탐만 굳힌다」**이다.
- ★②의 후보 술어는 «리터럴 문자열» 하나다. 더 넓은 술어(「도구를 실행」 같은 문구까지 허용)는 오탐을 줄이지만
  ★**정확히 그만큼 «손으로 세고 그 문장만 쓴 회차»를 통과시킨다** — 프록시의 본질적 대가이고, 이 회차는 그것을 넓히지 않았다.
- ★도구가 `--record` 를 갖게 되면 ①의 「흔적 없음」은 사라진다. 그때는 ⒝가 다시 후보가 된다 —
  다만 «참인가»는 여전히 못 보고, 그 축은 `#p2` 의 것이다.
