## [2026-09-07] `--expect-last-frame` 을 «켰다» — 러너 블록의 그 두 줄에, 줄을 늘리지 않고 (wie-expect-last-frame-gate-has-zero-callers)

**무엇을**: `AGENTS.md` 러너 블록의 `keydraw_*` 줄에 **`--expect-last-frame`** 를 붙였다(+ 왜/한계/예산 설명 22줄).
★**`.rs`·워크플로·스크립트 무접촉** · ★**플래그 자체 무접촉**(앞선 회차 산출물) · ★**새 픽스처 0**.

**왜**: 그 플래그는 **만들어졌는데 호출자가 0** 이었다(`.github/` 전수 `wie_validate` **0건** 재현).

**★F2 는 ⒜ 를 골랐고, 그 «대가»가 이미 사라져 있었다**: 브리프·제안이 「`keydraw_*` 두 줄은 러너 블록에도 «없다» ⇒
⒜를 고르면 블록이 길어진다」고 적었는데 ★**그 전제가 낡았다** — `wie-agents-md-runner-list-missing-keydraw-and-inject`(#104)가
**2026-09-06 에 그 두 줄을 이미 넣었다**(실측 히트 2). ⇒ ⒜의 실제 비용은 ★**«실행 줄 0 증가»**(플래그 한 토큰)다.
★**⒝ 는 «쓸 수 없다»**(고른 게 아니라 없다): CI 가 부르는 스크립트 11개 중 `wie_validate` 를 **실행**하는 것은 **0**이고,
쓰는 `smoke_gate.sh` 는 `game_lab/`(gitignore) 이 필요한 **로컬 전용**이다.
★**⒞ 를 버린 이유**: 그 클래스를 **매 PR 에 이미 막는 것은 브라우저 Scenario F** 다(#117 이 수로 확정). CI 에 또 걸면
같은 그물을 두 번 치면서 wasm 없는 **20초 로컬 되먹임**이라는 이 축의 «고유한 값»은 못 얻는다.

**★F3 예산 — `--timeout` 은 «이 경로에서 무력»하다(브리프 전제 정정)**: `--inject` 면 `deadline_secs` 가
**스케줄에서 다시 계산**된다(`boot 2.5 + 0.3 + 27스텝 × 0.6 + 1.0 = **20.0s**`) ⇒ `--timeout 5` 와 `20` 이 **둘 다 ~20초**(실측).
움직이는 노브는 `--boot-secs`/`--action-secs` 이고 줄이면 **덜 본다**(`--boot-secs 1.0` → 18.7초 · paints 55 → **37**).
⇒ ★**그대로 둔다**(결정). 6회 실측: KTF `20090 20260 22230 22331 26094 24452` · LGT `20663 20292 20224 21393 20275 20326` —
★**20.0 위의 흔들림은 «틱 오버런»이지 예산 부족이 아니다**(최대 +6.0초 = +30%).

**양방향**:
⑴★**검은 마지막 프레임에서 진다** — 2026-09-05 형상을 개악으로 재현하니 LGT **rc=1**(`result=PASS · last_frame=False · paints=75`).
★**`result` 는 PASS 인 채**라 ★**이 플래그만이 아니라고 말한다.** KTF 무영향(rc=0 · 패키지가 없어 형식이 같다).
⑵★**위양성 0** — 정상에서 `keydraw_{ktf,lgt}` **rc=0** · `helloworld_{ktf,lgt}`·`draw_j2me`(플래그 없이) **rc=0** · 원복 후 rc=0.
⑶★**그 줄에 걸지 «않았다»** — 그 줄은 세 픽스처를 도는 **한 루프**이고 `helloworld_{ktf,lgt}` 가 **구성상 진다**(걸면 **rc=1** · 실측). ★★**`draw_j2me` 는 «지지 않는다»** — `last_frame_content true` 라 플래그를 걸어도 **rc=0** 이다(실측 · `wie_validate.rs` 헤더 측정표에는 그 행이 **없다**). ⇒ ★못 거는 이유는 **공유 루프**이지 픽스처가 아니다 — 따로 빼면 걸리지만 **실행 줄이 늘어** 이 회차의 「0 증가」가 깨진다.

**사용자 영향**: 없다(문서 한 파일 · 엔진·CI 산출물 무접촉).

**★한계를 적었다**: 이것은 **로컬 그물**이지 CI 강제가 아니다 — `.github/` 는 여전히 `wie_validate` 를 부르지 않는다.
★그리고 CI 쪽 `Scenario F` 도 **무조건이 아니다** — `engine-contract.yml` 의 `dorny/paths-filter` **`engine` 게이트 뒤**라
엔진 경로를 안 건드린 diff 에서는 「Reporting success without rebuilding」으로 **돌지 않는다**(F3 minor 반영).
그 문장을 러너 블록 옆에 남겨 ★**「적혔으니 CI 가 막는다」로 오독되지 않게** 했다.
