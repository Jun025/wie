## [2026-09-07] key-reach 시험에 `paints > 0` 을 ★**얹지 않았다** — 그 수가 사고를 가르지 못한다 (wie-key-reach-tests-assert-frame-composited)

**무엇을**: 판단 회차다. ★**단언 0 · 시험 수 0 증가 · `.rs` 실행 코드 0줄**. 두 시험 헤더에 **왜 안 하는지**를
각각 한 블록(주석 12·13줄)으로 남겼다 — ★**추가줄 전건이 `//!` 주석**이다(비-주석 추가 **0**).

**왜 — F4 가 «수»로 답한다(그것이 F2 를 할지 정한다)**:

| 축 | 값 |
|---|---|
| Scenario **E**(KTF keydraw · 픽셀) | `KTF_KEYS = ["HASH","STAR","NUM5"]` **3키** · `runE.pixels … expected` 단언 |
| Scenario **F**(LGT keydraw · 픽셀) | ★**같은 3키**(`ktfKeys` 를 그대로 재사용) · `runF.pixels … expected` |
| ⇒ 두 캐리어 커버리지 | ★**3키 × 2캐리어 = 픽셀 단언 6건** — ★**덮는다** |

⇒ 브리프 F4 의 판정 규칙 「★**덮으면 ⒞가 정당하다**」에 그대로 걸린다.

**★그리고 더 강한 이유 — 그 단언은 «인용된 사고»를 못 잡는다**(측정):

| 상태 | `paints` | `paints > 0` |
|---|---:|---|
| 정상(이 시험 · 두 캐리어 모두) | ★**1** | 참 |
| 2026-09-05 LGT 검은 화면(직전 회차 M2 재현) | ★**83** | ★**참** |

★**두 상태 모두 참** ⇒ 그 단언은 이 사고를 **가르지 못한다**. 그 사고를 «실제로» 본 유일한 그물은
Scenario F 이고, 그 시나리오 헤더가 스스로 「**Neither `cargo test` nor `wie_validate` sees that**」이라 적는다.

**F1(가정 금지 · 세 항 전부 출력으로)**:
⑴`TestPlatform::paint_counter() -> Arc<AtomicUsize>` · `redraw_flag() -> Arc<AtomicBool>` — `test_utils` 에 있고
캐리어 어휘 **0** ⇒ **중립 참**. ⑵두 `test_key_reach.rs` 는 `Event::Keydown(KeyCode::NUM5)` 를 **틱 전에 큐잉**하고
`key:53` 을 stdout 으로 단언한다 — 그리는 데까지 **간다**(프로브로 `paints=1` 확인).
⑶`helloworld_ktf/lgt.zip` → ★**`paints=0 · content=false`** ⇒ 「아무것도 그리지 않는다」 **참** ⇒ ★**그 파일들 무접촉**.

**사용자 영향**: 없다(주석만 · 실행 코드 0).

**★내 가설 하나가 틀렸다 — 숨기지 않는다**: 「하니스가 `Event::Redraw` 회신을 안 하므로 `paints` 는 늘 0이고
그 단언은 «영영 지는 시험»이 된다」고 추정했는데, ★**프로브 실측은 `paints=1`** 이었다. ⇒ 단언을 얹는 것 «자체»는
2줄로 가능하다. ★**거절의 근거는 「불가능」이 아니라 「가르지 못한다」이고**, 그 구분을 흐리면 다음 사람이 틀린 이유로 재개한다.

**남긴 것**: 시험 헤더 두 곳에 ★**「Scenario E·F 가 사라지면 이 결정을 다시 열어라 — 그때는 이 카운터가 값싼 헤드리스 대역이다」**
를 적었다. 결정이 증발하지 않게 하는 것이 이 회차의 절반이다.
