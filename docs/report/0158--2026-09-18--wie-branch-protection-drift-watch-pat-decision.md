## [2026-09-18] PAT 판정 — **「지금은 쓰지 않는다」** · 다만 «사람이 본다»가 사실상 «안 본다»임을 수로 인정한다 (wie-branch-protection-drift-watch-pat-decision)

**무엇을**: 채택 제안 `2026-09-17-branch-protection-claim-guard#p1` 의 이행.
★**판정 회차이고 구현이 아니다** — **코드 0줄 · PAT 발급·등재 0 · 시크릿 0 · #p0(탐지 구현) 무접촉**(계약 1·3).

## ★판정 1줄 (Acceptance ⑴)
> **PAT 를 «지금은» 쓰지 않는다.** 이 ruleset 을 바꿀 수 있는 주체가 **운영자 한 사람뿐**이라(`owner.type=User` · 조직 상위 ruleset 부재 · `bypass_actors: []`),
> 주기 감시는 대체로 **운영자가 자기 변경을 자기에게 알리는 일**이 되고, 그 값이 **시크릿 1개 + 만료·회전 의무**보다 작다고 판단했다.

★**그러나 이 판정을 «사람이 본다」로 포장하지 않는다 — 아래 ⑵⒝ 의 수가 그것을 허락하지 않는다.**

## ⓐ 전제를 «내가» 확인했다 — 인용을 믿지 않았다
| 확인 | 실측 |
|---|---|
| `permissions: administration: read` 가 거부되나 | `gh run view 35180786771` → **failure** · ★**jobs 0** · `name` 이 **원시 경로**(`.github/workflows/doc-liveness.yml`) = **startup_failure 의 지문** |
| CI 토큰이 엔드포인트를 읽나 | `gh run view 35181122022` → **failure** · `event=pull_request` (그 회차에서 가드가 rc=2 로 멈췄다) |
⇒ ★**「주기 감시에는 PAT 가 필요하다」는 참이다.** 제안이 재현된다.

## ★★결정적 실측 — «누가 이 설정을 바꿀 수 있나»
```
gh api repos/Jun025/wie      → owner.type = User · private = false · permissions.admin = true
gh api …/rulesets/23572944   → source_type = Repository · bypass_actors = 0
```
⇒ ★**조직 상위 ruleset 이 «존재할 수 없다»**(사용자 소유 repo 위에는 org 가 없다) · ★**우회 주체 0**
⇒ ★**변경 가능한 주체 = 운영자 1인.** 이것이 판정의 축이다.

## ⑵ 세 선택지의 대가 — 수 또는 구체로
### ⒜ **쓴다(PAT)**
- 시크릿 표면 **3 → 4 (+33%)**(현재 `CLOUDFLARE_ACCOUNT_ID` · `CLOUDFLARE_API_TOKEN` · `OTTERPEBBLE_DISPATCH_TOKEN` · variables 0)
- 만료 ≤ **1년**(fine-grained PAT 상한) ⇒ **회전 의무 1건이 새로 생긴다** · 회전 주체 = **운영자**(= 이 저장소가 「주인 없는 의무는 죽는다」로 규탄한 형태)
- ★**이점은 정직하게 적는다**: 실패가 **시끄럽다** — 만료되면 주간 잡이 **red** 가 되고 그 red 에는 **이미 소유자 규칙이 있다**(`doc-liveness` red → 다음 게이트③). 드리프트의 침묵과 달리 **탐지된다**.

### ⒝ **사람이 가끔 본다** — ★**측정해 보니 «안 본다»에 가깝다**
`AGENTS.md` 가 명명한 두 사건의 실제 발화:
```
사건 ①「REQUIRED-CHECKS 블록을 고칠 때」 → first-parent 이력 전체에서 ★1회 (6144f635 · 2026-09-17)
사건 ②「머지가 문서와 다르게 굴 때」      → 그 사유로 돌린 기록 0(회신 언급 5건은 «규율 인용»이지 실행이 아니다)
```
⇒ ★**저장소 역사에서 «한 번» 울렸다.** 「사람이 본다」를 값으로 계상하면 그것은 **거짓**이다.

### ⒞ **안 본다** — 포기 범위
#p0 가 감시하는 **잎 필드 27개**(그중 **21개**는 종전에 아무도 안 보던 것)가 **두 사건 사이에는 보이지 않는다**.
★그 창에 **상한이 없다**(사건이 1회 울렸으므로).

## ★그래서 이 판정의 실체는 «⒝ 로 위장한 ⒞» 다 — 그 사실을 적는다
「지금은 안 쓴다」의 정직한 번역은 ★**「당분간 27개 필드를 «사건이 울릴 때만» 본다 = 사실상 거의 안 본다」**이다.
★**그 선택이 견딜 만한 이유는 «변경 주체가 1인»이라는 실측 하나뿐이고, 그 하나가 무너지면 판정도 무너진다.**
⇒ 그래서 **자동으로 뒤집히는 조건**을 아래에 박는다.

## ★재개 조건 — 하나라도 참이면 판정은 «쓴다» 로 뒤집힌다
★★**[승계 `-fix` · 게이트② 반려 사유] 초판은 조건 «문구»만 3개 두고 «누가 재는가»를 아무에게도 배정하지 않았다.**
게다가 ⑵⑶은 `rulesets` 엔드포인트를 묻는데 ★**CI 토큰은 그것을 못 읽는다**(403 · PR #197 실측 「로컬 admin 27/27 ↔ CI 0/27」)
⇒ 「X 를 관측하면 PAT 를 쓴다 · X 관측에 PAT 가 필요하다」로 **자기 전제가 자기를 닫았다**.
★**이 회차가 이 회차 자신의 논거로 반박당했다** — §4⒜ 가 PAT 회전 의무를 「주인 없는 의무는 죽는다」로 기각했는데, 그 자가 재개 조건에 그대로 걸린다.

| # | 조건 | 재는 술어 | ★누가·언제 | PAT |
|---|---|---|---|---|
| ⑴ | 소유자가 **Organization** 이 된다 | `gh api repos/Jun025/wie --jq .owner.type` | ★**오늘 주인 «없다»** — 기계 배정은 후속 `#p1`(가드에 2줄) · 그 전까지 관측 계기는 **운영자가 직접 하는 이전 행위**뿐 | 불요 |
| ⑵ | `bypass_actors` 가 **비지 않는다** | ★`node scripts/check-branch-protection-claim.mjs` 가 **rc=1** 로 **그 줄을 지목**한다 | ★**형상 가드**(PR #197) · `AGENTS.md` §Incident ledger 가 명명한 **두 순간**(REQUIRED-CHECKS 블록 편집 시 · 머지가 문서와 다르게 굴 때) | ★**불요** |
| ⑶ | ~~`updated_at ≠ created_at`~~ → ★**교체**: ruleset **형상이 바뀌었는데 운영자가 그 변경을 기억하지 못한다** | 같은 가드 **rc=1** + 운영자 확인 | 같음 | 불요 |

★★**⑶ 을 교체한 이유(게이트② 지적 2)**: `updated_at ≠ created_at` 은 ★**t=0 에 이미 참**이다 —
`created 2026-09-17T10:17:54.948+09:00` ↔ `updated …54.979` = **31ms 차(생성 트랜잭션의 부산물)**.
그 술어를 기계로 걸면 **상시 오탐 트립와이어**가 된다. ⇒ ★**형상 가드는 타임스탬프를 «버리므로»(정규화 제외 목록)
그 오탐을 구조적으로 못 낸다** — 대체 술어가 더 싸고 더 정확하다.
★**실측으로 확인했다**(⑵): 베이스라인의 `bypass_actors` 를 채우면 가드가 **rc=1 · `line 14: … "bypass_actors"`** 를 찍는다.

★★**의존을 숨기지 않는다 — ⑵⑶의 주인은 «PR #197 착지»가 전제다.** 그 PR 이 열려 있는 동안 ⑵⑶은 여전히 주인이 없다.
⑴은 착지 여부와 무관하게 **오늘 주인이 없다.**

### ★대가 — 조건을 좁혔으니 잃는 것을 적는다
관측 가능한 3술어로 좁히면 ★**그 밖의 경우는 이 조건이 «잡지 않는다»** — 예: 조직 이전 없이 운영자가 PAT 를 원하게 되는 경우 ·
활성 ruleset 이 여럿이 되는 경우 · 조직 상위 정책이 아닌 **다른 관리자 추가** 경로. 그때는 **판정을 다시 해야 한다**(유연성이 줄었다).
★그럼에도 값하는 이유: 「필요해지면」류 조건은 **아무도 재지 않으므로** 실질적으로 **«영원히 안 함»**과 같고,
그 형태를 이 회차가 §4⒜ 에서 스스로 기각했다. ★**「잃는 것이 없다」가 아니다.**

## ⑶ 그날의 **최소 권한 명세**(판정은 「안 쓴다」지만, 다시 파생시키지 않도록 미리 적는다)
| 축 | 값 |
|---|---|
| 종류 | **fine-grained PAT**(classic 금지 — 스코프가 계정 전체로 번진다) |
| 대상 | ★**`Jun025/wie` 단일 repo** |
| 권한 | ★**`Administration: Read-only` 하나**(이것이 `/branches/*/protection` 과 `/rulesets` 를 읽는 최소 권한) · 그 밖 전부 **No access** |
| 보관 | repo secret(예: `BRANCH_PROTECTION_READ_TOKEN`) — ★환경/조직 시크릿 아님 |
| 소비자 | 기존 **주간 `doc-liveness` 잡** 한 곳(새 잡 만들지 마라 — #p0 가 그 자리를 이미 쓴다) |
| 만료 | ≤ **1년** · ★**만료 시 잡이 red** 가 되고 그 red 의 소유자는 **다음 게이트③**(기존 규칙 재사용) |
| 발급 | ★**human-step 별건**(계약 3⒟) — 이 회차는 초안도 «발권»하지 않는다 |

## 계약 준수
- **계약 1**: 판정만 냈다 · ★**PAT 발급·등재 0** · 급소(수명·권한·보관 주체)는 위 표가 답한다(판정이 뒤집히는 날 쓰라고).
- **계약 3**: ⒜PAT 0 ⒝**#p0 무접촉**(`scripts/`·`.github/` 0줄) ⒞`wie` 등재 repo 규율 — approve 여도 **묶지 않는다** ⒟human-step **발권 안 함**.

## ★이 회차가 치르는 대가 — `STATE.md` 형제 무효화(게이트② 지적 3 · 브리프가 물은 축)
이 PR 은 `STATE.md §완료` 최상단에 항목 1건을 더한다 ⇒ ★**착지하면 그때 깨끗하던 형제 PR 이 `CONFLICTING` 이 된다.**
★**측정값**(검수자 읽기 전용 `merge-tree`): 이 PR 과 **#195** 가 당시 유일한 두 `MERGEABLE` 이었고
`git merge-tree --write-tree --name-only pr/198 pr/195` → **rc=1 · `STATE.md`**.
★**이 회차가 만든 결함이 아니다** — 저장소 관례를 따랐을 뿐이고 소관은
`wie-state-md-insertion-point-conflicts-every-open-pr`(그 회차가 「`merge=union` 기각 · 백필 3→0」으로 구조 처방의 기각 사유를 없앴다).
★**피할 수 없었다**: 원장 규약이 착지 회차에 §완료 항목을 요구한다.

## Acceptance ⑷ — 코드 0줄 · 시크릿 0
착수 시 `git status --porcelain` **0줄** · 커밋은 **원장 3파일**(`STATE.md` · `docs/report/0158…` · `docs/worklog/…json`) ·
`scripts/`·`.github/`·`src/`·`web/` **0** · 토큰 값·이름 **출력 0**(위 표의 이름은 «제안된 이름»이지 실재하는 시크릿이 아니다).
