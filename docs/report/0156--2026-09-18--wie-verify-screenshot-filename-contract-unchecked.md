## [2026-09-18] `verify_*.png` 파일명 계약 — 검사를 **만들지 않기로** 했다(측정으로 기각 · 코드 0줄) (wie-verify-screenshot-filename-contract-unchecked)

**무엇을**: 채택 제안 `2026-09-17-verify-browser-screenshot-side-effect#p0` 의 이행.
★**검사기 0개 · 코드 0줄 · 제품 스크립트 무접촉** — 이 회차의 산출물은 **측정과 결정**이다.

**왜 만들지 않았나**: ★**제안 자신이 걸어 둔 판정식을 그대로 돌렸고 답이 0이었다.**
그 tradeoff 문면: 「채택 전에 «이 이름이 실제로 움직인 적이 있는가»를 먼저 세라 — **0이면 만들지 않는 것이 옳다**」.

**사용자 영향**: 없다(코드 0줄).

### ⓐ 「대조 검사가 0건인가」 — **참**(티켓이 먼저 확인하라고 한 것)
`git grep -ln 'verify_' scripts/check-*.mjs` → **빈 출력**.
`verify_` 를 적은 추적 파일은 **세 곳뿐**:

| 자리 | 역할 |
|---|---|
| `scripts/verify-browser.mjs:60-61` | ★**생성**(`path.join(root, \`verify_${label}_screen.png\`)` / `_page.png`) |
| `AGENTS.md:448-450` | 문서(규칙 + 기본 실행의 실제 두 이름) |
| `.gitignore:35` | ★**패턴**(`/verify_*.png`) — 이름이 아니다 |

### ⓑ 「문서가 낡는 것이 진짜 피해인가」 — ★**기계 소비자 0**
`git grep -ln` 으로 그 **파일명을 읽는** 추적 파일을 조회 → `AGENTS.md` + `scripts/verify-browser.mjs` ★**둘뿐**.
`web.yml:128` 의 `upload-artifact` 는 `path: web/dist` 라 ★**그 png 를 올리지 않는다**(직접 확인).
⇒ ★**소비자는 «사람»뿐이고, 낡음의 피해는 «잠깐의 혼동»이다.**

### ★판정식 — 「그 이름이 움직인 적이 있는가」 = **0**
`scripts/verify-browser.mjs` 총 커밋 **3**건. 그중 ★**생성 두 줄(58-62)을 건드린 것은 1건**이고 그것은 **2026-06-24 파일 생성**(`ac4ce1aa`)이다.
이후 두 커밋은 같은 파일을 고쳤지만 **이름 두 줄은 무접촉**:
`a307cebf`(2026-09-07 · 배포 후 CI 스텝 배선) · `115e3665`(2026-09-17 · 콘솔 0에러 rc).

### ★★반대 증거도 잰다 — 기각을 그 한계 «위»에 세운다
`AGENTS.md` 가 그 이름을 적기 시작한 것은 `fc1784cb` **2026-09-17**, 즉 ★**하루 전**이다.
⇒ 측정된 것은 「**코드**가 3개월 안 움직였다」이지 「이 **쌍**이 안 갈렸다」가 **아니다**(문서 쪽은 표본이 없다).
★**정확한 진술은 「안정적이다」가 아니라 「지금까지 움직일 이유가 없었다」**이고, 기각은 그 위에 선다.

### 계약 1 — 「검사 전에 한 벌로 만들 수 있나」: 셋 다 값보다 비싸다
⒜**문서를 코드에서 생성** — `AGENTS.md` 는 손으로 쓰는 산문이다. 생성 단계를 넣으면 그 문서 전체가 「손으로 못 고치는」 것이 된다.
⒝**코드가 문서를 읽는다** — 제품 스크립트가 산문 마크다운을 파싱하게 만드는 것이라 기각.
⒞**공유 상수 파일** — 문서가 literal 을 적는 한 그 상수를 **다시 적어야** 하므로 사본이 줄지 않는다.

★**그래서 남는 것이 검사인데, 비-공허한 검사의 값이 여기서 음수다**:
문서는 이름뿐 아니라 ★**`<label>` 파생 규칙**(기본값 = 게임파일 basename)까지 적는다 ⇒ 문자열 대조 검사는 그 규칙의 ★**셋째 사본**이 된다.
★**사본을 늘려 사본 드리프트를 막는 형태**라 자기모순이고, 그것을 피하려면 제품 스크립트가 이름 생성을 `export` 하도록 리팩터해야 하는데 ★**그것은 이 티켓의 범위 밖**이다(계약 3⒝).

### 계약 2 — 잃는 것(★「없다」로 적지 않는다)
⑴★**제안이 지목한 드리프트 가능성은 «그대로 남는다»** — 값이 없다고 판정한 것이지 위험이 0이라고 판정한 것이 아니다.
⑵★**이 기각 자체가 낡을 수 있다** — 위 「반대 증거」가 그 이유다. 그래서 **재개 트리거를 측정 가능한 형태로** 남겼다(worklog `proposals[0]`):
  ⒤생성 두 줄이 바뀌는데 `AGENTS.md` 가 **같은 PR 에서 안 바뀐다** ⒥그 파일명을 **이름으로 읽는 소비자가 1개라도 생긴다**.
⑶★**그날의 가장 싼 구현도 미리 적어 뒀다** — `doc-liveness.yml` 주간 잡이 `npm run verify` 를 **이미 돌리므로** 그 실행이 두 파일을 **실제로 만든다**.
  그 뒤에 「문서가 적은 **그 정확한 두 이름**이 실재하는가」를 붙이면 규칙을 다시 적지 않고 **실제 코드 경로**를 판정한다
  (★「`verify_*.png` 가 있는가」류 **존재 검사가 아니다** — 계약 1 이 금한 그 공허함을 피한다). ※천장: 기본 실행 하나만 덮고 탐지 지연 최대 1주.

### 범위(계약 3)
⒜`verify-browser` 를 **언제** 돌릴지는 무접촉(`wie-verify-browser-as-post-deploy-step-decision` 리니지 몫 — ★재조회로 중복 0 확인)
⒝스크린샷 **내용·촬영 방식·해상도** 무접촉 ⒞**다른 산출물 이름으로 일반화하지 않았다**.
