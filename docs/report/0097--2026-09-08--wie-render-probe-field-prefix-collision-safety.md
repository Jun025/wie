## [2026-09-08] `lgt_render_probe.sh` 의 `field()` — ★제안이 지목한 결함은 «없었고», 옆에 «진짜»가 있었다 (wie-render-probe-field-prefix-collision-safety)
- **무엇을**: `scripts/lgt_render_probe.sh` 의 `field()` **하나**(13+/1−). ★**`.rs` 0줄 · 새 의존성 0 · 새 워크플로·잡 0 · 다른 로직 무접촉.**
- **왜**: 운영자 채택 `2026-09-06-validate-last-frame-richness#p1`.
- **★★⑴ 제안의 «기전»은 반증됐다** — 제안은 「비슷한 이름의 필드가 하나 더 생기면 조용히 다른 값을 읽는다」였다.
  ★**술어가 `"$1":` 라 이름 앞뒤가 «따옴표와 콜론»으로 이미 앵커돼 있다** ⇒ `last_frame_content` 는 `"content":` 를 만족할 수 없다.
  ★**심어서 확인했다**: `{"content":false,"last_frame_content":true}`(순서 양방향) · `{"paints_total":99,"paints":0}` ·
  `{"distinct_colors":7,"last_frame_distinct_colors":1}` — ★**옛 코드도 5/5 «맞는 값»**. ⇒ **접두 충돌 쌍 «0».**
- **★★⑵ 그러나 제안의 «관찰»은 옳았다** — 「**마지막으로 맞는 것을 집는다**」는 참이고, 그것이 `tail -1` 이다.
  ★**진짜 기전은 «접두»가 아니라 «같은 키의 반복»이다**: 중첩(`{"summary":{"paints":10},"frames":{"paints":3}}`)이나
  한 파일 두 문서에서 ★**옛 코드는 3 을 읽는다(기대 10)** — **위치로 조용히 결정**한다.
- **★⑶ 처방 — 「고르기」가 아니라 「말하기」**: 매치 수를 세어 **1건이 아니면 stderr 로 말하고**, 값은 **첫 매치**로 결정론화했다.
  ⇒ 반복 키에서 ★**옛 3 → 새 10** · 키 부재에서도 ★**「0건」을 말한다**(옛것은 빈 값을 조용히 냈다).
- **★⑷ 의존성 결정(채택 문면이 «선행»이라 못박은 것) = ⒞ 기존 관용구 유지**:
  `scripts/smoke_gate.sh:76` 이 **같은 `grep -o … | tail -1 | cut`** 로 JSON 을 읽는다 ⇒ ★**관용구가 이미 있고, 하나가 둘보다 낫다.**
  `jq` 는 로컬(`/usr/bin/jq`)·CI(`publish-artifact.yml`) 둘 다 실재하지만 ★**이 스크립트는 CI 에서 돌지 않는 로컬 전용**이라
  기여자 머신 보장이 없고, 한 스크립트만 `jq` 로 가면 **관용구가 둘**이 된다 ⇒ 버렸다. ⒝(awk 재구현)는 ⒞ 로 충분해 불요.
- **사용자 영향**: 없음(로컬 진단 스크립트). 현 JSON 에서 **값 3/3 불변**(위양성 0).
- **★남는 것**: ★**`smoke_gate.sh:76` 에 «같은» `tail -1` 이 있다** — 이 회차 범위 밖(티켓이 `field()` 하나로 못박음)이라 **고치지 않았다.**
  ★그 자리도 오늘 기준 단일 문서라 무증상이다. 제안으로 올렸다.
