## [2026-09-20] §C 의 「판정 유효 범위」를 고쳤다 — ★**검사기는 만들지 않았다. 기계가 «이미» 잡는 것을 재어서 확인했다** (wie-base-swap-must-not-silently-revert-ledger-section-c)

인용 트리: `origin/main` = `e22141ea` · 조회 2026-09-19T17:21Z. 채택 ref `2026-09-19-security-audit-schedule-red-three-days#p0`.

### ⒜⒝⒞ 실측 — 제안의 세 전제는 **전부 참이다**

| 무엇 | 실측 |
|---|---|
| ⒜`36df9c31` 이 두 crate 를 내렸나 | ★**참**. 부모↔자신 직접 대조: `rtrb` **0.3.5 → 0.3.4** · `event-listener` **5.4.2 → 5.4.1** |
| ⒝§C 가 그 버전을 «해소분»으로 적나 | ★**참**. C-1 「`cargo update -p rtrb` **0.3.3 → 0.3.5**」 · C-2 「`5.4.1` → **`5.4.2`** = 권고 `patched = [">= 5.4.2"]` 경계 도달」 |
| ⒞「판정 유효 범위」 칸의 실제 문면 | ★**참**. §C 머리: 「그 판정에는 «유효기간»이 있다 — … **의존성을 올리면 다시 재야 한다.** 그래서 아래 표에 **판정 유효 범위** 칸이 있다」 ⇒ **«올린다»만 가리키고 «되돌린다/교체된다»를 안 가리킨다** |

★**한 가지 보탠다**(제안이 말하지 않은 것): C-1 이 적은 해소분은 `0.3.5` 인데 swap 이 간 곳은 `0.3.4` 로
**C-1 의 출발점(0.3.3)보다는 높다**. 그래도 권고가 `^0.3.5 OR >=0.4.0` 이라 **해소는 무효**다 — 「되돌렸다」는 표현이 정확하다.

### ⒟ 갈래 판정 — ★**⒝를 골랐다. 그리고 그 이유는 «싸다»가 아니라 «이미 있다»이다**

브리프가 준 판정 축 4개로 재되, ★**축 하나를 «추가»했고 그것이 결정적이었다**:

| 축 | ⒜ 새 검사기(§C 표 ↔ lockfile) | ⒝ 체크 항목(기계 0) |
|---|---|---|
| ⑴표본 1건에서 굳히는 비용 | ★높다 — 사건 1건으로 파서를 굳힌다 | 없다 |
| ⑵파서 취약성 | ★높다 — §C 는 산문 칸이 많다(`판정 유효 범위` 칸이 통째로 산문). `check-audit-warnings.mjs` 는 §A 에서 이 문제를 **fail-closed** 로 다룬다(아래) | 해당 없음 |
| ⑶오탐이 만드는 `--ignore` 압력 | ★있다 — 이 저장소는 Constraint 5 로 `--ignore` 0건을 지킨다 | 없다 |
| ⑷사람 규율의 재현율 | — | ★**낮다**(이 집은 「사람 규율은 샌다」를 여러 번 겪었다) |
| ★**⑸[추가] 그 결함이 «이미» 기계에 잡히는가** | ★★**잡힌다 — 그래서 ⒜는 «두 벌»이다** | — |

★★**⑸를 «실측»했다**(현 `origin/main` 기준 · 원형 복귀 확인):

| 개악 | `cargo audit` | `check-audit-warnings` |
|---|---|---|
| **M1** `rtrb` 0.3.5→0.3.4 (=C-1 되돌림 · base swap 재현) | ★**rc=1** | — |
| **M2** `event-listener` 5.4.2→5.4.1 (=C-2 되돌림 · 비게이팅 계급) | rc=0(경고는 비게이팅) | ★**rc=1**(§A 집합 동등성) |
| 대조군(원형) | rc=0 | rc=0 |

⇒ ★★**게이팅·비게이팅 두 계급 «모두» 이미 red 로 말한다.** §C 를 lockfile 과 대조하는 새 검사기는
**같은 것을 두 번 잡는 기구**이고, 그것이 이 저장소가 반복해 규탄한 **«두 벌»** 이다.
★**그리고 ⑷(사람 규율은 샌다)가 ⒝의 약점인데, ⑸ 때문에 «샌 자리를 기계가 받는다»** — ⒝의 실질 위험이 그만큼 작다.

★**「그러면 무엇이 3일간 안 보였나」 — 탐지가 아니라 «자리»다.** `cargo audit` 은 **정확히 잡았고**(09-17·18·19 연속 red),
그 워크플로가 `schedule` 전용이라 **PR 단계에 뜨지 않았을 뿐**이다. ⇒ ★**남은 결함은 형제 `#p1`(PR 단계 편입)의 몫**이고
이 회차의 Non-goal 이다. ★**새 검사기를 만들었다면 «이미 잡은 것을 또 잡으면서 진짜 구멍은 그대로» 뒀을 것이다.**

★**⒜ 가 모르지 않도록 선례를 읽었다**(복제하지 않았다): `scripts/check-audit-warnings.mjs` 는 §A 표를
`| **A-<n>** |` 줄머리로만 잡고, **행을 하나도 못 찾거나** 대상/버전을 못 읽으면 ★**`failed = true` 로 «못 쟀다»를 운다**
(「★«못 쟀다»를 «이상 없다»로 넘기지 않는다 — 이 검사가 눈멀면 대장은 다시 조용히 낡는다」).
⇒ ★**파서 취약성은 «해결된» 것이 아니라 «fail-closed 로 감당»된 것**이고, 그 대가는 «대장 서식을 바꾸면 red» 다.
§C 에 같은 것을 하나 더 붙이면 그 대가를 **두 번** 치른다.

★★**⒝의 대가를 정직하게 적는다 — 기계는 «0»이다.** 이 회차가 더한 강제력은 **없다**.
남긴 것은 **산문 규율 한 문단**이고, 그것이 낡으면 아무도 말해 주지 않는다.
★**그럼에도 이것을 고른 것은 ⑸ 때문이다** — 규율이 새도 **결과는 red 로 드러난다**(3일이 걸렸을 뿐이고, 그 3일은 `#p1` 이 줄인다).

★★**그리고 ⒝의 «체크 항목»을 어디에 두었는지 밝힌다**: 브리프가 말한 「base swap 티켓 **템플릿**」은
`~/orchestrator/templates/` 에 있고 ★**이 repo 밖이라 wie 회차가 고칠 수 없다**(경계 ⒞ · `AGENTS.md` 「An external
contract is referenced, never copied」). ⇒ **repo 안에서 base swap 회차가 실제로 읽는 자리**, 곧 §C 머리에 넣었다.
※실측: 현재 `templates/` 에 **base swap 전용 템플릿은 없다**(`work-ticket.tpl` 이 범용) — 총괄이 그쪽에 넣기로 하면 별 회차다.

### ⒠ 고친 문면 — 전/후 축자

**전**
```
> ★★그리고 그 판정에는 «유효기간»이 있다 — 대개 «그때 그 판본의 소스»를 읽어 낸 결론이라
> 의존성을 올리면 다시 재야 한다. 그래서 아래 표에 판정 유효 범위 칸이 있다.
> ★그 칸이 빈 행을 만들지 마라 — 유효 범위 없는 판정은 낡은 채로 권위 있게 인용된다.
```
**후**(요지 — 전문은 `docs/project-kb/02_status.md` §C 머리)
- 「의존성을 **올리면**」 → 「의존성이 **움직이면**」
- ★**이 칸이 겨누는 사건을 «셋»으로 명시**: ⑴상향 ⑵★**lockfile·매니페스트의 통째 교체**(base swap · upstream 병합 ·
  `cargo update` 전량) ⑶**판정 전제의 변화**(feature 조합 · `[patch]` 핀 — ★**버전이 그대로여도 무효가 된다**)
- ★「**base 를 갈아끼우는 회차는 이 표의 C 행을 «전건» 재확인하라**」 1줄
- ★**「기계가 이미 잡는 것을 여기서 다시 만들지 마라」** + 위 M1·M2 실측 + 「남은 결함은 «탐지»가 아니라 «그 red 를 언제·어디서 보는가»」

★⑶(전제 변화)은 제안에도 브리프에도 없던 축이다 — C-3 의 유효 범위가 **feature 조합**이라
**버전이 안 움직여도 무효가 되는** 갈래가 실재하기 때문에 넣었다.

### ⒡ 개악 양방향 — ★**해당 없음**(⒝를 골랐다)
브리프 계약 4 는 「⒜를 고른 경우에만」이다. ★**대신 «⒝를 고른 근거»를 개악으로 냈다** — 위 M1·M2 가 그것이다
(새 기구가 아니라 **기존 기구**가 두 계급을 잡는지를 양방향으로 쟀고, 원형 복귀까지 확인했다).
※문서만 바꿨으므로 `check-audit-warnings.mjs` 의 §A 파서에 영향이 없음을 실행으로 확인했다(**rc=0**).

### 안 한 것
- §A·`check-audit-warnings.mjs` **무접촉**(읽기만) · §C 의 **판정 내용 변경 0**(바꾼 것은 «유효 범위 문면»뿐).
- 의존성 버전 **상향 0** · `--ignore` **0** · 형제 `#p1`(PR 단계 편입) 무접촉.
- ★**새 검사기 0** — 위 ⒟ 의 이유로 «만들지 않기»를 골랐고, 그 선택의 대가(기계 0)를 명시했다.
