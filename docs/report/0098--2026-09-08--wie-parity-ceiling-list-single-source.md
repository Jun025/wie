## [2026-09-08] 파리티 «천장 목록»을 한 벌로 — 정본은 «검사가 인쇄하는» 쪽이다 (wie-parity-ceiling-list-single-source)

**무엇을**: `wie_cli/tests/support/dod_ci_parity.rs` 의 `ceilings()` 에 ⑴**정본 선언** ⑵**빠진 2항**(⑧순서·중복 ·
⑨줄끝/CRLF) ⑶**⑤ 문면 정정** — 그리고 사본(`docs/worklog/2026-09-05-dod-ci-parity-checker.json`)에
**「스냅샷이지 정본이 아니다」 1키**. ★**술어·임계·워크플로·잡 무접촉 · 코드 동작 0 · 새 천장 0.**

**왜**: 운영자 채택 `2026-09-06-parity-outside-gate-decision#p0` — 「못 보는 것 목록이 «두 군데» 있고 «내용이
다르다 · 한쪽이 낡는다»」. ★고칠 것은 «항목»이 아니라 ★**«정본이 하나인가»** 다.

## F1 — 두 목록을 나란히 놓고 «양방향» 차집합

| 쪽 | 위치 | 항 수 |
|---|---|---:|
| 코드 | `wie_cli/tests/support/dod_ci_parity.rs` `ceilings()` | **7** |
| 사본 | `docs/worklog/2026-09-05-dod-ci-parity-checker.json` 키 **`ceilings`** | **9** |

```
코드에만 있는 것    : 0건
사본에만 있는 것    : 2건 — ⑧순서·중복 · ⑨줄끝(CRLF)
```
⇒ ★**제안의 「2항 추가」 가정이 «맞다»**(확인했지 승계하지 않았다).

★★**그리고 «문면 갈림»이 2건 더 있었다 — 수가 같아도 말이 다르면 그것도 갈림이다**:
- **⑤** 코드 「정규화가 «공백 접기»뿐」 ↔ 사본 「«공백 접기 + 꼬리 주석 제거»」 ⇒ ★**사본이 맞다**
  (`:317` 이 `norm(strip_comment(line.trim()))` 이다) ⇒ **정본을 그 값으로 정정했다.**
- **③** 코드는 2026-09-06 «넓히지 않는다» **결정**을 담고, 사본은 그 **결정 «전»** 문면이다 ⇒ ★**코드가 최신**
  (선행 회차 `wie-parity-lock-ceiling-three-promote-to-decision` · merged `d665f477` 이 승격했다).
  ★**그 결정은 손대지 않았다**(diff 상 ③ 관련 ± **0줄**).

## F2 — 정본은 ⒜**코드** · 버린 이유

| 안 | 판정 | 이유 |
|---|---|---|
| ★**⒜ 코드가 정본** | **채택** | ★**검사가 «매 run 에 실제로 인쇄»하는 목록**이라 green 을 읽는 사람과 목록을 읽는 사람이 **같은 것**을 본다 ⇒ 갈림이 **구조적으로** 막힌다. 대가 = 사본은 사료라 지울 수 없다 ⇒ **「그 시점 스냅샷」임을 명시**했다(아래) |
| ⒝ worklog 가 정본 | **기각** | ★**검사기가 «읽지 않는» 곳이 정본이 되면 갈림이 «다시» 난다** — 이번 갈림이 정확히 그 형태다(아무것도 인쇄하지 않는 사본이 9항으로 자랐다) |

**정본 선언**(코드에 실재 · 인쇄된다):
> `[천장 — 이 검사가 «못 보는» 것 · ★이 목록이 «정본»이다(다른 곳의 목록은 그 시점 «사본»)]`
> + doc 주석 `★THIS LIST IS THE SINGLE SOURCE OF TRUTH … if they disagree, this one wins.`

**사본 표시**(worklog 에 실재): `ceilings-are-a-snapshot-not-the-source-of-truth` 1키 —
★**`ceilings` 배열 9항은 «무접촉»**(사료라 지우지 않는다).

## F3 — 추가는 «차집합 그대로» · 발명 0

⑧⑨ 는 사본 문면을 옮기되 **worklog 내부 참조**(「위 mutation-c」·「아래 제안 #p0」)는 뺐고, 옮기기 전에
**실측으로 확인**했다: ⑧ `BTreeSet` 집합 비교(`:23,34-37`) ⇒ 순서·중복 미관측 ·
⑨ `tests/dod_ci_parity.rs:26 fn read()` 가 `raw.replace("\r\n","\n")` — ★**정확히 한 곳**.
★**새 천장 0.**

## 코드 동작 0 — 개악으로 보였다

```
M-A(드리프트): AGENTS.md lint 게이트 --all → --workspace
  → dod_region_and_rust_yml_agree  FAILED   (10 passed / 1 failed)   ★기존 축이 여전히 red
원복 → 11 passed / 0 failed
```
★그 실패 출력에 **정본 선언·⑧·⑨ 가 그대로 인쇄**됐다 ⇒ 「목록이 읽히는 자리에 있다」가 실증된다.

**사용자 영향**: 없다(시험 문면 · 제품 코드·CI 무접촉). 값은 ★**「이 검사가 무엇을 보장하는가」의 답이
한 벌이 됐다**는 것이다.
