## [2026-09-07] 파리티 락 축⑷ — 별칭을 «하드코딩»에서 «파일에서 읽기»로 바꿨다 (wie-parity-lock-axis4-module-alias-decision)
- **무엇을**: `scripts/check-parity-lock-wired.mjs` 의 `LOCK.callsChecker: "checker::parity("`(하드코딩)을 ★**시험의 `mod <이름>;` 에서 별칭을 읽어 `<별칭>::parity(` 를 요구**하는 형태로 교체. ★**의존성 0 · 워크플로 0 · 다른 축 무접촉.**
- **왜**: 운영자 채택 제안 `2026-09-06-parity-lock-guard-axes#p0`. ★**「고치는」 회차가 아니라 «정하는» 회차**였고, 셋(⒜파서·⒝술어 조이기·⒞안 함) 중 ⒝를 골랐다.
- **★★F1 재현 — 그리고 ⒜의 «해석»을 정정했다**: 두 방향 다 출력으로 재현했다. ★**위양성이 진짜 결함**이다 — `dod::parity(` 로 **전건 개명**하면 `cargo` **rc=0(11 passed · 락은 정상 작동)** 인데 가드만 **rc=1** 이고, 그 메시지가 «이제 없는 철자»를 가리킨다. ⇒ 정당한 리팩터가 CI 를 붉힌다. ★**반면 ⒜(`parity_checker`)는 가드 0 · cargo 0 인데 그것은 «옳은 답»이다** — 일관 개명은 배선을 깨지 않는다. ⇒ ★**제안이 「위음성」이라 부른 것은 «무해»했고, 값하는 것은 위양성 쪽이었다.**
- **★★자인 — 재현을 두 번 틀렸고 둘 다 스스로 잡았다**: ⑴처음엔 `checker::parity(` 만 바꿔 **부분 개명**이 됐고 `checker::parse_dod` 등이 남아 cargo **101**(개명 실패이지 위음성 아님) ⑵고친 뒤엔 검산 `assert 'checker::' not in s2` 가 ★**`parity_checker::` 안의 부분문자열에 걸려** 파일이 **아예 안 쓰였는데** 0/0 을 «재현»으로 읽을 뻔했다. ⇒ ★**경계 있는 정규식(`(?<![A-Za-z0-9_])checker::`)으로 다시 재서 확정**했다. ★**이 회차가 고친 결함과 «같은 형태»의 실수**다(부분문자열 일치).
- **★F2 비용 — 내가 다시 쟀다**: ⑴`tree-sitter` 는 **상시 구간에 `npm ci` 를 새로 넣어야** 한다 — 실측: 상시 스텝 **7개가 전부 Node 내장만** 쓰고 `npm ci` 는 **필터 «안»에만** 있다(`:238`) ⇒ ★**「문서만 고친 PR 도 문다」는 참**이다. ⑵`cargo … -- --list` 는 ★**이 축을 못 덮는다 — 실측으로 확정**: 개명 전/후 `--list` 출력이 **바이트 동일**(11줄) ⇒ 비용을 따질 필요도 없이 탈락. ※웜 **1.26s**(단일 타깃 기준 · 제안의 5.69s 는 다른 범위로 보인다 — **맞추지 않고 다르게 적는다**). ⑶`callsChecker` 는 **상수 1곳**(정의 `:55` · 소비 `:107-108`) ⇒ 「사람이 고치면 된다」도 참이었다.
- **★교환을 «수»로**(개정 전 → 후 · guard/cargo): 무개악 `0/0 → 0/0` · `parity_checker` `0/0 → 0/0`(불변 · 옳다) · ★`dod` **`1/0 → 0/0`**(위양성 해소). ★**회귀 방지도 확인**: `parity(` 호출을 전부 없애면 여전히 **guard 1** · `mod` 선언을 없애면 ★**guard 1(fail-closed)** — 「판정 불가」가 «통과»로 읽히지 않는다.
- **대가**: 의존성 **0** · 워크플로 **0** · 가드 실행 중앙값 **0.447s → 0.588s**(7회 · 부하 있는 맥). ★**0.1초대 차이를 «정규식 한 줄»의 몫이라 주장하지 않는다** — Node 기동 지터와 구분되지 않는다. 새 I/O·새 의존성은 **없다**.
- **★남는 천장(갱신)**: 별칭 «선언»만 읽지 호출부를 파싱하지 않으므로 `mod` 줄이 없는 배선(예: `use` 재수출)은 ★**판정 불가 = 실패**로 떨어진다. 그리고 여전히 Rust 파서가 아니다 — 그 기각 사유(상시 `npm ci`)를 천장에 함께 적었다.
- **사용자 영향**: 없음(CI 가드). 대신 ★**정당한 개명이 더 이상 CI 를 붉히지 않는다.**
