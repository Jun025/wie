## [2026-09-16] graphics 27줄 ⒜/⒝ 결정은 «이미 났다» — 채택된 순간 이미 8시간 지난 제안이었다 (wie-adopt-slice-d-blocked-on-lgt-abi-decision-p0)

### 무엇을

채택 제안 `2026-09-16-slice-d-blocked-on-lgt-abi-decision#p0`(원문 =
`docs/worklog/2026-09-16-slice-d-blocked-on-lgt-abi-decision.json` `proposals[0]`)은
「**graphics 27줄 배선의 ⒜/⒝ 를 운영자·총괄이 «명시로» 고른다**」였다.

★**그 결정은 이미 났고 집행까지 끝났다.** 총괄이 **⒝(공용 구현 복귀)** 를 «명시로» 골랐고
**PR #161** 이 **2026-09-16T08:35:30Z** 에 착지시켰다 — 이 제안이 tower 에서 채택된 것은
**13:06Z**(지시 주입 시각)이므로 ★**채택 시점에 이미 약 4시간 30분 지난 제안**이다.

**제품 코드 0줄.** 이 회차의 변경은 원장 3파일(이 리포트 · worklog · `STATE.md`)이다.

### 대전제 재고 — 네 축을 «전부» 실측했다

「이미 했다」는 주장은 **문서를 읽은 것으로 끝내지 않았다.** 네 축을 각각 쟀다.

| 축 | 실측(`origin/main` `7921b0f8`) | 판정 |
|---|---|---|
| ⒜ **결정이 배선돼 있는가** | `wie-lgt/src/runtime/wipi_c.rs` → `=> wie_wipi_c::api::graphics::` **27** · `=> graphics::` **0** | ★⒝ 집행됨 |
| ⒝ **«명시로» 골랐다는 기록** | `docs/upstream-realign-p3-slices.md` §D 「총괄이 ⒝(공용 구현 복귀)를 **«명시로» 골랐고**」 | ★실재 |
| ⒞ **tradeoff 가 요구한 «대가 기록»** | 같은 절 「**대가 — 축소하지 않는다**: benefit 「LGT 전용 그래픽 0→1,095줄」은 **«철회»가 아니라 «연기»**」 + 되돌림 조건·비용 | ★이행됨 |
| ⒟ **그 대가가 «지금도» 참인가** | `graphics.rs` **1,095줄** · `git diff --numstat upstream/main` **출력 0**(바이트 동일) · 모듈 선언에 `#[allow(dead_code)]` + 「This is a DEFERRAL, not a rejection」 2줄 · 그 경로 참조 파일 **1**(선언 자신) | ★참 |

⇒ ★**제안의 요구(결정) 와 그 tradeoff 의 요구(대가 문서화) 가 둘 다 이미 충족돼 있다.**
ⓑ **같은 것을 하는 축 0**(중복 결정 기구 없음) · ⓒ **제안이 틀린 것은 아니다** — 제안을 낸 회차 시점에는
**정확히 참**이었다. 틀린 것은 **시점**이고, 그것이 이 회차가 기록할 사실이다.

### 그런데 «읽었다»로 끝내지 않았다 — 개악 대조로 못박았다

문서가 「결정했다」고 적는 것과 **제품이 그 결정대로 동작하는 것**은 다르다. 격리 worktree(`origin/main`)에서
**제품 호출부**(`wie-lgt/src/runtime/wipi_c.rs` 의 배선 27줄)를 ⒜(LGT 전용)로 역치환했다:

| 배선 | `keydraw_lgt --inject --expect-last-frame` |
|---|---|
| **정상 ⒝**(공용 구현 27) | ★**PASS · paints 55 · `last_frame_content` true · rc=0** |
| ★**개악 ⒜**(LGT 전용 27) | ★**FAIL · paints 0 · `last_frame_content` false · rc=1** |

⇒ ★**결정은 «산문»이 아니라 «제품에서 무는» 상태다.** 그리고 이것은 조각 A 의 원 관측
(⒜ = `keydraw_lgt` FAIL 3/3)을 **독립으로 재현**한 것이기도 하다.
★`paints 55` 는 조각 D 기록(`51`)과 다르지만 **회귀가 아니다** — AGENTS.md 가 그 수를 **바닥(floor)** 으로
못박았고 측정 범위(idle 48~55) 안이며, ★**판정축인 `result`·`content`·`rc` 셋은 동일**하다.

### 무엇을 잃는가 / 안 하면 무엇이 나쁜가

- **잃는 것 ⑴ — 커버리지도 결정도 1비트 늘지 않았다.** 이 회차는 **확인**했을 뿐이다.
  ★「측정했다」를 「진전시켰다」로 읽으면 안 된다.
- **잃는 것 ⑵ — 회차 하나를 썼다.** 이미 끝난 일에 워커 슬롯 하나가 들어갔고, 그 비용은 실재한다.
  ★그 대가로 사는 것은 **아래 「안 하면」** 하나뿐이다.
- **안 하면**: ★**같은 제안이 내일 또 채택된다.** 이 제안은 tower 패널에 **집행이 끝난 뒤에도 남아 있었고**
  실제로 그 상태에서 채택됐다. `adoptedProposals` 기록이 **그 재채택을 막는 유일한 수단**이고,
  ⇒ ★**이 회차의 진짜 산출물은 worklog 의 그 한 줄**이다(리포트·STATE 는 그 근거다).

### 범위 밖 — 일부러 안 한 것

- ★**결정을 다시 하지 않았다.** ⒜/⒝ 중 실게임에 옳은 쪽은 **여전히 미측정**(292 코퍼스 부재)이고,
  그 축은 `humansteps/wie-p2-corpus-placement` 카드가 진다(2026-09-16 에 그 카드에 «첫 질문»이 박혔다).
- ★**`docs/upstream-realign-p3-slices.md` 를 고치지 않았다** — §D 의 기록이 **이미 정확**하다.
  ※§5(280행)의 「LGT 전용 그래픽 0→1,095줄」은 **P2 코퍼스 갈래(갑/을) 비교표**의 항목이고
  ⒜/⒝ 결정의 benefit 줄이 아니다. 그 절은 스스로 「**이 회차는 고르지 않는다**」라고 선언한 계획 기록이다
  ⇒ ★**낡지 않았으므로 손대지 않았다.**
- 형제 `#p1`(연번 `0113` 이중 claim) **무접촉** — 별 티켓이고, 그 사이 해소됐다(열린 PR claim 중복 **0**).
- 코드 `.rs` 접촉 **0** · 픽스처 **0** · 검사기 배선 **0**(개악은 격리 worktree 에서만, 제거 확인).
