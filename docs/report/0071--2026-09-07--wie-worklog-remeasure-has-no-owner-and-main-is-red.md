## [2026-09-07] 재측 약속의 «소유자»를 게이트③으로 고정하고 main 을 푼다 (wie-worklog-remeasure-has-no-owner-and-main-is-red)

**무엇을**: ⑴ 착지 #53 이 주기를 넘겨 밀린 재측을 멱등 `--record` 로 기록했다(항목 1건 추가 · 기존 항목 무접촉).
⑵ ★**소유자를 «게이트③ 회차»로 정했다** — AGENTS.md §Landing paperwork 에 규칙으로, 그리고 검사기의
OVERDUE 메시지에 `OWNER:` 절로 박았다. ⑶ `docs/worklog-coverage-remeasures.json` 을 **이 저장소의 원장 파일
목록에 추가**했다(그 파일을 만질 «권한»이 어디에도 없던 것이 이번 red 의 실제 구멍이다).

**왜**: 24시간 안에 같은 결함의 **양쪽 면**이 다 나왔다 — 2026-09-06 은 여러 회차가 «각자» 기록해 **중복 3건**,
2026-09-07 은 **아무도 기록하지 않아 main red**. 멱등 `--record`(`be37ca7d`)가 앞쪽을 닫았고 뒤쪽은
「누가 돌리나」라는 **미해결 질문**으로 남아 있었다. 두 워크플로가 `fetch-depth: 0` 이라 `origin/main` 이
`pull_request` 런에도 있으므로, 밀린 동안 ★**main 뿐 아니라 열린 PR 전건이 red** 다 — 즉 막히는 것이
정확히 게이트③이고, 그래서 게이트③이 소유자다.

**사용자 영향**: 없다(도구·문서·기록만). ★**비율·임계·판정식·워크플로 스텝은 한 바이트도 건드리지 않았다** —
진 것은 «비율»이 아니라 «기한»이었고(10/10 = 100%), 스텝을 끄거나 required 를 푸는 것은 결함을 숨기는 것이다.
★**남는 구멍을 수로 적었다**: 게이트③이 없는 동안은 여전히 red 다. 주기 10 은 **9h 18m**, 연속 착지 간격은
중앙 **70.5m** 이지만 **최대 115.2h** 다 — 조용한 구간에서는 며칠 갈 수 있다. 그것을 «고치지 않고 받아들인»
근거는 하나다: 이 검사는 **착지로만** 밀리고 **착지만** 막으므로, 조용한 구간에는 기다리는 것이 없다.
