## [2026-09-07] 핀이 움직여도 «어휘 축»은 이제 기계가 진다 — ⒜(상시 검사)를 골랐다 (wie-transmute-census-on-pin-bump-decision)

**무엇을**: `wie_wipi_c/src/lib.rs` 에 `#[cfg(test)]` 잠금 하나(**시험 1건**)를 넣었다.
★**워크플로 변경 0 · 새 의존 0 · CI 스텝 0 · `~/.cargo` 읽기 0** — `cargo test --all` 이 이미 도는 자리에 얹었다.

**★[2026-09-07 정정] 초판의 F1⑷ 는 «틀렸다»** — 게이트②가 반증했고 이 회차가 재현했다. 초판은 「lock 이 재생성될 때마다
움직인다 · 최근 이동은 RUSTSEC 범프의 부수효과 · 읽어 줄 핀 범프 회차가 없다」고 적었다. **셋 다 거짓이다.**
lock 줄을 «실제로 바꾼» 커밋 전수(`git log --full-history -m -G… -- Cargo.lock`):
★**31건 · 2025-07-02 ~ 2026-04-11(283일 · 평균 9.4일/회)** ⇒ ★**그 뒤 149일째 그대로**이고, 그 RUSTSEC 커밋(`74c924f0`)의
`Cargo.lock` diff 에 wipi 줄은 ★**0건**이다. 그리고 ★**31건 중 19건이 dependabot 전용 PR**(`Bump wipi_types from X to Y`)이며
`.github/dependabot.yml` 은 지금도 cargo 를 **daily** 로 돌린다 ⇒ ★**「읽어 줄 회차가 없다」도 거짓이다.**

**그래서 ⒜ 를 «다시» 판정했고 — 유지한다. 다만 논거가 바뀐다**: 초판이 «빈도»에 기대던 논거는 폐기다.
★**남는 근거는 «커버리지»와 «비용»이다**: ⒝(체크리스트)·⒞(스크립트)는 이제 **가능**하지만(dependabot 회차가 실재한다)
★**19건만 덮고 나머지 12건(기능 커밋 부수효과)을 못 본다** — 그쪽에는 읽어 줄 회차가 없다.
시험은 **31건 전부**에 걸리고, dependabot PR 도 CI 를 타므로 그 경로까지 함께 덮는다.
★**비용이 0에 가깝다는 것이 빈도를 무의미하게 만든다** — 새 스텝·의존·경로 0, 이미 매 커밋에 도는 스위트의 한 케이스다.
⇒ ★**주 단위로 움직이든 5개월을 서 있든 켜 둔다**(후자가 실제로 일어난 일이다).

**왜 ⒜ 가 가능했나 — 제안의 세 반박은 «특정 구현»에 대한 것이었다**(`~/.cargo` 를 읽는 스크립트).
`wipi_types` 는 ★**평범한 워크스페이스 의존**이라 시험이 그냥 `use` 한다 ⇒ ⑴경로 차이 없음 ⑵오프라인 문제 없음
(그것 없이는 애초에 빌드가 안 된다) ⑶`cargo test --all` 은 `AGENTS.md` 가 **이미 모든 커밋에 요구**하므로 새 스텝이 없다.
★파리티 락이 tree-sitter 를 기각한 근거(「상시 구간에 `npm ci` 를 새로 넣어야 한다」)도 ★**해당하지 않는다** — 넣은 것이 0 이다.

**사용자 영향**: 없다(시험 1건). ★**정직한 범위**: 이것이 잠그는 것은 «어휘»(변종 추가·개번·삭제)이고,
★**«누가 `from_raw` 를 지나는가»는 잠그지 못한다** — 그것은 `wipic_sys` 에 있고 이 워크스페이스의 의존이 아니다
(주석에만 등장한다). ★그 절반은 여전히 문서의 「뒤집힐 조건」이고, 그렇게 시험 주석에 적었다.
