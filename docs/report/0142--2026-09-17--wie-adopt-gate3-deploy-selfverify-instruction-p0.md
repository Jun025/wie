## [2026-09-17] 제안이 시킨 대로 «먼저 쟀다» — 그리고 그 계측기가 «틀렸다» (wie-adopt-gate3-deploy-selfverify-instruction-p0)

**무엇을**: 제안 `2026-09-17-gate3-deploy-selfverify-instruction#p0` 채택. ★**검사기를 «만들지 않았다»** —
제안 자신이 「먼저 «문서만으로 얼마나 고쳐지는지»를 다음 33건으로 재는 편이 싸다」고 처방했기 때문이다.
그 측정을 실제로 했고, ★**그 측정에 쓰라던 술어가 답을 낼 수 없다**는 것이 이 회차의 산출물이다.
접촉 = `AGENTS.md` 1곳(±27/8). ★**제품 코드 0줄 · Rust 0줄 · 기계 강제 0줄 신설.**

### ⓐ 전제 재측 — 제안은 참이지만 «수»가 틀렸다
| 분모 | 고르는 축 | n | `WIE_BASE` 인용 |
|---|---|---|---|
| 회신에 `Deploy to Cloudflare Pages` 리터럴이 있는 것 | **어휘** | 36 | 18 (**50%**) |
| ★**실제로 착지한 것**(`merged:` 가 sha) | **사실** | ★**107** | ★**57 (53%)** |

제안·`AGENTS.md` 가 들고 있던 「131건 중 33 배포 수반 · 15(45%)」는 **위 1행**이다(오늘 재측 36/18).

### ⓒ 그 술어는 «두 방향으로» 틀렸다 — 둘 다 실측이다
⑴★**보상한 회차를 «떨어뜨린다».** 오늘 `#177`·`#178` 을 착지시킨 게이트③ 두 회차는
   **브라우저 검증을 실제로 돌렸고**(`rc=0` · `console errors 0`) 회신에 **`WIE_BASE` 를 인용한다**.
   그런데 그 문구(`Deploy to Cloudflare Pages`)를 **쓰지 않아** 분자에서도 **분모에서도 빠진다**
   ⇒ ★**준수가 올라도 비는 움직이지 않는다.** 계측기가 자기 목적을 무력화한다.
⑵★★**분모 자체가 틀렸다 — 이 repo 에는 «배포 없는 착지»가 없다.**
   `web.yml` 의 `push` 트리거는 `branches:[main, …]` 에 ★**`paths` 필터가 없다**(YAML 파싱 확인)
   ⇒ **main 에 들어가는 모든 착지가 배포한다.** 실증: 최근 머지 회신 12건의 `merged:` sha 에 대해
   GitHub 에 `Web` 런 존재를 물으니 ★**12/12**.
   ⇒ 「107 착지 중 36 만 배포 수반」은 **구성상 거짓**이다.

### 처방 — 문서의 «수»와 «조건절»을 고쳤다(그 둘이 이 repo 안에 산다)
- `AGENTS.md` §verify-browser: 단일 문장이던 측정을 **2행 표**로 바꾸고 ★**「2행을 읽어라」**를 명시.
  실패 두 축(⑴⑵)을 그 자리에 적었다.
- ★**조건절 제거**: 「a gate③ round that **lands a deploy**」 → ★**「every gate③ round that lands」**
  (＋「there is no deploy-less landing here」). ★**이 repo 에서 4-C 의무는 «조건부»가 아니라 «무조건»이다.**
- ★**검사기를 만들 사람에게 축을 남겼다**: 「key it off the `merged:` sha and GitHub's run list,
  **never off reply wording**」 — 1행이 어휘 술어가 무엇을 하는지 보여 주는 증거다.

### 왜 «검사기»를 만들지 않았나 — 근거 셋
⑴★**제안이 그렇게 처방했다**(「먼저 재는 편이 싸다」). ⑵★**`target: orchestrator`** 이고 회신은 `~/orchestrator`
트리라 제품 repo CI 가 **볼 수 없다**(제안 자신의 tradeoff). ⑶★**그리고 지금 만들면 «틀린 술어 위에» 만든다** —
제안이 제시한 유일한 값싼 경로(`bin/done-lint` 에 「배포 수반이면 `WIE_BASE` 필수」)는 «배포 수반»을
**회신 문구로** 판정하는데, ⑴⑵ 가 그 판정이 **이미 깨져 있음**을 보였다.
⇒ ★**이 회차의 실익은 「검사기를 안 만든 것」이 아니라 「만들었으면 틀렸을 것을 보인 것」이다.**

### 개악 대조(양방향) — ★**무엇을 물었고 무엇을 «못» 물었는지 가른다**
| 상태 | `check-doc-liveness-parity` |
|---|---|
| 정상(명령이 **인라인**) | **rc=0** (25 documented lines) |
| 개악: 그 명령을 ```` ```sh ```` **펜스**로 | ★**rc=1** (`DOC ONLY — … + WIE_BASE=…`) |
| 복원 | **rc=0** |

★**정직하게 적는다 — 이 쌍이 무는 것은 «인라인 불변식»이지 «내가 고친 산문»이 아니다.**
그 불변식은 **직전 회차가 세운 것**이고, 이 쌍이 증명하는 것은 ★**내 편집(±27/8)이 그것을 깨지 않았다**는 것이다.
★**내가 고친 «수와 조건절»에는 기계 소비자가 없다** — 그래서 판정식 ⑴ 은 **그 축에서는 못 쟀다**(사유는 위 「왜 검사기를 안 만들었나」).

### 전 스위트
`fmt --check` OK · `clippy --all -D warnings` rc=0 · wasm clippy rc=0 · `+beta clippy` rc=0 ·
`cargo test --all` **전건 합산 385 passed / 0 failed**(result 줄 42 · `FAILED` 0 · `panicked at` 0) ·
검사기 **9종 전건 rc=0**(doc-liveness-parity · docs-report-serial · worklog-json · parked-workflows ·
★upstream-new-workflows(어제 착지분) · worklog-coverage · parity-lock-wired · linux-system-deps · `npm run audit`).

### 잃는 것 / 안 하면 무엇이 나쁜가
**잃는 것**: ⑴★**의무가 «넓어졌다»** — 「배포 수반이면」이 「착지하면 언제나」가 됐다. 게이트③ 회차마다
브라우저 실행 1회(실측 약 20~30초 + 배포 완료 대기)가 **무조건** 붙는다. ★그것이 사실에 맞지만 **비용은 실재한다.**
⑵★**표의 두 수도 «오늘의 수»다** — 상수로 인용되면 같은 병이 반복된다(그래서 축을 함께 적었다).
★**「잃는 것이 없다」고 적지 않는다** — ⑴이 실질이고, 그 대가로 산 것은 「배포가 깨졌는데 아무도 모르는 구간」의 축소다.
**안 하면**: `AGENTS.md` 가 ⒜**낡은 수**(45%/33)와 ⒝**거짓 조건절**(「배포 수반이면」 — 이 repo 엔 예외가 없다)을
계속 가르친다. 그리고 다음 회차가 제안대로 「33건으로 재」면 ★**움직이지 않는 비를 보고 「문서가 안 먹혔다」로 오독**해
**틀린 술어 위에 검사기를 만든다.**

### 열어 둔 것
- **검사기 자체는 `orchestrator` 소관**이다(제안 `target` 그대로). 이 회차는 **축만** 남겼다.
- 1행 술어(`Deploy to Cloudflare Pages`)를 쓰는 다른 곳이 있는지는 **세지 않았다** — 이 파일 밖은 범위 밖이다.
