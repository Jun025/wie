## [2026-09-08] §완료의 «맨 위 삽입 지점» — ★**그대로 둔다**(⒞ 채택 · 남은 3/10 은 «알고 남긴 값») (wie-state-completed-top-insert-residual-three)
- **무엇을**: ★**판단 회차 · 코드 변경 0 · `STATE.md` §완료 형식 무접촉.** 산출은 `AGENTS.md` §Landing paperwork 의 기록 블록.
- **왜**: 운영자 채택 제안 `2026-09-08-state-in-progress-pointer#p0`(직전 회차가 스스로 남긴 축).
- **★★⑴ 제안의 수를 «두 곳» 정정했다**(둘 다 이 회차 실측):
  ⒜**전향 충돌률은 3/10 이 아니라 «1/1»** — 3/10 은 과거 표본의 성질이지 예측이 아니다. `b0a08d73` 에서 일회용
  워크트리로 두 회차를 `main` 에서 **독립 분기**시켜 각각 §완료 머리에 1항목씩 넣으면 **CONFLICT**(`merge-tree` rc=1) ·
  한쪽을 **1줄** 내리면 **clean**(간격 1·2·3 전건 rc=0). ⇒ 동시 회차는 **항상** 부딪히고, 이 저장소는 하루 **10~25건** 착지한다.
  ⒝**`docs/report/` 는 §완료의 상위집합이 «아니다»** — 87항목 중 83개가 id 를 갖고 그중 **3건이 `docs/report/` 에 없다**
  (`wie-pr45-orphan-close-and-remnant-land-r2` · `wie-state-landed-pr56-residue-and-misc-unk9-error-lock` ·
  `wie-supply-chain-cargo-updates-a2-a3`) · id 없는 구형 4건 추가 ⇒ ★**포인터화하면 «다른 데 없는» 7항목이 사라진다.**
  ★★**id 추출 술어에 주의 — 백틱으로 감싼 것만 뽑으면 «82/5» 가 나오고 그것은 틀리다**(초판이 그랬고 게이트②가 잡았다).
  `2026-07-22` 항목은 id 를 **백틱 «경로» 안**(`` `~/orchestrator/reports/wie-main-branch-protection.done.md` ``)에 두어
  그 술어에서 «id 없음»으로 떨어지는데, 그 항목이 묶은 세 회차는 `docs/report/` 에 **전부 있다**(`0001`·`0002`·`0003`).
  ⇒ ★**`wie(-[a-z0-9]+){2,}` 처럼 백틱과 무관한 술어로 뽑아라.** ★그 술어는 어디에도 박혀 있지 않다(일회성 조사).
- **★★⑵ 그 7건이 §진행중과의 «성격 차이»의 실체다**: §진행중은 `gh pr list` 의 **낡은 사본**이었고 직전 회차가
  「항목 1 → 0 · 잃은 서술 **0**」으로 옮겼다. §완료는 사본이 아니다 ⇒ ★**같은 처방이 이식되지 않는다**(포인터 교체가 아니라 이관이다).
- **★⑶ ⒝(분산)는 «측정으로» 기각**: 필요한 간격은 «동시 회차마다 1줄»인데, 하루 10~25건이면 다투는 회차는 **같은 날**에 있다
  ⇒ 월·연 소절은 다시 같은 줄에 모인다. 회차별 파일만이 가르는데 그것은 PR #114 가 이미 기각한 형상이다.
- **★★⑷ 비용을 재서 «값한다»고 판정했다**: ★**기계 소비자 0** — 추적 코드/설정 **344파일**에서 `## 완료` 파서 **0** ·
  `STATE.md` 언급 **1**(그마저 Rust doc 주석 산문). ⇒ 충돌은 **어느 게이트도 막지 않고**, 비용은 회차가 어차피 하는
  base 당김 «안»의 union 편집 하나다. ★**남는 진짜 위험은 «조용한 소실»**(한쪽 통째 채택 → 게이트 green) —
  그래서 union 규약의 «양방향 보존 계수»가 필수다.
- **사용자 영향**: 없음(형식 무변경). 대신 **다음 사람이 같은 조사를 다시 하지 않는다** — 그것이 이 저장소가 반복해 치른 비용이다.
- **★되돌릴 신호 3종**(`AGENTS.md` 에 함께 적었다): ⒜§완료의 기계 소비자가 생긴다 ⒝위 7항목이 `docs/report/` 로 백필된다
  (그러면 ⒜안이 «값싼 포인터 교체»가 된다) ⒞실제로 항목이 소실된 회차가 나온다(1/1 이라 시간 문제다).
- **★남는 구멍**: 백필 7건은 **하지 않았다** — 이 회차는 «판단»이고, 그것을 하면 ⒜를 절반 집행하는 셈이다. 신호 ⒝로 남겼다.
