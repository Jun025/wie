## [2026-09-08] 파리티 락 «천장 ③» — ★넓히지 않는다(⒝) · 이미 서 있던 결정에 «배제 전수»와 as-of 를 붙였다 (wie-parity-lock-ceiling-three-promote-to-decision)
- **무엇을**: `wie_cli/tests/support/dod_ci_parity.rs` 의 `ceilings()` 항목 ③ **문면만**. ★**술어·임계·워크플로·잡 무접촉 · 코드 동작 0.**
- **왜**: 운영자 채택 `2026-09-07-parity-per-repo-parser-axis-design#p2`(원 worklog repo = RustJava · 대상 = wie).
- **★★⑴ 전제 정정 — 「천장을 결정으로 승격하라」의 절반은 «이미 되어 있었다»**: 항목 ③ 은 2026-09-06 부터
  「★**확장하지 않기로 결정했다**(제안 `2026-09-05-dod-ci-parity-checker#p1`)」를 **수와 함께** 담고 있었고,
  그 결정 회차는 **PR #99** 로 착지했다(`docs/report/0087`). ⇒ ★**이 회차가 «없던 결정을 만든» 것이 아니다.**
- **★⑵ 그래서 «남아 있던 구멍»만 닫았다** — 티켓 F3⒝ 가 요구한 「빠지는 것 «목록»을 그 옆에」가 **미충족**이었다:
  옛 문면은 `engine-contract·web·coverage` **3개만** 이름 댔는데 `rust.yml` 밖 워크플로는 실측 **7개**다.
  ⇒ ★**전수와 «분류»를 넣었다**: 후보였고 넣지 않기로 한 것 **3**(engine-contract·web·coverage) ·
  애초에 후보가 아닌 것 **4**(publish-artifact = 릴리스 · rust-audit = schedule 전용 · dependabot = PR 자동화 · opencode = 봇)
  — ★그 넷은 «커밋 전에 사람이 돌리는 명령»이 없어 **상등 술어의 좌변이 비어 있다**.
- **★★⑶ 낡은 수를 «현재»로 읽히게 두지 않았다**: 「문서 8 · CI 23 · 교집합 2 · 차집합 27」에 ★**as-of 2026-09-06 트리**를 명시하고,
  ★**다시 재려면 기각된 갈래 A 시제품을 다시 만들어야 한다**(일부러 커밋하지 않았다)는 사실과
  ★**「결정은 수가 아니라 «술어가 없다»에 서 있다」**를 함께 적었다. ⇒ 수가 낡아도 결정은 흔들리지 않는다.
- **★⑷ 기존 축이 여전히 무는가 — 실증**: 스크래치 클론(`git clone -s` · 40자 핀 `a51a8cca`)에서 `rust.yml` 의
  `cargo fmt --all -- --check` 한 줄을 지우니 ★**rc=101 · `dod_region_and_rust_yml_agree`·`m1_ci_gains_a_gate_the_dod_does_not_list` FAILED**(9 passed / 2 failed) · 원복 `porcelain` **0줄**.
  ⇒ ★**문면을 고치면서 술어를 깨지 않았다.**
- **사용자 영향**: 없음(문서). 대신 ★**「이 검사가 무엇을 안 보는가」가 «이름 전수»로 읽힌다** — 다음 사람이 7개를 다시 세지 않는다.
- **★남는 구멍(그대로 둔다)**: 「문서가 낡는다」 자체는 이 결정으로 **해결되지 않는다**(선행 회차가 이미 자인).
  남은 길은 «집합/존재»가 아니라 **문서가 이름 대는 명령을 실제로 실행하는 것**이고 크기가 다르다.
