## [2026-09-27] wie_validate --keys — 누를 키 순서 지정으로 특정 화면까지의 경로 고정 (wie-2026-09-25-lgt-unwind-keys-script-flag-adopt-p1)

**무엇을** — `wie_validate --inject --keys <파일|목록>`. 문법 `NAME[:GAP[:HOLD]]`(공백 구분 · `#` 주석 · `WAIT` = 키 없이 샷만).
미지정이면 종전 27키 일정 그대로. `--keys` 일 때만 120초 상한을 푼다(스크립트 길이가 곧 요청한 예산 · `--keep-timeout` 은 종전대로).
모르는 키 이름·0초 간격은 파싱 단계에서 거부(오타가 조용히 다른 경로가 되지 않게). 예시 `docs/keys/battlemonster-village.keys`.

**왜** — 고정 27키(0.6초 격자)는 부하가 높으면 게임이 키를 늦게 받아 다른 화면(도움말)으로 빠진다. 세 회차가
같은 커밋 안 한 `WIE_KEYS` 패치를 각자 다시 얹어 경로를 고정했다(증적 3곳에 사본). ⓐ반증: main 에 키 목록 지정 수단 없음 —
`--inject-keys` 는 기본 스크립트의 앞 N개 선택뿐, 환경변수 읽기 0.

**측정**
- 기본 불변: 27키를 `--keys` 로 그대로 적은 일정 = 기본 일정(키·샷 시각 동일, 시험) · `plan_schedule_defaults_unchanged_test` 무수정 통과.
- 개악(플래그 무시 = 항상 기본 스크립트): 시험 2건 red · 원상 green.
- ★옛 스크래치 경로는 현 main 에서 마을에 닿지 않는다 — #306(숲 멈춤 수리) 이후 23단계 뒤 포획 튜토리얼 확인창이 끼고
  `*`·취소·`#` 를 이름으로 요구한다(옛 경로 끝 = 튜토리얼 `*` 대기 화면). 확인창은 `NUM2` 로 거절되지 않았다.
  현 main(`8ec688bf`)에서 튜토리얼 경로를 따라 다시 기록: STAR 43 · CLR 47 · HASH 49 · 마을 95 · 집 안 102.
- 도달(릴리스 · 게임 바이트 0):

| 실행 | load1(시작→끝 / 30초 표본 범위) | 결과 | 95 마을 | 102 집 안 |
|---|---|---|---|---|
| x4 단독 | 39.7 → 55.2 | PASS 97/97 | ○ | ○ |
| b·c 동시 2 | 14.4–44.4(평균 24.6) | PASS 97/97 ×2 | ○○ | ○○ |
| d1–d4 동시 4 | 11.6–129.1(평균 45.2 · 마지막 3표본 64.8·98.8·129.1) | PASS 97/97 ×4 | ○○○○ | ××××(마을 안 다른 위치) |

  ⇒ 마을 7/7. 집 안은 끝 무렵 부하가 65→129 로 오른 판에서 0/4 — 누른 채 걷기(HOLD)는 초가 아니라 tick 만큼 걷는다.
  예시 파일 머리에 «목표는 마을»로 적었다.
- 탐색 중 1회(x2 첫 판) step 5 `panic … InvalidMemoryAccess(541552863)` — 같은 접두는 다른 7판에서 통과 · 이 변경은 일정만 바꾼다 ⇒ 무관 · 미추적.

**한계** — 키 스크립트는 기록한 엔진에 묶인다: 게임을 더 진행시키는 수리가 착지하면 경로가 밀린다(이번에 실제로 밀렸다).

**사용자 영향** — 없음(검증 도구 · 기본 동작 불변).

**게임 파일명 유입** — `corpus-name-inflow --corpus`: BOUNDED 4쌍 · SUFFIX-ATTACHED 0쌍. 새로 든 것은 타이틀명(식별자) 2곳
(`docs/keys/battlemonster-village.keys` 머리 주석 · worklog 요약) · `wie_validate.rs` 의 2쌍은 main 에 이미 있던 주석 줄. 게임 바이트·경로 0.

### -fix (게이트② request-changes 이행 · wie-2026-09-25-lgt-unwind-keys-script-flag-adopt-p1-fix)

- **필수 1 — step 60 `HASH` 가 한 화면 일찍 눌린다.** 검수 독립 재현 0/2: `#` 가 «#키를 눌러봐» 앞 대사에서 버려지고,
  그 뒤 OK 는 전부 `#` 대기 화면에서 무동작 → 튜토리얼에서 끝.
  원인: **같은 빌드·같은 명령에서도 대사 하나가 먹는 OK 수가 판마다 다르다**(이 회차 판 = 두 번째 HASH 앞 10개 · 검수 판 = 11개). 기제는 확정하지 못했다.
  세이브 상태는 아니다 — `wie_validate` 의 파일시스템·DB 는 메모리라 매 판 무세이브 시작.
  이 회차에서 수정 전 스크립트 1판(load1 11.9 시작)은 마을 도달 — 검수 조건을 여기서 재현하지는 못했다.
- 처방: **각 `HASH` 앞에 여분 OK 1개**(STAR 43 · CLR 47 은 수정 전 판에서 이미 여유 1~2개 관측 · HASH 49/60 은 0개).
  `#` 대기 화면에서 OK 는 무동작이다 — 여분 OK 전후 프레임 동일(아래 4판 × 2곳 = 8/8 · 검수 판의 `61`=`95`=`102` 동일도 같은 사실).
  ⇒ 대사가 OK 하나를 더 먹는 판(검수)도, 덜 먹는 판(이 회차)도 `#` 가 제 화면에 들어간다. 새 번호: HASH 50·62 · 마을 97 · 집 안 104.
  ★검수 판의 «11개»에서의 효과는 이 무동작 사실로 **구성상** 성립한 것이지 그 조건을 재현해 관측한 것이 아니다.

| 판 | load1(시작 → 끝 · 30초 표본) | 결과 | 49=48 | 61=60 | 97 마을 |
|---|---|---|---|---|---|
| r1 단독 | 4.67 → 4.19 | rc=0 | ○ | ○ | ○ |
| r2 | 3.94 → 48.29 | PASS 99/99 | ○ | ○ | ○ |
| r3·r4 동시(+ CPU 부하 6) | 6.93 → 46.21 · 표본 7.4–75.8 | PASS 99/99 ×2 | ○○ | ○○ | ○○ |

  ⇒ 마을 **4/4** · load1 3.9–75.8. 체크포인트 샷(48·49·50·59·60·61·62·63·97·104)은 오케스트레이터 증적 디렉터리에 남겼다(게임 바이트·경로 0).
- **필수 2** — `wie_validate.rs` 모듈 문서의 예시 경로 `docs/examples/keys/…` → `docs/keys/…`.
- 비차단: `--shotdir` 가 디렉터리를 만들지 않는 것은 예시 머리에 `mkdir -p` 한 줄(동작 불변) · `HOLD < GAP`·`GAP >= 0.05` 는
  `--keys` 도움말에 «검사하지 않음» 한 줄(기본값이 파서 밖 `--action-secs` 라 파서 검사로는 못 잡는다) ·
  인라인/파일 판별 모호성은 손대지 않음(키 이름과 같은 이름의 파일이 cwd 에 있을 때만 · 실해 낮음).
- 시험: 예시 스크립트 키 수 97 → 99 · 마을 샷 라벨 `97_OK`.
