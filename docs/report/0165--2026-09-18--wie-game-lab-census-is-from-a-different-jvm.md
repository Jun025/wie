## [2026-09-18] `game_lab/broken` census 를 **오늘 트리로 다시 떴다** — 서명 분포가 갈렸고, 최대 군집이 **바뀌었다** (wie-game-lab-census-is-from-a-different-jvm)

**무엇을**: 채택 제안 `2026-09-17-game-lab-broken-187-signature-triage#p0`. `broken/**/*.zip` **187건 전수**를 오늘 트리로
재실행하고 **7월 표와 나란히** 실었다. ★**분류 술어는 «새로 만들었다»** — 7월 규칙은 슬래시형에 앵커돼 있어 오늘 출력(점형)에
안 맞는다. **형식 무관 술어를 만들어 두 입력에 동일하게** 적용했다(아래 ⓓ).
★수리 **0건**(별 축) · `classify.sh` **무접촉** · 코퍼스 **바이트** repo 유입 **0**(파일명은 ⓒ 절의 4개 — 대가 ⑹).

> ★★**[게이트② 반려 승계 2026-09-18 · `-fix`] 이 문서는 «수»가 아니라 «주장»을 고쳤다.** 아래 표의 값은 그대로다 —
> 초판이 그 값에 붙였던 세 주장이 틀렸다: ⑴「분류 규칙이 어디에도 없었다」(거짓 — 7월 원장 done 이 공표했다)
> ⑵「7월 공표표와 6/6 정확 일치」(공표표는 **15버킷**이다) ⑶「같은 규칙」(형식이 갈려 성립하지 않는다).
> ★그리고 ⑷ **「`no frame rendered` 32 는 부하 인공물이 아니다」의 판별식이 무효**였다 — 아래 §오늘의 최대 군집 (정정 4).

## ⓒ 모집단 — 「1,312」은 **오늘 트리에서 재현되지 않는다**
| 센 것 | 값 |
|---|---|
| `broken/**/*.zip` | **187**(★distinct stem **184** — 중복 stem 3) |
| `working/**/*.zip` | 294 |
| `reports/` | `.json` **452** · `.log` 470 · `.md` 66 · `.tsv` 3 · `.png` 6,389 |
| `reports/` mtime | **2026-06 181 · 2026-07 271** — ★**2026-08 이후 0** |

⇒ ★**`1,312` 은 어떤 조합으로도 나오지 않는다**(json+log **922** · json+log+md+tsv **991** · zip 전체 **483**).
★**이 티켓의 모집단은 `broken` 187** 이고, 그중 **186** 이 7월 리포트를 갖는다(**1건 결손**) — 공표표의 `합 186` 이 바로 이 수다.
★「낡았다」는 **참이다**: 리포트 mtime 이 **전건 6~7월**이다.

★**그 「중복 3 · 결손 1」의 정체**(초판은 수만 적었다 — 수만 적으면 다음 사람이 다시 센다):
중복 stem 3 = **`놈3`**(lgt·ktf) · **`다크슬레이어2`**(skt·ktf) · **`이노티아연대기2`**(unknown·ktf) — 캐리어 디렉터리가 달라도
리포트는 stem 으로 한 벌이라 **같은 7월 리포트를 공유한다**(파일 단위로 세는 공표표의 셈법을 그대로 따랐다).
결손 1 = **`broken/lgt/크로이센.zip`** — ★**7월 원장 done 이 이미 이름까지 적어 두었다**(그 표 마지막 행 = 「리포트 결손 1」).
> ★**대가**: 초판이 내세운 「코퍼스 **파일명** repo 유입 0」은 이 네 이름만큼 **깨진다**(게임 **바이트**는 여전히 0이고
> `scripts/audit-no-leak.sh` 축과도 무관하다). ★그 이름이 없으면 「중복 3」을 **매번 다시 세야** 하므로 바꿨다.

## ⓓ ★분류 술어 — **7월 규칙은 기록돼 있었고, 그런데도 그것을 쓸 수 없었다**

★★**(정정 1) 「규칙이 어디에도 기록돼 있지 않았다」는 거짓이다 — 철회한다.**
7월 회차가 **자기 done 리포트에 순서 있는 `RULES` 표를 공표해 두었다**: 원장
`reports/wie-game-lab-broken-187-failure-signature-triage.done.md` **§⑴ 표 아래 「서명 추출 명령」 블록**
(★외부 계약은 **인용하지 않고 가리킨다** — `AGENTS.md` §Constraints).
★**왜 못 찾았나**(다음 사람이 같은 자리를 밟지 않도록 한 줄): 초판의 탐색 범위 「선행 리포트」를 ★**repo 의
`docs/report/`** 로 읽었다. 그 규칙이 있는 곳은 repo 밖 **원장 `~/orchestrator/reports/`** 이고,
CI 도 검사기도 그 경로를 못 보므로 **repo 안만 훑으면 구조적으로 안 나온다**.
⇒ ★**이 회차의 역산은 «최초 기록»이 아니라 «독립 교차검증»이다**(그 값은 남는다 — 아래에서 두 규칙이
같은 입력에 **0건 불일치**임을 실제로 쟀다). 비용은 회피 가능했다.

★★**(정정 2) 「7월 공표표와 6/6 정확 일치」 — 공표표는 «15버킷»이다.**
7월 표: `NoSuchMethod 37 · panic-unwrap 37 · java-exception 21 · invalid-mem 15 · unimpl-stub 15 · **UNCLASSIFIED 11** ·
blank/uniform 11 · unimpl-other 7 · PASS_stale 7 · unknown-stdlib-import 6 · HANG 6 · zip-unrecognized 4 ·
panic-unreachable 4 · NoClassDefFound 3 · panic-other 2 · 리포트 결손 1`.
⇒ ★정직한 문면은 **「15버킷 중 5버킷을 정확 재현했고 10버킷을 `UNCLASSIFIED` 로 접었다」**이고,
★**그 10버킷의 합이 87**(`21+15+11+7+7+6+4+3+2+11`)이다 — 즉 ★**「UNCLASSIFIED 87」은 7월 공표값이 아니다**
(**7월의 UNCLASSIFIED 는 11**). ★**「합 186」을 증거로 쓰지 마라 — 186 의 어떤 분할도 186 이다**(검증력 0).

★★**(정정 3) 「같은 규칙」이 아니다 — 형식 무관 술어를 «새로» 만들어 두 입력에 동일 적용했다.**
7월 규칙은 **슬래시형에 앵커**돼 있다(`java/lang/NoSuchMethodError:` · `net/wie/WieError: Unimplemented:`).
오늘 트리의 출력은 **점형**이다(`java.lang.…` · `net.wie.…`) — ★7월 회차 자신이 그 done 에 굵게 적어 두었다:
「**7월 로그로 만든 어떤 정규식도 오늘 새 데이터에 안 맞는다**」. 이 회차가 실제로 쓴 술어(형식 무관):

| 서명 | 이 회차의 술어 | 7월 술어와의 차 |
|---|---|---|
| `NoSuchMethod` | `NoSuchMethodError` **또는** `Method .*? not found` | 패키지 경로 앵커 **제거** |
| `panic-unwrap` | `unwrap\(\)` | `called \`…::unwrap()\`` 완화 |
| `unimpl-stub` | `WieError: Unimplemented` | `net/wie/` 접두 **제거** |
| `unknown-stdlib-import` | ★`Unknown (\w+) stdlib import` | **7월 형태로 되돌림**(초판은 `lgt` 하드코딩) |
| `panic-unreachable` | `unreachable code` | `entered ` 접두 완화 |
| `UNCLASSIFIED` | 그 밖 | — |

★★**양방향 실측 — 이것이 40% 의 «기전»이다**(입력 = 7월 `reports/*.json` 의 `reason` 186건 · 순서=첫 매치):

| 규칙 \ 입력 | 슬래시형(7월 원본) | 점형(형식만 치환한 **같은 186 문자열**) |
|---|---|---|
| **7월 규칙**(슬래시 앵커) | 37 · 37 · 15 · 6 · 4 · UNCL **87** ✓ 공표 5버킷 재현 | ★`NoSuchMethod` **16** · `unimpl-stub` **0** · UNCL **123** |
| **이 회차 술어**(형식 무관) | 37 · 37 · 15 · 6 · 4 · UNCL **87** | ★37 · 37 · 15 · 6 · 4 · UNCL **87** — **불변** |

⇒ ★**형식만 바꿔도 7월 규칙은 186건 중 36건(19.4%)의 버킷을 잃는다. 같은 전환에서 이 회차 술어는 0건 바뀐다.**
그리고 ★**두 규칙은 원본 입력에서 불일치 0건**이다 ⇒ 재현 주장은 (5버킷 범위에서) 그대로 서고,
★**「같은 규칙으로 쟀다」는 성립할 수 없다** — 7월 규칙을 **문자 그대로** 오늘 입력에 썼다면
`unimpl-stub` 은 **0** 이 되고 `NoSuchMethod` 도 무너져, ★**오늘 열은 «규칙이 안 맞는 표»가 됐을 것이다.**
※치환은 `java/lang/`·`net/wie/` 만 바꾼 **보수적** 형태와 슬래시 경로 **전면** 치환 **둘 다** 같은 36건을 냈다.
`NoSuchMethod` 가 0 이 아니라 16 으로 남는 이유는 실측으로 확인했다 — 7월 2번 규칙의 꼬리
`not found from ([\w/$]+)` 가 `org.kwis.msp.io.File` 에서 ★**`org` 한 토큰만으로도 맞는다**(그 규칙이 원래 느슨했다).

★**`unknown-stdlib-import` 의 `lgt` 하드코딩을 `(\w+)` 로 넓혔다**(검수 지적). ★**오늘 수는 안 바뀐다** —
그 문자열의 **유일한 발신처**가 `wie-lgt/src/runtime/stdlib.rs:34` 의 `"Unknown lgt stdlib import: {:#x}"` 이고,
트리 전체에서 `stdlib import` 문자열은 **그 1건뿐**이다(실측) ⇒ ktf/skt 변종은 **오늘 존재할 수 없다**.
★그래도 넓힌 이유 = 다른 캐리어가 같은 줄을 추가하는 날 **조용히 UNCLASSIFIED 로 새지 않게** 하려는 것이다.
※초판의 첫 시도(`not implemented`·`NoClassDefFoundError` 류 상식 술어)가 **21/37/7/3/4/114** 로 어긋난 기록은 그대로 유효하다 —
급소는 `Method …@N not found from …`(16건)·`Unknown lgt stdlib import`(6건)가 `WieError: Fatal error` 안에 숨어 있던 것이다.

## ★★두 표 — **한 술어(형식 무관) · 두 입력**(FAIL 만: census 의 목적이 «실패 서명»이다)
| 서명 | 7월 | **오늘** | 차 |
|---|---:|---:|---:|
| `NoSuchMethod` | 37 | **8** | **−29** |
| `panic-unwrap` | 37 | **15** | **−22** |
| `unimpl-stub` | 15 | **6** | **−9** |
| `unknown-stdlib-import` | 6 | **2** | **−4** |
| `panic-unreachable` | 4 | **0** | **−4** |
| `UNCLASSIFIED` ※ | 80 | **99** | +19 |
| **FAIL 합** | **179** | **130** | −49 |
| **PASS** | **7** | ★**57** | **+50** |

※★★**이 행의 「7월 80」을 «7월 공표 버킷»으로 읽지 마라 — 7월이 공표한 `UNCLASSIFIED` 는 11 이다.**
여기 80 은 **이 표의 5버킷 밖에 떨어진 FAIL 잔여**이고, 7월 표에서 이미 **이름이 붙어 있던**
`java-exception 21 · invalid-mem 15 · blank/uniform 11 · UNCLASSIFIED 11 · unimpl-other 7 · HANG 6 ·
zip-unrecognized 4 · NoClassDefFound 3 · panic-other 2`(+ FAIL 밖 `PASS_stale` 7)이 **여기로 접혔다**.
⇒ ★**이 표는 «5버킷 + 나머지»이지 «7월 15버킷 대 오늘»이 아니다.** 10버킷을 접은 것은 이 회차의 선택이고,
★그 선택은 **오늘 열을 만들 수 있게 한 대신 해상도를 잃었다**.

★**짝지은 186건 중 75건(40%)이 서명 버킷을 바꿨다** — `NoSuchMethod→UNCLASSIFIED` 29 · `panic-unwrap→` 27 ·
`unimpl-stub→` 9 · `unknown-stdlib→` 4 · `panic-unreachable→panic-unwrap` 3 · `UNCLASSIFIED→panic-unwrap` 2 · 불변 111.
★★**이 40% 는 «형식 인공물»이 아니다** — ⓓ 의 양방향 표가 그것을 가른다: 같은 술어가 형식 전환에서 **0건** 움직인다.
⇒ 움직인 75건은 **런타임 거동이 바뀐 몫**이다. ★**그래서 (정정 3)은 결론을 약화시키지 않고 «떠받친다».**
⇒ ★**제안의 전제(「낡았다」)는 참이고, 표를 다시 뜨는 것이 값을 했다.**

## ★★오늘의 최대 군집 — `no frame rendered` **32**(7월 **1**) · ★**부하 인공물과 «가르지 못했다»**
오늘 FAIL 130 의 `UNCLASSIFIED` 99 내부(★★**이 이름들은 «새 구조»가 아니다** — 7월 표가 이미
`invalid-mem 15 · java-exception 21 · NoClassDefFound 3 · blank/uniform 11 · HANG 6` 으로 **이름 붙여 공표한** 군집들이고,
이 회차가 그 10버킷을 접었기 때문에 여기 «안»에서 다시 보이는 것이다):
```
32  no frame rendered (hang/black screen)      ← ★7월에는 1건
12  WieError: Invalid memory access
 9  Fatal error: U…            8  WieError: Fatal error
 7  java.lang.NullPointerException              6  java.lang.NumberFormatException
 6  java.lang.NoClassDefFoundError              5  only blank/uniform
 3  java.lang.Error   3  load error   2  hang: SIGKILL   1  attempt to add with overflow
```
★★**(정정 4) 초판의 「부하 인공물이 아니다 — 갈랐다」를 철회한다. 가르지 못했다.**
⑴★**판별식의 한 축이 무효다** — 초판은 「기아면 `ms`·`ticks` 가 **작아야** 한다」로 갈랐는데,
`ms` 는 ★**벽시계 예산**이라 기아일 때도 **그대로 다 탄다**(`AGENTS.md` §Definition of Done 이 이미
「`paints` 는 «고정» 벽시계 예산에 들어간 틱 수」라고 적었다). ⇒ ★**`ms` 축은 판별력이 0 이고, 남는 축은 `ticks` 하나**인데
초판은 32건 중 **3건**만 재실행했다(그마저 하나는 `ticks=20`).
⑵★★**이 버킷은 catch-all 이다** — `wie_cli/src/bin/wie_validate.rs:795` 의 **최종 `else`** 가
「예산이 끝났는데 `paints == 0`」인 **모든** 경우를 여기로 받는다(이유를 가리지 않는다). CPU 기아가 정확히 여기로 떨어진다.
⑶★**게이트② 검수가 그 인공물을 «재현»했다**(독립 표본 · 같은 게임·같은 트리·같은 인자, 부하만 다름):
고부하 2/2런 `no frame rendered`(ticks **3·2**) ↔ 저부하 3/3런 ★**PASS**(ticks **4,171만~4,390만**).
그쪽 관측 3건이 **전건 저부하에서 뒤집혔다**. ⇒ ★**초판의 「수천만 틱을 돌리고도 안 그린다」는 «저부하의 정상 틱 수»와 구분되지 않는다.**
⑷★**그리고 이 census 자체가 `CPU idle 0.0%` 에서 돌았다**(초판이 스스로 측정·기재) — ★그 조건이 바로 이 인공물을 만드는 조건이다.

⇒ ★★**판정: 「32」는 «관측된 수»로 남기고, 「돌지만 안 그린다 군집」이라는 «해석»은 내린다.**
★**저부하 재측(검수 처방 ⒜)은 이 회차에서 구조적으로 불가능했다** — ★그 32건이 **어느 게임인지 모른다**(아래 §대가 ⑸:
per-game 원자료를 남기지 않았다). ⇒ 재측하려면 **187건 전수를 다시 돌려야** 하고, 그러면 이 표의 «오늘 열» 자체가
다른 값이 된다. ★**이 회차의 계약은 «주장을 측정에 맞추는 것»이지 «표를 흔드는 것»이 아니므로 ⒝(명시)를 택했다.**
※★**계열로 보면 신규성은 더 작다**: 「안 그렸다」 계열은 7월에도 `blank/uniform` **11** · `HANG` **6** 이 따로 있었다
(계열 합 7월 **18** → 오늘 **39** = 32 + `only blank` 5 + `hang: SIGKILL` 2). ★**1 → 32 만 적으면 신규성이 과장된다.**
※동질적이지도 않다 — 재실행 3건 중 하나는 `ticks=20` 이라 **단일 틱 행**에 가깝다.

## ★혼입 변인을 숨기지 않는다 — 「다른 JVM」만이 원인이 아니다
`PASS 7 → 57` 의 상당 부분은 ★**같은 날 착지한 폰트 수정**(`wie-game-lab-broken-187-failure-signature-triage` · `HeadlessPlatform::font()`)이다.
그 회차가 **A/B 로 `panic … not implemented` 8/9 → 0/9** 를 이미 쟀다. ⇒ ★**오늘 표의 «7월 대비 차»는 «JVM base swap + 폰트 수정»의 합**이고,
★**둘을 가르지 않았다**(가르려면 두 트리에서 각각 전수를 떠야 한다 = 비용 2배). **제안의 문면(「다른 JVM 의 기록」)은 절반만 맞다.**

## ⓑ 비용 — 실측(ⓑ 가 요구한 칸)
- 표본 8건 직렬 **111초** ⇒ 187건 추정 **43분** · **실제 전수 45분**(04:55 → 05:39 · `nice -n 15`)
- ★**이 맥은 self-hosted CI 러너를 겸한다** — 측정 중 load **98~135**, ★**CPU idle 0.0%**, `vm_pressure` 1(normal).
  ★**그 45분 동안 열린 PR 5건이 그 러너를 기다렸다** — 이것이 이 회차가 치른 값이다.
- ★**병렬화하지 않았다**: 이 저장소가 이미 기록한 「부하가 verdict 를 뒤집는다」(`AGENTS.md` §Definition of Done) 때문이다.
  ⇒ 대신 **`nice -n 15`** 로 양보했고, 판정 축을 **PASS/FAIL 이 아니라 «서명»**으로 유지했다.

## ⓐ 왜 전수를 돌렸나 — 표본이 «갈린다»고 답했다
시드 고정 표본 8건(그중 7건 비교 가능): ★**4건이 갈렸다**. ★그리고 **부하 flake 가 아니다** — 그 4건을 **3런씩** 재실행해 **12/12 PASS 안정**이었고,
그중 둘은 7월에 **`NoSuchMethod` 서명**이었다(서명이 사라지는 것은 부하로 설명되지 않는다).
★**단 이 안정성을 `no frame rendered` 버킷으로 옮겨 읽지 마라** — 여기서 안정적이었던 것은 **PASS** 이고,
★**부하가 뒤집는 것은 정확히 «PASS → no frame rendered» 방향**이다(위 정정 4). 게이트② 검수의 독립 표본도
**8건 중 7건이 2런 간 버킷 동일**로 «갈렸다»는 방향 자체는 지지했고, 흔들린 것은 그 한 버킷뿐이었다.

## ★7월 표를 인용한 문서 — **고치지 않고 «기준일»을 명시**한다(판정)
전수 조회: 7월 수치를 인용하는 파일은 ★**`STATE.md` 하나**다(`docs/report/0151`·worklog 는 그 표를 **수로 싣지 않는다**).
⇒ ★**`docs/report/0151` 을 고치지 않는다** — 그것은 **날짜 박힌 회차 기록**이고, 원장 기록을 사후에 고쳐 쓰는 것은 이 저장소가 금지하는 형태다.
대신 **`STATE.md` 의 그 항목에 「7월 기준」과 이 회차 포인터를 붙였다**(항목 «안» 편집 — 새 삽입점 **0**).

## 대가 — 「잃는 것이 없다」가 아니다
⑴**러너 45분**(위) ⑵★**두 변인이 섞였다**(JVM + 폰트) — 가르려면 비용 2배 ⑶★**이 표도 «오늘» 에 묶인다**:
`reports/` 가 6~7월에 멈춰 두 달 만에 낡았듯, **base 가 다시 움직이면 이 표도 낡는다**
⇒ ★그래서 **쓴 술어를 이 문서에 박아** 다음 census 가 **재역산 없이** 같은 축으로 뜰 수 있게 했다.
⑷`STATE.md` 항목 1건 추가 ⇒ 착지 시 `behind ≥ 1` 열린 PR 을 무효화한다(소관 = `wie-remove-state-md-completed-insertion-point`).
⑸★★**per-game 원자료(186행 stem→버킷)를 남기지 않았다**(`mktemp` 스크래치와 함께 지웠다) — ★**이 회차 최대의 대가다.**
그것 하나 때문에 ⒜「`no frame rendered` 32 가 어느 게임인가」를 못 말하고 ⒝그래서 **저부하 재측이 불가능**하며
⒞**오늘 열의 재검이 45분 전수 재실행 없이는 안 된다**. ★**남기는 비용은 거의 0 이었다** — 후속 제안 `#p1` 이 그 자리를 진다.
⑹★**「코퍼스 파일명 repo 유입 0」을 4개 이름만큼 포기했다**(ⓒ 절) — 게임 **바이트**는 그대로 0 이다.
⑺★**10버킷을 접었다**(ⓓ 정정 2) ⇒ 두 열을 비교 가능하게 만든 대신 **7월 표의 해상도를 잃었다**.

## 범위
187건 중 **수리 0** · `classify.sh` **0줄** · 새 검사기·새 잡 **0** · 제품 코드 **0줄** ·
★코퍼스는 **읽기 전용**으로만 썼다(다른 레인이 그 체크아웃에서 작업 중 — 쓰기 **0**) ·
★**게임 바이트 repo 유입 0**(집계만 · 파일명은 위 ⑹의 4개).
★**분류 술어는 «이 문서 안»에만 있다** — `classify.sh` 도 `game_lab/` 도 고치지 않았다(그 축은 후속 `#p1`).
