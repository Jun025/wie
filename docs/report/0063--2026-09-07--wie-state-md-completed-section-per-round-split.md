## [2026-09-07] `STATE.md` 「완료 (최근)」 분할 — ★**하지 않는다.** 충돌면은 그 절이 아니라 「진행중」이다 (wie-state-md-completed-section-per-round-split)

**무엇을**: 판단 회차다. ★**구현 0**(코드·`STATE.md` 구조 변경 **0**). 채택 제안
`2026-09-07-report-per-round-files#p0`(「완료 (최근)」 466줄도 가른다)을 조사해 ★**그 기전이 목표를 달성하지 못한다**로 닫았다.

**왜 — 대조군으로 쟀다(수사가 아니라 실행)**: 가짜 회차 둘이 «동시에» 진행중 항목을 추가하는 형상에서

| 형상 | `git merge-tree` |
|---|---|
| **현행**(STATE.md 681줄) | **rc=1 · `CONFLICT (content): Merge conflict in STATE.md`** |
| ★**「완료」 543줄을 들어낸 뒤**(681 − 543 = **138** · 자리표시 스텁 4줄 ⇒ 파일 **142줄** · 79% 축소) | ★**rc=1 · 같은 충돌 — 변화 0** |

⇒ ★★**분할해도 충돌이 그대로다.** 근인: ★**추가 지점이 「완료」가 아니라 「진행중」의 «맨 위»** 이고,
「진행중」은 **살아 있는 상태**라 이 티켓 자신이 「통째로 가르지 마라」로 못박은 자리다.

**뒷받침 실측(모집단 = 열린 PR 5건)**: `STATE.md` 를 만지는 것 **4건** · 그중 ★**「진행중」을 만지는 것 4/4** ·
★**「완료」«만» 만지는 것 0건**. 절별 추가줄 합계 = 「진행중」 **23줄** ↔ 「완료」 **7줄**(#98 한 건뿐).
`-U0` old-side 로 다시 재도 **hunk 전건이 「진행중」 직전**에 붙는다(#110 전문 확인: 삽입 8줄 전부 `## 진행중` 바로 아래).

**★형제 저장소 판단이 «그대로 전이되지 않는다»**: `otterpebble-state-md-per-round-files-decision(-fix)` 는 **approve**로
「한다」를 냈는데, 그 판단의 **핵심 근거**가 ★**「살아 있는 상태를 든 절 = 0 (197/197 «없음»)」** 이다.
★**wie 는 다르다** — `## fork 의 현재 위치`(16줄) · `## 진행중`(8줄) · `## 다음`(97줄)이 **실제로 살아 있고**,
서두 17줄과 합쳐 ★**138줄**(= 681 − 완료 543)이 「지금」을 말한다. ★자는 **전체줄**이다. ⇒ ★**전제가 성립하지 않으므로 결론도 옮겨오지 않는다.**
(그리고 형제도 **구현은 아직 안 했다** — `STATE.md` 6,808줄 · `docs/state*` **0건** 실측. 그쪽 인계 §6-1 이 요구한
「연번 유일성을 «함께» 풀어라」는 wie 에서 **PR #113 이 진행 중**이다.)

**사용자 영향**: 없다(구현 0).

**★소비자 전수(했더라면 옮겨야 했을 것 · 모집단 추적 파일 553)**: `.github/`·`scripts/` 히트 ★**0건** —
기계 소비자는 없다. 「완료 (최근)」 문자열은 `STATE.md` 자신과 회차 기록 2건뿐이다.
⇒ ★**비용이 낮다는 것은 맞다. 그러나 «효과가 0»이라 낮은 비용도 정당화되지 않는다.**

**★남긴 것**: 진짜 추가 지점은 **「진행중」의 맨 위**다. 그것은 회차별 파일로 못 가르지만
(살아 있는 상태 · 착지 시 «지워지는» 항목), **다른 처방**이 필요하다 — 후속 제안으로 등재했다.
