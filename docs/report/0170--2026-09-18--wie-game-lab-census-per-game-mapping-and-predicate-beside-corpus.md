## [2026-09-18] per-game 매핑을 «생성기»로 되살렸다 — 표를 커밋하지 않고 술어를 repo 에 두는 제3안 (wie-game-lab-census-per-game-mapping-and-predicate-beside-corpus)

**무엇을**: `scripts/game-lab-census-map.mjs` **1파일**(repo) — 분류 술어를 싣고 코퍼스 옆에
`game_lab/census-map.tsv`(187행 · **git 에 보이지 않음**)를 낸다.
★**제품 코드 0줄 · 새 검사기 0 · `.gitignore` 무접촉 · 인구조사 표 무접촉 · `STATE.md` 무접촉 ·
repo 에 들어온 게임 이름 «0».**

**왜**: 인구조사가 per-game 행을 `mktemp -d` 에 쓰고 **지웠다** ⇒ 합계만 남아 「이 숫자가 어느 게임인지」를
아무도 모르게 됐고, 형제 제안 `#p0`(저부하 재측)이 **187건 전수 재실행**을 해야 하는 상태가 됐다.

**사용자 영향**: 없다(제품 0줄). 다음 회차가 **버킷별 파일 목록을 한 명령으로** 얻는다.

### ⓐ 거처 — ★**세 안을 «수로» 재고 골랐다**

| 안 | 대가(내 실측) |
|---|---|
| ⑴`game_lab/` 안에만 | ★repo 가 **못 본다** ⇒ 낡아도 아무도 모른다 |
| ⑵repo 안에 표를 커밋 | ★**187 파일 / 184 고유 stem**이 git 이력에 **영구·불가역** 유입. ★**186/187 이 비-ASCII** 이고 상표성 어휘(`컴투스`·`게임빌`·`던파`·`제노니아`·`SKT`·`KTF`·`LGT`)가 실재한다 |
| ★**⑶ 제3안 — «생성기»를 repo 에**(채택) | ★한 겹이 생긴다: **재생성해야** 최신이다. ★**그러나 티켓이 값 매긴 «해시↔이름 대응표»는 «없다»** — 조인할 것이 없고 다시 돌리면 이름이 나온다 |

⇒ ★**채택 = ⑶.** repo 에는 **술어와 생성기**만(리뷰·CI 가시) · 이름은 **코퍼스를 가진 기계에만**.
★**안 사는 것도 적는다**: 출력이 여전히 CI 에 안 보이므로 ★**낡음을 «검사가 잡지 못한다».**
완화는 「재생성이 한 명령」이지 「낡을 수 없다」가 아니다. ⇒ ★**이것을 «안전»으로 읽지 마라.**

### ⓑ 술어 거처 — ★**복제가 아니라 «정본 이전»이다**

종전엔 규칙이 ★**원장에만** 있었다 ⇒ repo 를 훑은 회차가 **0건**을 받고 「규칙이 어디에도 없다」로 결론냈고
그 주장을 후속이 철회했다. ⇒ `RULES` 를 **생성기 안에 정본으로** 두고 원장 경로는 ★**«출처»로만** 인용한다
(주석에 명시: 「둘이 갈리면 **이 파일이** 게임을 분류한 것이다」). ⇒ ★**두 벌이 되지 않는다.**

### ⓒ 술어 재적용 규율 — ★**«강제»가 아니라 규율이고, 그렇게 적었다**

`--reports <dir>` 플래그가 그 규율의 형태다: 술어를 고치면 **두 입력에 각각** 돌려 두 열을 다시 낸다.
★**기계가 강제하지 않는다** — 사람이 진다. ★**그 한계를 주석에 그대로 적었다.**

★★**그 규율이 «왜» 필요한지를 실증했다**(개악 대조 · `trap` 원복 · 커밋 0):

| 술어 | `NoSuchMethod` | `unimpl-stub` | `UNCLASSIFIED` |
|---|---:|---:|---:|
| 정본 | **37** | **15** | **80** |
| 인구조사가 「상식적」이라 부른 판 | **24** | **7** | **101** |

⇒ ★**한 술어를 바꾸면 표가 조용히 갈린다.**
※**첫 시도는 실패했다** — `unwrap\(\)` → `unwrap\(` 로 한 글자를 지웠는데 **표가 안 갈렸다**
(이 코퍼스의 `unwrap(` 은 전부 `)` 가 따른다). ★**약한 개악은 «증명»이 아니다** — 그래서 인구조사가
실측한 판으로 바꿔 다시 했다. 그 실패도 적는다.

### ⓓ·⑵ 계수 — ★**187 이고 186 이 아니다. 그 차이가 «파일 ↔ stem» 이다**

★**코퍼스 실측: 파일 187 · 고유 stem 184**(중복 3: `놈3`·`다크슬레이어2`·`이노티아연대기2` — 각각 두 캐리어).
★★**합계 보존 — 파일 기준이 인구조사 7월 열과 «7/7 정확 일치»한다**:

| 버킷 | 인구조사 7월 | ★내 매핑(파일 기준) | stem 기준(참고) |
|---|---:|---:|---:|
| `NoSuchMethod` | 37 | ★**37** | 37 |
| `panic-unwrap` | 37 | ★**37** | 35 |
| `unimpl-stub` | 15 | ★**15** | 15 |
| `unknown-stdlib-import` | 6 | ★**6** | 6 |
| `panic-unreachable` | 4 | ★**4** | 4 |
| `UNCLASSIFIED` | 80 | ★**80** | 79 |
| `PASS` | 7 | ★**7** | 7 |
| 합 | **186** | ★**186**(+NO-REPORT 1) | 183 |

⇒ ★**중복 3건이 차이를 «정확히» 설명한다**(`놈3`·`다크슬레이어2` = panic-unwrap **+2** · `이노티아연대기2` = UNCLASSIFIED **+1**).
★★**그래서 매핑을 «파일» 단위로 만들었다** — 소비자가 돌릴 대상은 «경로»이고, stem 으로 키를 잡으면
★**중복 3건의 사본 하나씩을 조용히 빠뜨린다.**

### ⑷ ★**실효 판정 — 추출은 «돈다». 그런데 32 가 «1» 이다**

```
$ node scripts/game-lab-census-map.mjs --bucket unimpl-stub | tail -3
game_lab/broken/ktf/엘즈웨어.zip
game_lab/broken/ktf/컴투스포춘골프3D.zip
game_lab/broken/ktf/티어즈오브메탈.zip
# 15 file(s) in bucket "unimpl-stub" (input: game_lab/reports)

$ grep -c 'no frame rendered' game_lab/census-map.tsv
1            ← ★32 가 아니다
```
★★★**이유를 찾았고, 그것이 이 회차의 핵심 발견이다**: ★**`game_lab/reports/*.json` 은 «7월» 데이터다** —
mtime 범위가 **2026-06-25 ~ 07-01** 이고, 내 계수가 인구조사의 «7월 열»과 7/7 일치한다.
★**인구조사의 «오늘» 데이터는 스크래치와 함께 지워졌다** ⇒ ★**오늘의 32건은 «어떤 매핑으로도 뽑을 수 없다».**
⇒ ★★**티켓 ⑷ 는 «이 회차 안에서 충족될 수 없다»** — 오늘 데이터를 만들려면 전수 재실행이 필요하고
그것은 계약 3 이 **금지한 바로 그 일**이다(`#p0` 몫).
★**그래서 «빈 칸»으로 두지 않고 이렇게 남긴다**: 매핑 기구는 **서 있고**(추출이 실제로 돈다),
★**`#p0` 이 재실행하는 순간 그 reports 를 `--reports` 로 가리키면 32건이 «그 자리에서» 나온다.**
⇒ ★**이 회차가 없앤 것은 「전수 재실행을 또 해야 한다」가 아니라 「재실행해도 또 잃는다」이다.**

### ⒜ 매핑이 맞는가 — 표본 5/5

무작위 5건(seed 고정)을 `reports/*.json` 원문과 대조: `정통맞고2007`(panic-unwrap) ·
`드래곤나이트_ex`(UNCLASSIFIED) · `해적왕2007`(NoSuchMethod) · `메르헨전기`(UNCLASSIFIED) ·
`(KTF)투스워즈`(UNCLASSIFIED) ⇒ ★**5/5 일치**.

### 한계 — 숨기지 않는다

- ★**⑷ 를 충족하지 못했다.** 오늘의 32건은 **데이터가 없어서** 못 뽑는다(위). 기구만 세웠다.
- ★**출력이 CI 에 안 보인다** ⇒ 낡음을 **검사가 못 잡는다**. 「재생성이 한 명령」이 완화의 전부다.
- ★**⒞ 규율은 사람이 진다** — `--reports` 는 «편하게 해 줄 뿐» 강제하지 않는다.
- ★**매핑의 «날짜»가 헤더에 박힌다** — 그것이 이 회차가 넣은 유일한 낡음 방어다(`mtime range` 줄).
  ★그래도 **읽는 사람이 그 줄을 봐야** 한다.
