## [2026-09-07] 원장 파일 목록에 `docs/report/**` 를 적었다 — 승인 열거가 이관보다 «오래됐다» (wie-merge-contract-path-list-stale-after-report-split)

**무엇을**: `AGENTS.md` §Landing paperwork 에 「이 저장소의 원장 파일 = `STATE.md` · `REPORT.md` ·
`docs/report/**` · `docs/worklog/**`, 충돌은 합집합으로」 한 블록을 적었다. ★**`REPORT` 를 지우지 않았다** —
그 파일은 고정 안내로 여전히 실재하고, 그것을 고치는 회차도 원장 파일을 고치는 것이다.

**왜**: 머지 계약 2-c⒜ 가 «이름으로» 승인하는 목록은 `STATE`·`REPORT`·`docs/worklog/**`·`reports/`·`tasks/` 이고,
★**회차 기록이 `docs/report/` 로 옮겨진 2026-09-07(머지 `a5091df6`) 보다 «먼저» 쓰였다.**
⇒ 2026-09-07 의 한 머지 회차가 그 자리에서 「권한이 있는가」를 유도해야 했다. ★**그 회차는 옳게 처리했다**
(옮긴다 = 손실 0 · 저작한 새 텍스트 0바이트). ★**그러나 유도가 매번 같게 나온다는 보장이 없다** —
어느 회차가 「버린다」를 고르면 **회차 기록이 조용히 사라진다.** 이 블록은 판단을 없애는 것이 아니라
**이미 난 판단을 적어 두는 것**이다.

**사용자 영향**: 없다(문서만 · 엔진·웹 산출물 무접촉).

**★관측(고치지 않았다 · 별 소관)**: 그 열거의 정본은 `~/orchestrator/templates/merge-ticket.tpl` 이고
**이 저장소 밖**이다. ⇒ ★**여기서 고칠 수 없다.** 그리고 그 파일의 `docs/report` 히트는 ★**0** 이다 —
형제 저장소 `otterpebble` 이 **2026-08-23** 에 같은 이관을 했으므로 ★**그쪽은 같은 잠복을 2주 넘게 안고 있다**
(그 사이 머지 회신 여러 건이 이미 `docs/report` 를 만졌다). ★**한 템플릿이 전 저장소의 머지 티켓을 렌더**하므로
그 한 줄이 두 저장소를 함께 덮는다 — 총괄 소관으로 올린다.
