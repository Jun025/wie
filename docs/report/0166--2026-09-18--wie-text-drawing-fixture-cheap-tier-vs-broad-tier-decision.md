## [2026-09-18] 글자 픽스처의 CI 층 결정 — ★**싼 층 ⒜ 를 골랐다**(넓은 층 ⒝ 는 17분/PR 이었다) (wie-text-drawing-fixture-cheap-tier-vs-broad-tier-decision)

**무엇을**: `wie_validate.rs` 에 `#[cfg(test)]` 시험 **1개**(`headless_platform_font_measures_text_test`) +
`AGENTS.md` 에 결정·비용·재개 조건 기록. ★**새 워크플로 0 · 새 픽스처 0 · 새 의존 0 · `font()` 무접촉 ·
착지한 픽스처 무접촉 · `game_lab/` 무접촉 · `STATE.md` 무접촉 · 187 수리 캠페인 0건.**

**왜**: 직전 회차가 `test_data/text_j2me.zip` 을 착지시켜 `font()` 결손을 무는 자를 처음 만들었는데,
★**그것은 로컬 러너와 «주간» doc-liveness 에서만 돈다** ⇒ 「고쳤다」를 증명할 자가 **per-PR 에는 없었다**.

**사용자 영향**: 없다(제품 동작 0줄 — 시험 코드뿐). `cargo test --all` 이 다리당 **약 10 ms** 길어진다.

### ⓐ 전제 재측 — ★**「0건」이 틀렸다. 그러나 실질 주장은 살아남는다**

`grep -rn 'wie_validate' .github/` → ★**3건**(전부 `doc-liveness.yml:134·137·139`).
★**그런데 그 워크플로의 `pull_request` 트리거는 `paths: ['.github/workflows/doc-liveness.yml']`** 로
**자기 파일에만** 걸려 있다 ⇒ ★**다른 PR 에서는 0** 이다. ⇒ 「per-PR 로는 안 돈다」는 **참**이고, **수만 틀렸다.**
★**언제 거짓이 됐나**: 그 문장은 `0673e3e3`(2026-09-07 12:57) 이 썼고 `4e39dfaa`(2026-09-10 09:41)가
러너 블록을 `doc-liveness.yml` 에 넣었다 ⇒ ★**8일간 거짓**이었다. `AGENTS.md` 를 고쳤다.

### ⓑ ★급소 — ⒜ 가 «무는가» (red/green)

| 트리 | `cargo test --all -p wie_cli --bin wie_validate` |
|---|---|
| 정상 | ★**ok. 11 passed; 0 failed** |
| `font()` → `unimplemented!()` | ★**FAILED. 10 passed; 1 failed** · `panicked at wie_validate.rs:359: not implemented` |

⇒ ★**문다.** 그리고 ★**떨어진 것은 내 시험 «하나»뿐**이다(나머지 10건 불변) = 남의 축 무사.
★개악은 백업+`trap` 으로 자동 원복했고 끝난 뒤 `git diff` 로 `font()` 가 `&self.font` 임을 확인했다(커밋 0).

★★**단언이 «공허하지» 않다**: 같은 호출이 **빈 문자열에서 0** 을 낸다(`assert_eq!(minimum_width(font, "", 12.0), 0)` 통과)
⇒ `> 0` 은 판별력을 갖는다. ★그래서 「패닉 안 함」에서 멈추지 않고 **`text_layout::minimum_width` 를 지난다** —
폰트가 «있는데 글자폭이 0» 인 형상은 did-not-panic 검사를 통과하면서 아무것도 안 그린다.

### ⓒ·ⓓ 비용 — ★**두 자릿수 차이가 아니라 세 자릿수 차이다**

| 층 | 비용(실측) | 범위 |
|---|---|---|
| ★**⒜ (채택)** | 시험 실행 **0.03 s → 0.04 s**(10→11건) ≈ **10 ms** · ×6다리 ≈ **60 ms** · 새 의존 **0** | ★**`font()` 한 메서드** |
| ⒝-2 `rust.yml` 6다리 | ★**러너 블록 168.1 s**(웜 빌드 · loadavg 180) × 6 ≈ ★**17분/PR** | 텍스트 경로 전체 |
| ⒝-1 `contract` 잡 | 그 잡의 툴체인은 `if: engine == 'true'` 이고 **wasm32** 타깃·wasm 캐시다 ⇒ **네이티브 빌드를 새로** 넣어야 한다. ★필터 «안»이면 **docs-only PR 을 못 본다**(배선을 깨는 diff 가 정확히 그것) · 필터 «밖»이면 **모든 PR 이 네이티브 cargo 빌드를 문다**(그 잡은 doc-only diff 에서 지금 **12 s**) | 텍스트 경로 전체 |

★**제안이 ⒝-1 을 「cargo 빌드를 새로 넣거나」로만 적었는데 «절반 틀렸다»** — 툴체인은 **이미 있다**(`:310`).
문제는 부재가 아니라 ★**타깃·캐시가 wasm 이고 그 스텝 전체가 paths 필터 뒤에 있다**는 것이다.
★**이 repo 는 self-hosted 러너 1대**라 그 17분을 형제 PR 이 기다린다 — per-PR 비용은 공짜가 아니다.

### ★고르지 않은 층 — 왜, 그리고 언제 다시 보나

★**⒝ 를 안 골랐다**: ⑴⒜ 가 **실제로 문다**(위 red/green) — 「싼데 없는 것」이 아니었다
⑵비용이 **60 ms ↔ 17분**이다 ⑶★**`AGENTS.md` 가 2026-09-07 에 «같은 비용»으로 `--expect-last-frame` 승격을 이미 기각했고 그 결정문이 유효하다.**
★**재개 조건 — 하나뿐이고 구체적이다**: ★**`font()` «밖»의 텍스트 경로에서 회귀가 착지하고, 그것을 잡은 것이 «주간 잡»일 때.**
그때는 이 교환이 틀렸다는 증거이고, 그 미만은 아니다(`AGENTS.md` 에 같은 문장을 박았다).

### 한계 — 숨기지 않는다

- ★**⒜ 는 «한 메서드»만 지킨다.** `drawString` → 화면 사이 다른 회귀는 **여전히 주간(최대 1주)** 이다.
  ★**그것이 이 선택의 «대가»이고, 「잃는 것이 없다」가 아니다.**
- ★**수리 캠페인의 회신을 이 층이 «검증할 수 있는가» = «부분적으로만».** 캠페인이 「`font()` 결손 때문에
  broken 으로 분류된 게임」을 고쳤다고 주장하면 ⒜ 는 **그 결손의 재발만** 막는다 — 개별 게임의 PASS 는
  `game_lab/` 코퍼스가 증명하고 그것은 **Constraint 9 로 CI 에 못 들어간다**. ★**그 축은 별건이다.**
- ★시험이 `HeadlessPlatform` 구성을 `run()` 과 **따로** 적는다 ⇒ 구조체에 필드가 늘면 **컴파일 에러**로 갈린다.
  ★그것은 «조용한 드리프트»가 아니라 **눈에 띄는 실패**라 그대로 두었다.
