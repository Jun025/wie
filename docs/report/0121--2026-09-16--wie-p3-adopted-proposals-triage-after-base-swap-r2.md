## [2026-09-16] 채택 제안 9건 재측 — 8건이 낡았고, 정본 한 곳만 살아 있었다 (wie-p3-adopted-proposals-triage-after-base-swap-r2)

### 무엇을

운영자가 tower 에서 채택한 wie P3 제안 **9건**을 base swap(PR #161 · `37734e74`) **이후 기준으로** 다시 재고,
세 칸(⒜집행 / ⒝발권초안 / ⒞무효)으로 갈랐다. 결과: ★**⒜ 1건 · ⒝ 1건 · ⒞ 7건.**

코드 변경은 ⒜ 1건뿐이고 **제품 코드 0줄** — `docs/upstream-realign-verdict.md` §6-P4 의
「선 안쪽(보낼 수 있다)」 세 항목을 새 base 에서 재측한 값으로 갱신했다(+15 / −5).

### 왜

제안 9건 중 **넷이 「조각 D 에 …를 넣어라」**인데 ★**조각 D 는 이미 끝났다**(PR #161 · 2026-09-16 08:35 착지).
「넣을 자리」가 지나간 뒤에 제안을 그대로 집행하면, 이미 참이 된 것을 다시 하거나 없는 자리에 적게 된다.

그리고 정본이 실제로 낡아 있었다. §6-P4 는 upstream 에 보낼 것을 **10종 + canvas.rs +149줄 + font.rs 1건**
이라 적었는데 재측하면:

| 항목 | 종전 문안 | 재측(2026-09-16) | 판정 |
|---|---|---|---|
| `wie-wipi-java` 우리만 있는 클래스 | **10종** | ★**6종** | 4종은 PR #159 로 폐기됨(런타임 중복) |
| `canvas.rs` | **+149줄 = 전부 단위테스트 9개** | ★**1 insertion · 1 deletion** | base swap 이 흡수 · 남은 1줄은 «테스트»가 아니라 **경계 검사**(`data.len() >= 4`) |
| `font.rs` 메서드 1건 | 우리 10 ↔ upstream 9 | **그대로 참** | upstream 9 ↔ ours 10 · 델타 +45줄 |

★**`canvas.rs` 항목은 «수»만 틀린 게 아니라 «근거»가 틀렸다** — 종전 문안은 「테스트라서 IP 축과 무관」이라
정당화했는데, 오늘 남은 한 줄은 테스트가 아니라 공개 포맷 파서의 방어 코드다. 수만 고치면 근거가 붕 뜬다.

### 사용자 영향

없다(문서 한 파일). 다만 ★**upstream 에 PR 을 보내는 다음 회차가 «없는 것 4종 + 흡수된 149줄»을 보내려다
멈추는 일이 사라진다** — 그 회차가 실제로 물을 대상이 6종 + 1줄 + 1메서드로 확정됐다.

### 재측 명령(다음 사람이 그대로 쳐서 같은 값에 닿는다)

```sh
git diff --name-only 44fbf265 origin/main -- wie-wipi-java/src/classes/   # 6종
git diff --stat      44fbf265 origin/main -- wie-backend/src/canvas.rs    # 1 insertion · 1 deletion
git diff --stat      44fbf265 origin/main -- wie-midp/src/classes/javax/microedition/lcdui/font.rs
```

`44fbf265` = PR #161 머지의 **upstream 쪽 부모**(`git log -1 --format=%P 36df9c31` 의 둘째 값).
★**이 sha 를 고정값으로 외우지 마라** — 다음 base swap 이 오면 그 회차의 upstream 부모로 바뀐다.

### 함께 재측된 것 (코드 변경 없음 · 근거만 남긴다)

- **그룹 B 5행**(조각 C 가 「구조적으로 못 지운다」고 판정한 것): 새 base 에서 **`cargo check --all` 통과** ⇒
  그 회차가 측정한 컴파일 오류 10건은 ★**머지가 해소했다**.
- **그룹 C 9행**(보류분 · 「Java 가시 메서드 18개 + 클래스 1종」): 새 base 에 ★**18개 전건 실재** ·
  `ImageObserver` 등록도 실재 ⇒ ★**손실 0**. 조각 C 의 예측(「upstream 판본이 그 18개를 전부 갖는다」)이 맞았다.
- ★**그러나 이것은 «설계대로 된 것»이 아니라 «맞아떨어진 것»이다** — 조각 D 의 티켓·회신 **5파일 전건에
  「그룹 B」·「그룹 C」·「폐기 21행」 문자열이 0건**이다. 제안 `p3-slice-c#p0`·`#p1` 이 요구한 리터럴 항목은
  ★**조각 D 에 전달되지 않았고**, 결과만 우연히 같았다. 다음에 같은 형태를 운에 맡기지 마라.
