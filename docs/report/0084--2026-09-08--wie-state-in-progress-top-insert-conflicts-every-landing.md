## [2026-09-08] `STATE.md` 「진행중」을 고정 안내로 — 충돌하는 것은 「맨 위」가 아니라 «지점이 하나»인 것이었다 (wie-state-in-progress-top-insert-conflicts-every-landing)

**무엇을**: `STATE.md` §진행중 을 **회차가 만지지 않는 고정 안내**(정본 = `gh pr list`)로 바꿨다.
`AGENTS.md` §Landing paperwork 에 그 계약과 근거 수치를 적었다. ★**코드·검사기·워크플로 0** · 새 파일 0.

**왜**: 착지 회차가 이 절 **맨 위**에 자기 항목을 끼워 넣어 형제 회차가 전부 **같은 삽입 지점**을 다퉜다.
`REPORT.md` 가 2026-09-07 에 같은 병으로 고정 안내가 됐고 ★**남은 절반이 정확히 이 절**이었다.

## F1 — 재현(인용 아님)

**⑴ 형식**: `## 진행중` 바로 아래 첫 줄이 삽입 지점이었다(최신이 위). `## 완료 (최근)` 도 같은 형태(항목 **83**).
최근 커밋 **40** 중 `STATE.md` 접촉 **37** · 그중 §진행중 접촉 **33**.

**⑵ 열린 PR**: **2건**(#98 `205d191c` · #99 `4fc8f32c`) — ★**2/2 가 `STATE.md` 를 만지고, 2/2 의 유일한
헝크가 old-line **35** = §진행중 첫 줄**(두 PR 의 merge-base 에서 `## 진행중` 은 34행). §완료만 만지는 것 **0건**.
※제안이 잰 모집단은 5건이었다 — 그 5건이 그 사이 착지해 오늘은 2건이다. **수가 줄었지 형태는 같다.**

**⑷ 실제로 충돌했는가 — 기계로 재현했다.** 최근 착지 **15**건 전부 `P1 ⊂ P2`(= 착지 전 base 를 당겼다).
그중 «당기기 전» 브랜치 끝을 복원할 수 있는 **10**건을 그때의 `main` 과 다시 병합했다:

| | 값 |
|---|---:|
| 충돌 | ★**10 / 10** |
| 그중 **`STATE.md` «만»** 충돌 | ★**10 / 10** — `REPORT.md` 는 **0** |

⇒ 2026-09-07 이관이 `REPORT.md` 축을 **실제로** 없앴고 남은 것이 이 파일 하나다.

**⑶ 「완료를 갈라도 그대로인가」 — 절 단위 대조군 4셀**(세 판본 모두에서 절을 들어내고 3-way 병합):

| 대조군 | 충돌 |
|---|---:|
| C0 무처치 | **10 / 10** |
| C1 §완료 제거 | 7 / 10 (★**3건은 이것만으로 깨끗해진다**) |
| C2 §진행중 제거 | 3 / 10 (★**7건이 깨끗해진다**) |
| C3 둘 다 제거 | ★**0 / 10** |

⇒ ★**제안의 「완료를 갈라도 그대로다」는 «대체로» 맞지만 «전부»는 아니다**(3/10 은 완료만으로 풀린다).
★**정확한 형태는 «둘 다 지점»이고 어느 하나도 충분하지 않다**는 것 — 그것이 이 회차의 발견이다.

★★**7 과 3 은 «분할»이다 — 따로 인용하지 마라.** 두 집합은 **서로소이고 10 을 채운다**(`7 + 3 = 10`),
그리고 ★**C1 이 깨끗하게 만드는 집합 = C2 가 잔여로 남기는 집합**이 **정확히 일치**한다:

```
§진행중 때문(7) : a88e9777 e177969b 709e35ac 74247dd0 9c0bf763 02ed8991 ed70b279
§완료   때문(3) : 6ed4ee8e 480e8654 c2c9552d
두 절 모두 (0)
```

★**우연이 아니라 강제다**: `C3 = 0/10` ⇒ 모든 충돌은 §진행중 ∪ §완료 «안»에 있다 · `C0 = 10/10` ⇒
두 절 «모두»에서 무충돌인 쌍은 없다 ⇒ `|A ∪ B| = 10`, `|A| = 7`, `|B| = 3` ⇒ 포함배제로 `|A ∩ B| = 0`.
★★**그래서 이 쌍은 «자기 검산»을 품는다 — 재측을 믿기 «전»에 이것부터 돌려라**:
**`C1_clean + C2_clean ≤ 10`**(서로소일 때만 등호). ★**초판이 C1 의 clean 을 **4** 로 적었는데
`4 + 7 = 11 > 10` 이라 «측정하지 않고도» 틀렸음이 드러난다** — 게이트②가 실제로 그렇게 반증했다.
★**스텁 길이 0줄·1줄 둘 다 같은 값**이다(측정 인공물이 아니다).
★부수 대조군: 「열린 형제 PR: #…」 **공유 열거 줄만** 들어내면 ★**0/10** 만 풀린다 ⇒ 부딪히는 것은
그 줄이 아니라 **회차 «항목»** 이다.

## F2 — 고른 안과 «버린 이유» (대가 포함)

★**결정적 실측 하나가 후보를 갈랐다** — git 3-way 병합은 두 삽입 지점 사이에 **1줄**만 있으면 붙는다:

```
같은 지점       → CONFLICT      1줄 떨어짐 → clean
0줄 떨어짐      → CONFLICT      2~4줄     → clean
```

⇒ ★**「맨 위」가 병이 아니라 «지점이 하나»인 것이 병이다.**

| 안 | 판정 | 근거 · 대가 |
|---|---|---|
| ⒜ 맨 아래 append | ★**기각 — 효과 0** | 위 실측대로 **같은 지점이면 위든 아래든 충돌**한다. 4셀 모델(진행중×완료 = top×append)에서 **4셀 전부 2/2 충돌**. ★총괄 소견이 「가장 작다」고 본 안인데 ★**작은 게 아니라 아무것도 안 한다.** |
| ⒝ PR 번호 정렬 삽입 | ★**기각** | 지점을 «분산»시키는 발상은 옳지만 ★**이 저장소의 형제 회차는 PR 번호가 «연속»이다**(#120~#133 실측) ⇒ 슬롯이 다시 서로 인접한다. 대가: 정렬 규칙을 사람이 지켜야 하고 어기면 조용히 되돌아간다. |
| **⒞ 생성물화 — «고정 안내» 형태** | ★**채택** | 지점을 **없앤다**. 대가 ⑴**오프라인에서 못 읽는다**(`gh` 필요) ⑵**진행 중 회차의 서술이 이 파일에 안 남는다**(정본 = PR 본문·티켓 · 착지하면 「완료」로 들어온다). |
| ⒞′ 렌더해서 **커밋** | ★**기각** | 회차가 그 결과를 커밋하는 순간 ★**같은 줄을 다시 매 회차 고쳐 써 지점이 되살아난다** — 고정 안내보다 **엄격히 나쁘다**. |
| 회차별 파일 | ★**범위 밖** | 제안이 명시적으로 버렸다(「진행중은 착지하면 지워지는 항목이라 성립하지 않는다」). 만들지 않았다. |

★**⒞ 를 «스크립트 없이» 고른 이유**: `REPORT.md` 가 이미 이 저장소에서 같은 처방으로 돌고 있다
(고정 안내 + 정본은 딴 데). ★**선례를 따랐지 발명하지 않았다.**

## F3 — 충돌이 실제로 줄었는가

**⒜ 열린 PR 두 개를 «순서 바꿔» 실제 `git merge`**(격리 워크트리 · 두 브랜치 무접촉):

```
처방 «전»  #98 → #99 : CONFLICT — REPORT.md STATE.md
           #99 → #98 : CONFLICT — REPORT.md STATE.md
처방 «후»  #98 → #99 : clean
           #99 → #98 : clean
```
※「후」는 세 판본(merge-base·#98·#99) 모두에 처방을 이식한 합성 커밋이다 — ★**남의 브랜치엔 커밋 0**.
※「전」의 `REPORT.md` 충돌은 이 두 PR 이 **2026-09-07 이관보다 오래돼서**다(새 회차엔 없는 축) —
그래서 「후」에서는 그 축도 이관 후 판으로 맞춰 **`STATE.md` 만 남겼다**.

**⒝ 과거 10쌍 전수** — ★**이쪽이 정직한 수다**:

| | 충돌 |
|---|---:|
| 처방 전 | **10 / 10** |
| 처방 후 | ★**3 / 10** |

★★**0 이 아니다. 「0 이 아니다」라고 적는다.** 잔여 3건의 첫 충돌 줄은 ★**전건 `## 완료 (최근)`** 안이다
(63행 · 3/3). ⇒ ★**남은 지점은 «완료의 맨 위» 하나**이고 이 회차의 범위가 아니다(`#114` 가
「완료 분할은 하지 않는다」로 이미 판단했고 그것을 뒤집지 않았다). 후속 제안으로 올렸다.
★**차이의 이유도 분명하다**: #98·#99 는 «작성» 회차라 완료를 안 만지고, 잔여 3쌍은 «착지끼리»라 만진다.

## 정보 보존 · 순서

- §진행중 항목 수 **전 1 → 후 0**. ★그 1건은 회차 서술이 아니라 「열린 형제 PR: #98 · #99」
  **열거 줄**이고, `gh pr list` 의 **낡는 사본**이었다. ⇒ ★**잃은 서술 0.** 착지 기록은 §완료 **83건 무접촉**.
  ★★**[정정] 낡는 «방향»이 초판 서술과 반대다**(게이트② 지적 N3 · 이 회차가 직접 재측했다).
  초판은 「직전 판본이 **이미 착지한 #100** 을 달고 있었다」고 적었는데 ★**`main` 에서는 재현되지 않는다** —
  #100 은 자기 착지 커밋 `53336bae`(09-08 03:08 KST = merge 18:08:38Z)에서 **정확히 그때 빠졌다**.
  ★**낡음은 «반대 방향»으로 실재한다**(둘 다 실측):
  ⑴**`main` 에서는 «열린 것을 누락»한다** — `9bd48326`(09-07 20:19 KST = 11:19Z)의 열거는 `#98 #99 #100`
    인데 그 순간 **#126**(created 09:06Z · merged 13:11Z)과 **#128**(created 11:12Z · merged 12:43Z)이
    **둘 다 열려 있었다.**
  ⑵**브랜치에서는 «착지분이 남는다»** — PR **#98** head 의 열거 줄은 `#96 #97 #99 #100 #101` 이고
    그중 ★**#96·#97·#100·#101 넷이 이미 착지**했다(열린 것은 #99 하나).
  ⇒ ★**「낡는 사본」이라는 결론은 그대로 서고, 근거가 바뀌면서 오히려 강해진다** — 양방향으로 틀린다.
- ★**기존 항목 순서 변경 0** — ⒞ 는 §완료 를 만지지 않는다.

## 형제 원장 파일 — 세어서 적기만 (이 회차는 `STATE.md` 다)

| 파일 | 공유 삽입 지점 |
|---|---|
| `REPORT.md` | **0**(2026-09-07 고정 안내) |
| `docs/report/**` (81) · `docs/worklog/**` (69) | **0**(회차별 파일) |
| `docs/worklog-coverage-remeasures.json` | **1**(배열 끝 · 판정이 `max` 라 union 안전) |
| `STATE.md` §진행중 | **1 → 0** ← 이 회차 |
| `STATE.md` §완료 | **1**(잔여) |
| `STATE.md` §다음 | **0** |

**사용자 영향**: 없다(문서 규약만 · 제품 코드·CI 무접촉). 값은 파이프라인 쪽에 있다 — 착지 회차의
원장 재해소 왕복이 측정상 10/10 → 3/10 으로 준다.
