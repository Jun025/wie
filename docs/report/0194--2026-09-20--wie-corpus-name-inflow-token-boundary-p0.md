## [2026-09-20] 유입 모집단을 「broken 한정」에서 «코퍼스 전체»로 넓혔다 — 두려워하던 대가를 재니 0이었다 (wie-corpus-name-inflow-token-boundary-p0)

채택 제안 `2026-09-20-corpus-name-inflow-token-boundary#p0` 의 이행. 제안은 「넓힐지 정하라」였고,
★**제안 자신이 적어 둔 추정 결론(「넓히지 않고 broken 한정을 명시하는 편이 맞을 수 있다」)은 실측으로 뒤집혔다.**

### 결정

★**넓힌다.** `scripts/corpus-name-inflow.mjs` 의 기본 모집단을 `game_lab/broken` → **`game_lab` 전체**로
바꾸고, **`vendor_sdk/` 한 버킷만** 뺀다. 뺀 사실은 화면에 찍는다(조용히 빼지 않는 이 도구의 설계 그대로).

| | 종전 | 현행 |
|---|---|---|
| 모집단 | `game_lab/broken` · 고유 stem **184** | `game_lab` − `vendor_sdk` · 고유 stem **451** |
| 보이지 않던 이름 | **266개(59%)** | 0 |

### 왜 — 「working 은 아무도 안 센다」가 이미 «틀린 문장»을 만들었다

제안의 근거는 가설이 아니라 **직전 회차 자신**이다. `docs/report/0187` 은 SUFFIX-ATTACHED 16건을 손으로
가르면서 11건을 「더 긴 다른 제목 — 전건 **«코퍼스 stem 이 아님»을 실측**」이라며 떨어냈다.
★그 실측은 `broken/` 에만 물은 것이고, 그중 **다섯은 `game_lab/working/` 의 아카이브다**(NFC 정규화 후 실측):

| 0187 이 「코퍼스 stem 이 아니다」라 적은 이름 | 실제 |
|---|---|
| `엑스맨3` | `game_lab/working/ktf/엑스맨3.zip` |
| `크로이센1.04` | `game_lab/working/ktf/크로이센1.04.zip` |
| `하이브리드2` | `game_lab/working/lgt/하이브리드2.zip` |
| `일지매영웅전기2` | `game_lab/working/lgt/일지매영웅전기2.zip` |
| `붕어빵타이쿤3작은화면` | `game_lab/working/ktf/붕어빵타이쿤3작은화면.zip` |

★**과장하지 않는다 — 이것은 «유출»이 아니다.** 그 다섯은 전부 `scripts/smoke_gate_baseline.tsv` 에
**이미 합법적으로 커밋돼 있다**(AGENTS.md 가 축복한 283 stem). 넓혀서 막은 것은 바이트 유출이 아니라
★**「그 이름은 코퍼스에 없다」는 문장이 참인 줄 알고 쓰이는 일**이다. 이 도구의 자기 규격이
「이름 필터가 아니다 · Constraint 9 는 바이트에 관한 것」이라고 못박고 있으므로, 고친 것은 정확히
**술어의 모집단**이지 정책이 아니다.

### 대가 — 제안이 적은 tradeoff 를 그대로 재 봤고, «지불되는 자리»에서 0이었다

제안의 tradeoff 는 둘이었다. ⑴SUFFIX-ATTACHED 바구니가 커져 **회차마다 손으로 갈 일이 는다**
⑵`smoke_gate_baseline.tsv` 의 283 stem 이 **매번 BOUNDED 로 잡힌다**.

★**둘 다 «전 추적 트리(--all-tracked)»에서는 참이고, «회차가 실제로 쓰는 기본 모드»에서는 거짓이다.**
기본 모드의 대상은 그 회차의 diff 뿐이다. 최근 착지 **25회차**를 각자의 변경 파일 목록으로 재측(pristine
`origin/main` 워크트리 · 이 PR 의 편집이 섞이지 않게 별도 트리에서 쟀다):

| 축 | broken 한정 | 코퍼스 전체 | Δ |
|---|---|---|---|
| SUFFIX-ATTACHED 쌍(손으로 갈 것) | — | — | ★**25회차 «전건» 동일 · Δ=0** |
| BOUNDED 쌍 합계 | 114 | 150 | +36 (회차당 평균 **+1.44** · **중앙값 +1** · 최대 +11 · Δ=0 인 회차 **10**) |
| `scripts/smoke_gate_baseline.tsv` 접촉 | — | — | **0/25 회차** |
| `docs/lgt_abi.md` 접촉 | — | — | **0/25 회차** |

★★**[정정 — 이 표의 초판은 파일 목록을 «세 배로» 뽑았다.]** 회차의 변경 파일을
`git diff-tree -r --no-commit-id --name-only -m --first-parent <c>` 로 물었는데, ★그 명령은 `--first-parent` 가
붙어도 **«두 부모 모두»와의 diff 를 내놓는다**(`4acb1631` 실측: 진짜 4파일 ↔ 그 명령 **12파일**).
올바른 물음은 **`git diff --name-only <c>^1 <c>`** 다. 전 25행을 그것으로 재측했고 위 표는 **재측값**이다.
★**결론은 바뀌지 않았다** — SUFFIX-ATTACHED 쌍은 여전히 **25/25 회차 동일(Δ=0)** 이고 중앙값도 **+1** 그대로다.
바뀐 것은 합계뿐이다: **211→114 · 273→150 · +62→+36 · 평균 2.5→1.44 · 최대 13→11 · Δ=0 회차 8→10.**
접촉 0/25 두 행은 **부풀린 집합에서도 0 이었으므로 부분집합에서도 0** 이다(재확인함).
※발견은 형제 회차 `wie-corpus-name-inflow-token-boundary-p1`(`docs/report/0195`)이 같은 명령을 쓰려다 했다.

⇒ ★**⑴은 측정상 «전혀» 실현되지 않았다**(늘어난 것은 사람이 읽는 줄 수이지 사람이 가르는 건수가 아니다).
⇒ ★**⑵의 283 stem 은 그 파일을 건드릴 때만 나오는데, 최근 25회차 중 0회차가 건드렸다**(전체
first-parent 이력에서도 `smoke_gate_baseline.tsv` 22커밋 · `lgt_abi.md` 24커밋 — 둘 다 드물다).
넓히며 들어온 stem 중 **244개(working 285 중 85.6%)는 추적 트리에서 그 한 파일에만 나온다** ⇒
비용이 «그 파일 한 장»에 몰려 있고, 그 파일은 거의 안 열린다.

### `vendor_sdk` 만 뺀 이유 — 재서 골랐다

`game_lab` 을 통째로 넣으면 `vendor_sdk/` 45개가 따라온다. 그것은 게임이 아니라 에뮬·SDK 아카이브다
(`agent.jar` · `KEmulator-mmpp.jar` · `lwjgl-glfw-natives-linux.jar`). stem `agent` 는 ★**지시서가
`AGENTS.md` 인 저장소에서 평범한 영어 단어**다:

| `vendor_sdk` 만을 모집단으로 --all-tracked | 결과 |
|---|---|
| BOUNDED | 13회 / 8쌍 (`agent` 9 · `KEmulator-mmpp` 4) |
| SUFFIX-ATTACHED | **34회 / 21쌍** — ★`broken/` 전체가 만드는 26쌍보다 «많다» · 전건 거짓 |

★**제외는 «내려간 하위 디렉터리»에만 적용된다** — `--corpus game_lab/vendor_sdk` 로 일부러 가리키면
여전히 본다. `inbox/`·`_nongame/` 은 **넣었다**: 분류 전 수신함이야말로 회차가 이름을 옮겨 적을 확률이
가장 높은 자리이고, 오늘 그 비용은 0(아카이브 0개·1개)이다.

### 개악 양방향 (계약 2)

★검사기(CI red)를 **만들지 않았다** — 이 도구는 코퍼스가 필요해 CI 가 돌릴 수 없다(Constraint 9).
그러나 «가드»(모집단 제외)는 넣었으므로 양방향을 쟀다. 원문을 `sed` 로 고쳐 실제로 돌렸다:

| | 모집단 | `agent` BOUNDED | SUFFIX-ATTACHED |
|---|---|---|---|
| ⒜ 정상(현행 판) | 451 | **0** | 40쌍 |
| ⒝ 개악 — `EXCLUDED_BUCKETS` 를 비움 | 496 | **11** | **61쌍** |

⇒ 제외가 살아 있고(⒜), 떼면 즉시 잡음이 들어온다(⒝). ★한 방향만 보이지 않는다.

### 한계 — 숨기지 않는다

- ★**전 추적 트리 기준으로는 확실히 비싸졌다**: BOUNDED 161쌍 → **491쌍**. `--all-tracked` 는 진단 모드이고
  회차가 수를 «보고»하는 모드가 아니지만, 그 모드를 쓰는 회차는 이 수를 그대로 만난다.
- ★**겹치는 stem 은 두 번 센다.** `엑스맨3` 이 본문에 있으면 stem `엑스맨3` 으로 BOUNDED 1쌍,
  stem `엑스맨` 으로 SUFFIX-ATTACHED 1쌍이 «함께» 잡힌다. 넓힐수록 이 겹침이 는다 —
  전 추적 트리 SUFFIX 31쌍 → 40쌍의 상당 부분이 그것이다. 합치는 규칙은 이 회차가 정하지 않았다.
- ★**「그 이름은 이미 저장소가 합법적으로 커밋한 자산이다」를 기계는 여전히 모른다.**
  `smoke_gate_baseline.tsv` 의 283 stem 을 «기존 자산»으로 표시하는 넷째 갈래는 만들지 않았다 —
  그것은 이 제안이 물은 것이 아니고, 표본이 1건이라 지금 굳히면 오탐이 굳는다.
- ★`vendor_sdk` 제외는 **디렉터리 이름 하드코드**다. 버킷 이름이 바뀌면 조용히 다시 들어온다 —
  다만 도구가 «뺀 버킷»을 매번 찍으므로 「없음」이 화면에 뜨는 것으로 보인다.

### 이번 회차가 넣은 이름

★이 리포트는 위 다섯 이름을 본문에 적는다 — 그 다섯이 **결정의 근거 자체**라 인용하지 않으면 판정이
검증 불가능해진다. 전부 `smoke_gate_baseline.tsv` 에 이미 커밋된 이름이고, 새로 들어온 코퍼스
바이트는 0이다. 도구가 잰 이 PR 의 수는 worklog 의 `verification` 에 있다.
