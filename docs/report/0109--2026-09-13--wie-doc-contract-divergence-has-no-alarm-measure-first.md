## [2026-09-13] 문서↔계약 어긋남 경보 — ★**만들지 않는다(⒥)**. CI 가 그 계약 파일을 «볼 수 없다» (wie-doc-contract-divergence-has-no-alarm-measure-first)

- **산출물은 «판정»이다**(계약 1). 코드·검사기 **구현 0** · `AGENTS.md` 재수정 **0** · 남의 정본(`contracts/`·`templates/`) **무접촉**.

- **★★판정 — ⒥ 「만들 값이 없다」. 근거를 수로 적는다.**
  | 축 | 측정값 | 판정에 기여하는 바 |
  |---|---:|---|
  | ⓐ 지금 잡는 기계 | **0** | 「울지 않는다」는 **참** |
  | ⓐ-2 repo 내 정합성 검사기 | **9** | 전부 살아 있다 — 그러나 ★**9/9 가 «repo 안 ↔ repo 안»** |
  | ⓑ **살아 있는 지시문** 중 «값 복사» | ★**7**(그중 ★**현행 어긋남 1** — 이 회차가 고쳤다) | 티켓 임계(10+)에 **미달** · ★**초판의 «2» 는 두 파일만 본 값이라 틀렸다** |
  | ⓑ 같은 계약의 **재진술**(규칙 복창) | 3 | 값이 아니라 규칙 — 이 repo 사건 대장이 독립으로 떠받친다 |
  | ⓑ **포인터**(경로·절 앵커) | 4 | 복사가 아니다 — 깨지면 **시끄럽다**(★단 «절 앵커»는 예외 · ⓓ 한계 참조) |
  | ⓑ **이력 기록**(세는 기준에서 제외) | **45** | `STATE.md` §완료 21 + `docs/report/**` 24 — 스냅샷이 정본이라 «어긋남»이 아니다 |
  | ⓒ CI 러너가 그 계약을 볼 수 있는가 | ★**볼 수 없다**(★**9/9** GitHub 호스티드 · self-hosted **0**) | ★★**이 한 줄만으로 ⒥ 가 선다**(개수와 무관) |
  | ⓒ 참조 대상의 버전 추적성 | ★**있다 — 초판의 「없다」는 철회**(`origin/main` 재측: 둘 다 추적 · tpl **1,228줄** · `4-A` **11건**) | ★**둘째 다리는 무너졌다** — 남는 위험은 「머신마다 다른 바이트」로 좁혀진다 |

- **★★ⓒ 가 이 판정의 «결정타»다 — 단 다리는 «하나»다. 초판의 둘째 다리는 «틀렸고 여기서 철회한다».**
  ★**살아남은 다리 = CI 가 그 계약 파일을 «볼 수 없다»**: `.github/workflows/` **9종 전수**
  (`coverage`·`dependabot`·`doc-liveness`·`engine-contract`·`opencode`·`publish-artifact`·`rust-audit`·`rust`·`web`)
  가 **9/9 `runs-on` 보유 · self-hosted 0** ⇒ 러너에 `~/orchestrator` 가 **없다**.
  ⇒ ★**게이트가 될 수 없고, 로컬 전용이면 «매 회차가 기억해야 하는 것»이 하나 느는 것**이다.
  ★**이 다리는 «값 복사 개수»와 무관하게 성립한다** — ⓑ 가 7이든 70이든 검사기는 CI 에서 못 돈다.
  ★★**[철회] 초판의 「참조 대상의 버전 추적성이 없다」는 «거짓»이었다 — 내가 «낡은 체크아웃»을 «정본»으로 착각했다.**
  `orchestrator` **`origin/main` 기준 재측**(이번 회차):
  | 참조 대상 | `origin/main` | 판정 |
  |---|---|---|
  | `templates/merge-ticket.tpl` | ★**추적됨** · **1,228줄** · `4-A` **11건** | ★**§4-A 는 «실재한다»** |
  | `contracts/upstream-sync-repos.conf` | ★**추적됨** · `repo` **2건** 등재 | 정상 |
  ⇒ ★**정본은 제대로 버전관리되고 있다.** 초판이 「untracked · 63줄 · `4-A` 0건」이라고 적은 것은
  ★**이 맥의 «작업 체크아웃» `~/orchestrator`@live(HEAD `13b88a0c` · 2026-08-17 · `origin/main` 보다 **2,476 커밋 뒤**)
  에서 본 값**이었다. ★**그 수치를 지우지 않고 남긴다** — 그것이 「로컬에서 도는 검사기가 무엇을 읽게 되는가」의
  **실측 예시**이기 때문이다: 그 체크아웃에서 커밋본은 **63줄 · `4-A` 0건**이고 그 파일 중 하나는 **untracked** 다.
  ⇒ ★**남는 위험은 «정본이 추적되지 않는다»가 아니라 «머신마다 다른 바이트를 읽는다»** 이고,
  이것은 여전히 로컬 전용 검사기의 실질 결함이다(★그러나 ⓒ 첫 다리만으로도 판정은 이미 선다).

- **ⓐ 전수 — 잡는 기계 0**: repo 검사기 **9종**(`check-audit-warnings`·`check-doc-liveness-parity`·
  `check-docs-report-serial`·`check-engine-contract`·`check-engine-runner-fixtures`·`check-parity-lock-wired`·
  `check-worklog-coverage`·`check-worklog-json`·`dod_ci_parity.rs`) 중 ★**repo 밖 경로를 읽는 것 0**.
  `~/orchestrator/bin/` 에서 `AGENTS.md` 를 읽는 도구 **0**. ⇒ 양쪽 어디에도 이 축이 **없다**(중복 위험 0).
  ★**그 9종이 «할 수 있는» 이유가 대비를 만든다** — 전부 **한 PR 안에서 양쪽이 함께 움직이는** 축이다
  (문서 ↔ 워크플로 · 문서 ↔ 픽스처 · 원장 ↔ 원장). 문서↔외부계약은 ★**양쪽이 다른 저장소에서 따로 움직인다.**
  ★★**단 이 repo 에는 «그 간극을 실제로 푼» 선례가 하나 있다 — `check-engine-contract.mjs`(F4).**
  그것이 검사하는 `docs/contracts/featurephone-engine-contract.json` 은 ★**otterpebble 이 소비하는 «두 저장소
  계약»인데 이 repo 안에 «벤더링»되어 추적된다**(머리주석: 「The otterpebble featurephone shell consumes …
  calls a pinned surface on it」). ⇒ ★**«복사해서 추적»하면 cross-repo 계약도 in-repo↔in-repo 로 바뀌고,
  그 순간 CI 에서 돌 수 있다.** 아래 재개 조건 ⑶ 이 «가정»이 아닌 이유가 이것이다.

- **★★ⓑ — repo 전수로 «다시» 셌다(초판은 `AGENTS.md`·`CLAUDE.md` 두 파일만 봤다 · 그것이 반려 사유다).**
  ★**세는 기준을 먼저 못박는다**(초판에 없던 것): ⒜**살아 있는 지시문** = 지금 회차가 «따라야 하는» 문장 ⇒ **센다**
  ⒝**이력 기록** = 그때 무슨 일이 있었는지의 서술(`STATE.md` §완료 · `docs/report/**` 의 날짜 회차 기록) ⇒ **빼다**.
  ★**왜 ⒝를 빼나**: 이력은 **그 시점의 스냅샷이 정본**이라 계약이 나중에 바뀌어도 «어긋난» 것이 아니다 —
  오히려 고치면 **사료가 거짓이 된다**(이 저장소가 `-fix` 회차에서 이미 그렇게 판정했다).
  ★**전수 계수**(`git ls-files '*.md'` · `origin/main` 기준):

  | 문서 | 히트 | 분류 |
  |---|---:|---|
  | `STATE.md` §완료 | **21** | ★**이력 ⇒ 제외**(검수자가 지적한 그 대다수) |
  | `docs/report/**` | **24**(13 파일) | ★**이력 ⇒ 제외** |
  | `STATE.md` §다음(P3 DoD) | 5 | **살아 있는 지시문** — 값 복사 + **정본 포인터 동반**(정확) |
  | `STATE.md` §진행중 | 1 | 포인터(`~/orchestrator/tasks/`) |
  | `AGENTS.md` | 7 | 값 복사 **2** · 규칙 재진술 2 · 포인터 2 · 자기 인용 1 — ★**그 둘은 «같은 계약»이되 «다른 두 절»**(§DoD ↔ §Git Workflow)이다. ★**초판의 「같은 불릿」은 틀렸다**: 한 불릿이면 한 번에 보이지만 **두 절이면 한쪽만 고쳐도 나머지가 남는다** — 9일 모순이 정확히 그 형태였다 |
  | `CLAUDE.md` | 2 | 규칙 재진술 1 · 포인터 1 |
  | ★**`CONTRIBUTING.md`** | **1** | ★**값 복사 · 그리고 «현행 어긋남»** ⇒ ★이 회차가 고쳤다(아래 처분) |
  | `REPORT.md` | 1 | 사실 복사(«머지 커밋으로 착지» — **정확**) |
  | `docs/report-migration-revert.md` | 2 | 사실 복사(`-m 1` 의 근거로 씀 — **정확**) |
  | `docs/upstream-realign-verdict.md` | 7 | 값 복사 + **정본 포인터 동반**(정확) |

  ⇒ ★**살아 있는 지시문에서 «값 복사» = 7**(초판 **2** 는 두 파일만 본 값이라 **틀렸다**) ·
  이력 제외분 **45**. ★**그중 «지금 어긋난» 것은 1**(`CONTRIBUTING.md`) — 나머지 6은 **정확**하고,
  ★**대부분이 «정본 포인터를 함께 달고 있다»**(`upstream-sync-repos.conf` 를 이름으로 가리킨다).
  ★★**이 수가 판정을 뒤집지 않는 이유**: 7 은 티켓 임계(10+)에 못 미치고, ★**더 중요하게는
  ⓒ의 첫 다리(CI 가 못 본다)가 «개수와 무관하게» 검사기를 불가능하게 한다.**

- **★★ⓑ-2 처분 — `CONTRIBUTING.md` 의 현행 어긋남은 «이 회차에서 고쳤다»**(별 티켓으로 미루지 않았다).
  어긋난 문구(★줄번호 아닌 문구로 지목): 「whoever merges removes the remote branch
  (**`gh pr merge --squash --delete-branch`**) and the local one (`git branch -D <branch>`; force `-D`
  **because squash-merged branches aren't recognized as merged by `-d`**)」.
  ⇒ ★**`AGENTS.md` 가 `9ef24b43` 으로 고친 것과 «같은 결함이 같은 저장소의 다른 파일에» 남아 있었다.**
  ★**고친 이유**: ⑴계약 3 의 범위 밖 목록에 이 파일은 **없다** ⑵고치는 형태가 **이미 착지한 선례와 동일**
  (값 복사 → 정본 포인터) ⑶★**그대로 두면 이 판정이 「사람 규율로 충분하다」의 «반례»를 안고 닫힌다**
  — 검수자 지적 그대로다. ⇒ **같은 문형으로 교체**(방식은 티켓 frontmatter 가 정한다 · 정본은 tpl §4-A ·
  이 repo 값은 `merge` · `--delete-branch` 미사용 이유까지).

- **ⓓ 더 싼 대안 — ★이미 착지했고, 값이 증명됐다**: 「**값을 복사하지 말고 정본을 가리켜라**」
  (오늘 `9ef24b43`). ★**복사가 없으면 어긋날 것이 없다** ⇒ 검사기가 지킬 표면 자체가 사라진다.
  | | 비용 | 커버리지 | 오탐 |
  |---|---|---|---|
  | ⒤검사기 | 신규 술어 + ★**CI 에서 실행 불가**(로컬 전용 ⇒ «기억해서 돌려야» 한다) | 값 복사 2곳 | ★위 ⓒ 두 갈래 |
  | ⒥가리키기 | **0**(이미 착지) | ★**값 복사 자체를 없앤다** | 없음 |
  ★★**⒥ 의 한계도 적는다(F3) — «가리키기»가 공짜는 아니다**: 절 단위 포인터(`§4-A`)는 ★**조용히 썩는다** —
  정본이 절을 renumber 하면 이 repo 에는 **아무 신호도 오지 않고**(그것이 바로 이 티켓의 결함이다) 포인터는
  «틀린 절»을 가리킨 채 남는다. ★오늘은 실재한다(위 재측 `4-A` 11건) — **오늘 기준으로만 참**이다.
  ⇒ ★**⒥ 는 «위험을 0으로 만든다»가 아니라 «조용한 값 불일치»를 «시끄러운 경로 불일치»로 «바꾼다»**(후자가 낫다).
  ★★**⒤의 치명상은 «로컬 전용»이다** — 그러면 그것은 게이트가 아니라 **또 하나의 «매 회차가 기억해야 하는 것»**이고,
  ★**이 티켓이 없애려던 실패 양식(「매 회차가 스스로 알아차려야 한다」)을 그대로 재생산한다.**

- **⒥ 처방 — 사람 규율 한 줄을 «어디에» 둘 것인가**(계약 3 · ★**이 회차에서 쓰지 않았다** — `AGENTS.md`
  재수정이 범위 밖이라 **후속 제안**으로 남긴다):
  > 자리 = `AGENTS.md` §Constraints 의 **「Held by you, not by a machine」** 절(기계가 안 잡는 것만 모아 둔
  > 바로 그 자리 · 이미 「No game bytes」·「Secrets are referenced, never embedded」가 같은 성격으로 산다).
  > 문안 = **「An external contract is referenced, never copied — point at the canon (path + section) and let
  > the ticket's frontmatter bind. Nothing machine-checks this: CI cannot see `~/orchestrator`.」**
  ★**「Secrets are referenced, never embedded」와 «같은 문형»을 고른 것은 우연이 아니다** — 그 절의 기존 항목들도
  전부 「기계가 못 잡으니 사람이 진다」를 명시한 것들이고, ★**«못 잡는 이유»까지 적는 것이 그 절의 관례**다.

- **★재개 조건**(기각은 영구가 아니다): ⑴**self-hosted 러너가 도입**되어 CI 가 `~/orchestrator` 를 볼 수 있게 된다
  ⑵ⓑ의 **«값 복사» 자리가 5곳 이상**으로 는다(오늘 2) ⑶계약 파일이 **이 repo 안으로 벤더링**된다
  (그러면 in-repo↔in-repo 가 되어 기존 9종과 같은 축이 된다 — ★그때는 싸다).

- **게이트**: ★**러스트 무접촉**(diff 3파일 전부 `.md`/`.json`) ⇒ 4게이트 전량 불요. 문서 검사기 5종 전건 통과.
  채택 기록 = worklog `adoptedProposals: ["2026-09-13-merge-section-points-at-the-canon#p0"]`.
