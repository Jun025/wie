## [2026-09-16] 「조각 D 재발권은 #159 뒤로」는 «이미 지켜졌다» — 다만 #159 의 가드는 지금 돌지 않는다 (wie-adopt-slice-d-blocked-on-lgt-abi-decision-p2)

### 무엇을

채택 제안 `2026-09-16-slice-d-blocked-on-lgt-abi-decision#p2`(원문 =
`docs/worklog/2026-09-16-slice-d-blocked-on-lgt-abi-decision.json` `proposals[2]`)는
「**조각 D 재발권은 #159 착지 «뒤»로 잡는다**」 — **순서 요구**다.

★**그 순서는 지켜졌다.** **제품 코드 0줄** · 변경은 원장 3파일.

### 대전제 재고 — 두 축을 «원격 조회»로 쟀다

| 축 | 실측 | 판정 |
|---|---|---|
| **순서** | `#159` MERGED **2026-09-16T01:20:39Z**(머지커밋 `3744f84e`) · `#161`(조각 D 재발권분) MERGED **08:35:30Z** ⇒ ★**#159 가 먼저 · 간격 7:14:51** | ★지켜짐 |
| **실익**(같은 파일을 두 번 화해시키지 않는다) | #159 가 지운 **7파일**이 오늘 `origin/main` 에 ★**전건 부재**(`git cat-file -e` 잔존 **0**) | ★성립 |

⇒ ⓐ **해소됐다.** ⓑ **같은 축 0**(순서를 강제하는 기구는 없다 — 사람 판단으로 지켜졌다).
ⓒ **제안이 틀린 것은 아니다** — 제안을 낸 시점에는 정확히 참이었고, ★**틀린 것은 «시점»이다**(형제 `#p0` 과 같은 형태).

### 제안의 「8건」 ↔ 내 실측 「7건」 — 모순이 아니다

제안은 「74 충돌 중 **8건**이 #159 가 지우는 파일과 같다」고 적었고, 내가 센 삭제 파일은 **7건**이다.
★**센 대상이 다르다**: 제안이 센 것은 **충돌과의 교집합**, 내가 센 것은 **삭제 수**다.
#159 가 `wie_wipi_java/` 아래에서 만진 것은 전수 **10건**(`D` **7** · `M` **2** = `classes.rs`·`lib.rs` · `A` **1**)
⇒ **삭제 7 + 수정 1 = 8** 로 맞아떨어진다.

### ★못 잰 것을 «못 쟀다»고 적는다

조각 D 가 기록한 **74 → 67** 충돌 계수는 ★**오늘 재현할 수 없다** — 그 수는 당시 merge-base 기준이고
그 base 는 이후 여러 번 움직였다(오늘 `origin/main` = `6a56ab6e`).
⇒ 그래서 순서와 실익을 각각 **착지 시각**과 **파일 부재**로 **대체 측정**했다 — 그 둘은 **오늘도 참**이다.
★**재현 불가를 «확인됨»으로 적지 않는다.**

### ★부수 관측 — #159 가 세운 개악 대조가 **지금 돌지 않는다**(범위 밖 · 고치지 않았다)

#159 는 `wie_wipi_java/tests/preload_classes_come_from_the_runtime.rs` 를 자기 개악 대조로 세웠다.
오늘 실측:

- 파일은 **실재**한다(**3,136B**).
- 그러나 workspace member 는 **`wie-wipi-java`**(하이픈)이고 ★**`wie_wipi_java`(언더바)는 member 가 아니다**
  (`grep -c '"wie_wipi_java"' Cargo.toml` → **0**).
- ⇒ `cargo test --all --no-fail-fast` 출력에 그 이름이 ★**0회** — **존재하지만 실행되지 않는 가드**다.

★**조각 D 회신이 이미 「고아 5파일 · 조각 E 소관」으로 지목한 축**이라 ★**이 회차는 손대지 않았다**
(고치면 코드 0줄 전제가 깨지고, 그것은 이 제안이 요구한 일도 아니다). ⇒ 후속 제안으로 뺐다.

### 무엇을 잃는가 / 안 하면 무엇이 나쁜가

- **잃는 것 ⑴ — 아무것도 진전시키지 않았다.** 이미 지켜진 순서를 **확인**했을 뿐이다.
- **잃는 것 ⑵ — 회차 하나를 썼다.** 이미 끝난 일에 슬롯이 들어갔다.
  ★그 대가로 사는 것은 아래 「안 하면」과, **부수 관측 하나**다.
- **잃는 것 ⑶ — 이 기록도 부분적으로 «재현 불가»를 안고 있다.** 74→67 은 다시 못 잰다(위 절).
- **안 하면**: ★**같은 제안이 내일 또 채택된다.** 형제 `#p0` 이 **집행 4시간 30분 뒤에 채택된** 실례가 있다.
  `adoptedProposals` 기록이 그 고리를 끊는다 — ★**이 회차의 진짜 산출물이 그 한 줄**이다.

### 범위 밖 — 일부러 안 한 것

- ★**#159 의 고아 가드를 살리지 않았다**(조각 E 소관 · 후속 제안으로 기록).
- 형제 `#p0`(⒜/⒝ 결정 — **이미 해소**, PR #172 로 기록) · `#p1`(연번 `0113` 이중 claim — **그 사이 해소**) **무접촉**.
- 코드 `.rs` 접촉 **0** · 픽스처 **0** · 검사기 배선 **0** · `docs/upstream-realign-p3-slices.md` **무접촉**.
