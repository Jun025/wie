## [2026-09-17] 0134 중복은 «이미» 해소됐다 — 그리고 «규칙대로» 해소됐음을 커밋 시각으로 확인했다 (wie-adopt-serial-claim-window-cross-pr-p0)

**무엇을**: 채택 제안 `2026-09-17-serial-claim-window-cross-pr#p0` 의 이행 — ★**코드 0줄**.
제안이 지목한 상태(「PR #176·#177 이 같은 연번 `0134` 를 들고 **지금** 둘 다 열려 있다」)가
**이미 해소돼 있었고**, ★**제안 자신의 tradeoff 가 그것을 예고했다**
(「남은 일은 #177 이 아니라 «이미 끝났다»이므로 이 제안은 그 사실의 기록으로 읽어라」).

**왜 그래도 회차가 값을 하는가**: 「끝났다」를 **믿지 않고 쟀고**, 재는 김에 그 다음 질문 둘에 답했다 —
⑴**규칙대로** 끝났는가 ⑵이 부류가 **다시 나지 않는가**. 둘 다 실측했다.

**사용자 영향**: 없다(코드 0줄 · 제품 동작 0줄).

### ⒜ 해소 확인 — 두 축
- **#176 · #177 둘 다 `MERGED`**(`00:58:39Z` · `01:41:30Z`). 착지본 파일 목록:
  #176 = `docs/report/0133…` + ★**`0139…`** · #177 = `0134…` ⇒ **중복 0**.
- **지금 열린 PR 전건 claim**(2026-09-17 18:02 KST 재측):
  `#162 0116 · #180 0137 · #184 0142 · #185 0144+0146 · #187 0147 · #188 0148` ⇒ ★**중복 0**.
  디스크 **139건**도 중복 0(`check-docs-report-serial` rc=0).

### ⒝ ★「규칙대로였는가」 — 커밋 시각으로 판정
규칙은 「**나중에 claim 한 쪽이 옮긴다**」다. 그 판정은 **PR 번호가 아니라 claim 시각**이고, 둘은 실제로 **어긋난다**.

| 사건 | 커밋 | 시각 |
|---|---|---|
| **#177** 이 `0134` 를 claim (★**먼저**) | `f8b48c99` | **08:13:15** |
| **#176** 이 `0134` 를 claim (★**나중** · 4분 58초 뒤) | `115e3665` | **08:18:13** |
| ★**#176 이 `0139` 로 이동** | `0aa4a865` | **09:45:29** |

⇒ ★**옮긴 쪽 = 나중에 claim 한 쪽 = 규칙과 일치.** 번호만 보면 #176 이 «낮아서» 먼저처럼 보이는데
실제 claim 은 #177 이 먼저였다 — ★**두 축이 진짜로 갈리는 사례**이고, `AGENTS.md:478` 이 그 점을 이미 기록했다.

### ⒞ ★「다시 나지 않는가」 — 개악 대조 양방향
코드를 고치지 않았으므로, 막는 힘은 이 회차가 아니라 **이미 착지한 축**이 진다.
그것이 **실제로 무는지**를 제품 호출부에서 확인했다(`scripts/check-docs-report-serial.mjs` 기본 모드).

| 축 | 개악 | 결과 |
|---|---|---|
| **A** 열린 PR claim 대조 | 열린 PR **#187** 이 든 `0147` 을 내 트리에서 **또** 집는다 | ★**rc=1** — 오류가 **상대 PR(#187)·상대 파일명·이동 규칙**까지 찍는다 ↔ 제거 후 **rc=0** |
| **B** 착지 중복 대조 | 디스크에 `0145` 를 하나 더 둔다 | ★**rc=1** 「아직 착지하지 «않은» 쪽을 `--next-serial` 번호로 `git mv` 하라」 ↔ 제거 후 **rc=0** |

★**A 가 «상수 대 상수»가 아니다** — 대조 대상이 **GitHub 의 열린 PR 목록**이고, 실제로 #187(오늘 내가 연 PR)을 지목했다.

### 남긴 천장(숨기지 않는다)
⑴창은 **«조회→커밋»(회차 작업 시간)** 이라 두 회차가 «거의 같은 순간»에 커밋하면 **둘 다 green** 으로 지나간다 —
그때는 **착지 후 중복 축(B)** 이 받는다. **2단 방어이지 1단이 전부가 아니다.**
⑵열린 PR 조회가 실패하면(`gh` 404·권한) A 축은 ★**경고로 강등되고 막지 않는다**.
이번엔 정상 동작했으나 「막는다」를 상시 사실로 읽지 마라.

### 회귀 0 — 전건 합산(★`tail` 미사용)
코드 0줄이므로 「영향 없음」을 **값으로** 보인다:
4게이트 + `cargo +beta clippy` **전부 rc=0** · `RUST_MIN_STACK=4194304 cargo test --all` **rc=0**
— `^test result:` 전수 합산 = ★**43타깃 · 386 passed · 0 failed · 0 ignored** ·
node 검사기 **9/9 rc=0** · `npm run audit` **PASSED**.
