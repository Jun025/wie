## [2026-09-07] no-leak 의 «dist 스캔» 을 배선하지 «않는다»로 닫았다 — 저울을 다시 쟀고 근거를 커밋했다 (wie-no-leak-dist-scan-never-runs-in-ci)
- **무엇을**: `scripts/audit-no-leak.sh` 섹션 5 머리에 ★**주석 블록 하나**(어디서 도는가 · CI 로그 근거 · 배선하지 않은 이유 4 · ★뒤집힐 조건). ★**판정 술어 무접촉**(비-주석 변경 **0**) · 워크플로 무접촉.
- **왜**: 운영자 채택 제안 `2026-09-06-wire-audit-no-leak-in-ci#p0`. ★**그 제안을 쓴 회차가 스스로 「안 하는 쪽」을 골랐고 운영자가 그것을 채택했다** ⇒ 이 회차의 일은 배선이 아니라 ★**저울을 다시 재는 것**이었다.
- **★F1 — 「CI 에서 한 번도 안 돈다」를 CI 로그로 · ★전수로 확인**: 그 스텝을 실은 run **19건**(API: **18 success + 1 skipped**) 중 ★**성공 18건 전건 로그를 받아 18/18 이 `web/dist not built yet — skipping dist scan`** 을 찍고 `contains no game files` 는 **0/18** — ★**로그 미취득 0건**.
- **★★그 과정에서 «틀린 수»를 만들었고, 원인 진단도 틀렸다 — 지우지 않고 남긴다**: 초판은 「10건 중 5건 · 나머지는 로그가 «잘렸다»」였다. ★**로그는 잘리지 않았다.** 진짜 원인은 ★**`gh run view --log` 가 일부 run 의 스텝명 열을 `UNKNOWN STEP` 으로 렌더**하는 것이고(실측 `34053057393`: 2열 유일값이 `UNKNOWN STEP` 인데 문제의 줄은 **241행에 실재** · 스텝명 grep 히트 **0** ↔ 대조군 `34058152112` 는 스텝명 히트 **26**), ★**«출력 문자열»로 한 번만 더 grep 했으면 그 자리에서 풀렸다.** ⇒ ★**0 을 «부재»로 읽지 않은 것은 옳았으나, 그 0 의 «원인»을 재지 않고 「잘렸다」로 단정한 것이 틀렸다.** ★**같은 함정을 이번에 한 번 더 밟았다**: `gh api …/jobs/<id>/logs` 가 `--allow-escape-sequences` 없이는 아무것도 출력하지 않아 **18건 전부 «미취득»으로 보였다** — 이번엔 결론 «전»에 원인을 재서 풀었다.
- **★★F2⑶(이 회차의 급소) — 「추적 바이너리 0 이면 dist 도 안전」은 ★참이다**: ⒜★`web/public/` 이 **없다**(Vite 의 verbatim 복사 벡터가 부재) ⒝빌드가 `npm run wasm && tsc -b && vite build` 뿐 — **내려받기 0** ⒞dist 에 닿는 유일한 «추적되지 않은» 입력 `web/src/wasm/` 는 `.js`/`.wasm`/`.d.ts` 만 낸다 ⒟★**실제로 빌드했다**: `web/dist` 파일 **4개**(wasm·js·html·css) · **게임류 0** · 그 상태에서 섹션 5 는 `✅ contains no game files` 로 **공허 통과**.
- **★F2⑴⑵**: 두 호출은 **다른 절**을 돈다(`:124-127` dist ↔ `:128` `git ls-files`) — 공허하지 않다 · 비용 「+0.3s」는 맞다(로컬 0.26~0.34s · ★러너 실측 **0.067s**).
- **사용자 영향**: 없음(주석). 대신 ★**다음 사람이 같은 조사를 다시 하지 않는다** — 그것이 「안 한다」를 값하게 만드는 유일한 것이다.
- **★뒤집힐 조건(주석에 명시)**: `web/public/` 이 생기거나 · 빌드가 자산을 내려받거나 dist 로 복사하기 시작하면 벡터가 열리고 그때는 두 번째 호출이 값한다.
- **★남는 구멍**: 술어가 **게임 확장자만** 보므로 «게임류가 아닌» 추적되지 않은 파일(wasm 산출물)은 대상이 아니다 — ★술어 확대는 **이 회차 범위 밖**이다(티켓 명시).
