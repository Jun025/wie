## [2026-09-19] LGT 값싼 2건 — ★하나는 되살렸고, ★★다른 하나는 «이미 살아 있었다»(감사의 오탐 1건). 대신 «진짜로 없던» 케이스를 넣었다 (2026-09-18-sibling-crates-missing-tests-machine-audit-p1)

### ⒜ 원문 대조 — 브리프와 원문은 «글자 그대로» 같다

정본 = `docs/worklog/2026-09-18-sibling-crates-missing-tests-machine-audit.json` 의 `proposals[1]`.
브리프가 인용한 `title`·`plainSummary` 두 줄은 원문과 **바이트 동일** — 어긋난 곳 **0**.

★**원문 `tradeoff` 가 이 회차에 «두 가지를 먼저 확인하라»고 했고, 둘 다 했다** — 그리고 ★**그 둘이
서로 다른 결론을 냈다**:
⑴「svc_ids 쪽은 번호 체계가 바뀌었을 수 있으니 번호를 먼저 확인하라」 → ★**안 바뀌었다. 그대로 적용된다.**
⑵「helloworld 쪽은 픽스처가 필요한지부터 봐라」 → ★★**픽스처가 아니라 «그 단언이 이미 있는지»가 문제였다.**

### ⒝ ★★표적 2는 «오탐»이다 — 개악으로 물어서 확인했다

원문: 「`test_helloworld_jar_named_application` 은 HEAD 동명 파일에 나머지 2케이스만 남은 자리라
**세 번째 케이스만 더하면 된다**」.

★**그 단언은 사라지지 않았다 — `test_helloworld` «안»에 있다.** 지금의 `test_helloworld` 는 픽스처의
`00000000.jar` 를 **`application.jar` 로 바꿔 넣고** 부팅한다(`:32-35`). 그게 옛 세 번째 케이스의 본문
그대로다(`cbb9ba0b` 에서 복원해 대조).

★**주장이 아니라 «측정»으로 확인했다**(`evidence/precheck-helloworld-coverage.txt`) —
옛 이름 기반 탐색(`format!("{aid}.jar")`)으로 되돌려 보았다:
```
원형(내용 기반 탐색)          test result: ok.     2 passed
개악(이름 기반으로 되돌림)     test result: FAILED. 0 passed / 2 failed   ★두 시험 다 red
원복                         test result: ok.     2 passed
```
⇒ ★**그 회귀를 «지금의 `test_helloworld` 가 잡는다»** ⇒ 세 번째 케이스를 더할 필요가 없다.

★★**감사가 왜 이것을 놓쳤는지도 원문 안에 이미 있다** — 축 E 는 **«함수 이름 집합»**을 비교한다.
★**이름은 다른 시험 «안으로» 사라질 수 있다.** 원문 요약이 스스로 「A~D 는 파일을 보므로 …」라고 축의
한계를 적었는데, ★**E 에도 대칭의 한계가 있다**: 파일 축은 «합쳐진 시험»에 눈멀고, 함수 축은 «흡수된 단언»에 눈먼다.
⇒ ★**감사의 10건 중 최소 1건은 오탐이다.** 나머지 9건에 같은 축의 오탐이 더 있는지는 **재지 않았다**(후속).

### ⒞ ★그런데 «진짜로 없던» 케이스가 있었다 — 그 쪽을 넣었다

옛 설계는 **일부러 비대칭인 쌍**이었다(그 회차의 주석을 복원해 읽었다):
「`test_helloworld` 는 픽스처 jar 가 AID 이름이라 **그 깨짐을 볼 수 없고**,
`test_helloworld_jar_named_application` 은 이름을 바꿔서 **본다**」.

★★**오늘은 두 시험이 «둘 다» jar 를 옮긴다** — `application.jar` 와 `P/00000000.jar`.
⇒ ★**아카이브가 실제로 출하되는 형상(손대지 않은 `00000000.jar`)을 부팅하는 시험이 «하나도 없었다».**
그 baseline 을 `test_helloworld_unmodified_archive` 로 넣었다 — ★**옛 쌍에서 «살아남은 쪽»이 아니라
«사라진 쪽»이 그것이다.**

★**그 시험의 검출력이 «고유한가»도 물었다**(`evidence/mutation-helloworld-baseline.txt`):
AID 이름 jar 를 건너뛰는 탐색 규칙을 주입하면
```
test_helloworld                  ... ok      ← 이름을 바꿔서 안 걸린다
test_helloworld_jar_under_p_prefix ... ok    ← P/ 로 옮겨서 안 걸린다
test_helloworld_unmodified_archive ... FAILED  ★혼자 red
```
⇒ ★**고유 검출력이 있다.** ★**단 좁다** — 내용 기반 탐색 아래에서는 다른 둘이 green 인 한 대개 green 이고,
혼자 값을 하는 경우는 «건드리지 않은 경로에만 생기는 회귀»뿐이다. **그 한계를 파일 머리에 적었다.**

### ⒟ 표적 1 — 되살렸다(번호 체계를 «먼저» 확인하고)

`wipic_svc_0x581_maps_and_table_stays_fail_closed` 를 **옛 본문 그대로**(`4e37e9e4`) 되살렸다.
★**원문이 경고한 그 확인을 실제로 했다**(형제 케이스 `misc_unk9` 는 대상 함수가 사라져 «적용 불가» 였다):

| 확인 | HEAD 실측 |
|---|---|
| `Unk16 = 0x581` 선언 | **있다**(`:229`) |
| `0x581 => Self::Unk16` in `try_from` | **있다**(`:335`) |
| `0x582` 가 어딘가 매핑되는가 | ★**0건** ⇒ Err 경로 단언 유효 |
| `From<WIPICSvcId> for u32` | **있다** ⇒ `u32::from(id)` 성립 |

⇒ ★**번호 체계는 바뀌지 않았고 본문은 한 글자도 안 고쳤다.**

### ⒠ 무엇을 바꿨는가 — 수로

**2파일 · +102/-0**(제품 코드 **0줄** — 전부 시험과 주석이다):

| 파일 | 행 | 무엇 |
|---|---|---|
| `wie-lgt/src/runtime/svc_ids.rs` | **+34/-0** | 시험 1건 복원(그 파일의 시험 **0 → 1**) |
| `wie-lgt/tests/test_helloworld.rs` | **+68/-0** | 시험 1건 신설(**2 → 3**) + ★파일 머리에 «오탐 정정» 기록 |

★**파일 머리 기록이 부수물이 아니다** — 그것이 없으면 ★**다음 감사가 같은 이름을 다시 «결손»으로 올린다.**

### ⒡ 양방향 개악 — ★제품 호출부, **3축**

```
A1 «맵된다» 절반     원형 21 passed → try_from 에서 0x581 arm 제거      → ★FAILED 1 → 원복 21 passed
A2 «fail-closed» 절반 원형 21 passed → catch-all 이 Unk16 을 돌려주게    → ★FAILED 1 → 원복 21 passed
B  baseline 고유성    원형 3 passed  → AID 이름 jar 를 건너뛰는 탐색 규칙 → ★새 시험만 FAILED → 원복 3 passed
```
★**A1·A2 를 «따로» 잰 이유**: 그 시험은 두 성질을 단언하고, 하나만 재면 다른 하나가 사문이 된다.
★**B 는 «red 가 나는가»가 아니라 «혼자 red 가 나는가»를 잰다** — 같이 red 면 고유 검출력이 0이다.

### ⒢ 대가 — 「잃는 것이 없다」가 아니다

1. ★**`test_helloworld_unmodified_archive` 는 검출력이 좁다** — 내용 기반 탐색에서는 다른 둘과 함께 green/red 인
   경우가 대부분이다. ⒞의 개악처럼 «건드리지 않은 경로»를 겨냥한 회귀에서만 혼자 선다. ⇒ **시험 1개만큼 느려진다**
   (LGT 게스트를 한 번 더 부팅한다 — 이 파일이 3회 부팅으로 늘었다).
2. ★**감사의 «10건»이라는 수가 흔들린다** — 최소 1건이 오탐이다. ★**나머지 9건을 재검하지 않았다**
   ⇒ 그 수를 인용하는 문서는 지금 낡았을 수 있다(후속으로 남겼다).
3. ★**복원한 svc_ids 시험은 «옛 본문 그대로»다** — 오늘 관점에서 더 나은 단언(예: 표 전체의 왕복)을
   쓰지 않았다. 복원의 목적은 «그때 지켜지던 것을 다시 지키는 것»이라 그렇게 했지만, **더 나은 시험을 안 쓴 것도 사실**이다.
4. ★**표적이 LGT 두 곳뿐이다** — 감사가 센 10건 중 **8건(canvas)** 은 손대지 않았다(그것은 `#p0` 소관이고
   원문이 「먼저 재서 정하라」고 한 자리다).
5. ★**제품 코드 0줄** ⇒ 게임은 하나도 더 통과하지 않는다. 이 회차가 사는 것은 **회귀 방지선**뿐이다.

### ⒣ 게이트

`evidence/gates.txt` 참조(rc + 소요 초). ★제품 코드가 0줄이라 게이트는 **이 diff 를 직접 겨누지 않는다** —
이 회차의 실제 검증은 위 ⒡ 의 개악 3축이고, 그중 A1·A2·B 는 **제품 코드를 고쳐서** 쟀다.

### 범위 판정 — 제안자 추정 `S` ↔ ★내 실측도 `S`

한 회차에 들어갔다. ★**좁히지 않았다** — 원문이 지목한 2건을 **둘 다 처분했다**(하나는 복원, 하나는
«오탐»으로 판정하고 그 자리의 진짜 결손을 대신 채웠다). **넓히지도 않았다** — canvas 8건은 `#p0` 소관.
★**반쪽 착지 위험 없음**: 두 단언이 각각 독립으로 값을 한다.

### 범위·경계

제품 코드 **0줄**(개악은 주입 후 전부 원복 · `git status --porcelain` 커밋 전 2파일만) ·
`game_lab/` **무접촉** · `.gitignore`·`STATE.md`·`AGENTS.md` **무접촉** · 새 검사기·새 워크플로 **0** ·
게임 파일명 유입 **0**.
