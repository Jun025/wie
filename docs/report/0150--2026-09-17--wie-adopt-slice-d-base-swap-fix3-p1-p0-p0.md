## [2026-09-17] 「본문에만 사는 human-step 이 몇 건인가」 — «모른다»에서 **199건**으로 바꿨다(정밀도 53%) (wie-adopt-slice-d-base-swap-fix3-p1-p0-p0)

**무엇을**: 채택 제안 `2026-09-17-adopt-slice-d-base-swap-fix3-p1-p0#p0` 의 이행.
★**세는 것이 산출물인 조사 회차**이고 계약이 「**발견분을 고치지 마라**」로 못박았다 ⇒ **코드 0줄 · 카드 생성 0장**.
★**산출물의 정본은 이 파일이 아니라 회신**이다 — `~/orchestrator/reports/wie-adopt-slice-d-base-swap-fix3-p1-p0-p0.done.md`
(199건 전수 목록·술어 전문·표본 검증표가 거기 있다). ★**여기에 그 목록을 복사하지 않는다** — 소관이 `~/orchestrator` 라 두 벌이 되면 한쪽이 낡는다.

**사용자 영향**: 없다(제품 0줄). 바뀐 것은 **다음 발권의 입력이 생겼다**는 것이다.

### 답 — 수
`~/orchestrator/reports/*.done.md` **7,198건** 중
「human-step 을 **선언**했는데 `humansteps/` **카드가 없다**」 = ★**199건**.
표본 15건 손 검증 정밀도 **8/15 = 53%** ⇒ **실 선언 ≈ 105건**(점추정).

### ★술어를 «두 번» 고쳤고, 그 과정이 이 회차의 실익이다
제안이 제시한 형태 그대로의 초판(V1)은 **576건 · 정밀도 2/15 = 13%** 였다.
표본 검증이 **결함 둘**을 잡았다:

| 결함 | 증상 | 예 |
|---|---|---|
| ⒜ **`★` 를 안 벗겼다** | `★**없다.**` 가 «항목»으로 셌다 | `riffle-…-fix2` · `qts-de-eu-benchmark…` |
| ⒝ **콜론 뒤 부정형을 안 봤다** | `human-step: none` / `human-step: 없음` 이 히트 | `bridge-jwt-view` · `qts-p3-validators` 외 6건 |

고친 V2 = **199건 · 53%**. ★**같은 표본 크기에서 정밀도가 4배**가 됐고, ★**그것을 잡은 것은 술어를 노려본 것이 아니라 «표본을 손으로 읽은 것»이다**(계약 2 가 요구한 그 검증).

### ★대조군이 실제로 값을 했다 — 술어가 좁다는 것을 **그것이** 잡았다
Acceptance 는 「이미 화면에 **뜨는** human-step 이 술어에 걸리는지 확인하라 — 걸려야 한다」였다.
제안의 **바로 그 건**(`wie-main-branch-protection`)은 ★**`## human-step` 절이 없고 §C 산문**으로 선언한다
⇒ 절만 보는 V1 은 그것을 ★**놓쳤다**. V2 의 산문 축이 잡는다.
★**대조군이 없었으면 «절 기반 341건»을 답으로 냈을 것이고, 그 답은 제안이 든 사례 자신을 포함하지 못한다.**

### ★★반증축에서 더 큰 것이 나왔다 — 도구는 **이미 있고 매일 돈다**
| 실측 | 값 |
|---|---|
| `bin/humanstep-harvest` | **실재**(46,751B) · 머리주석이 「미등재·이월·파싱 실패를 **분리해서** 낸다」고 선언 |
| 주기 호출자 | **있다** — launchd `com.jun.cc.humanstep-nudge`(09:10)가 `bin/humanstep-nudge` → `humanstep-harvest` 호출 |
| ★그 호출의 **모드** | **`--carry-json`** — 그 분기는 `carry` 와 `resolved` **만** 출력한다(`:521-540`) |
| ⇒ | ★**미등재 집합은 스케줄 경로에 «아예 없다»** — 이 조사가 한 번도 생산된 적 없는 **구조적** 이유 |
| `bin/healthcheck`·`bin/dispatcher` 의 언급 | **전건 주석**(`_cut100` 선례 인용) — 호출 아님 |
| 직접 실행 | `--report` 가 ★**10분에 미완**(SIGTERM · 7,198건 bash 루프) — 「있는데 안 쓰인다」의 둘째 이유(★1회 실측이라 임계로 쓰지 마라) |

### 남긴 한계 — 숨기지 않는다
⑴★**199 는 «선언»의 수이지 «미해소»의 수가 아니다.** 표본에도 이미 해소된 건이 있다
(`main` branch-protection 은 2026-09-17 적용됨). 미해소만 고르려면 **건별 사람 판단**이 필요하고,
★**제안 원문이 그것을 «이 조사의 실제 비용»으로 미리 적었다** — 이 회차는 그 비용을 치르지 **않았다**.
⑵★정밀도 53% 는 **표본 15건** 기준(모비율 95% CI 대략 27~79%) ⇒ 「105건」을 **점추정 이상으로 읽지 마라**.
⑶★**AMBIG 931건은 세지 않았다** — 「없음, **단** …」 형태라 사람이 읽어야 갈린다.
★**이것이 다음으로 큰 미측정 구역이고 199 보다 크다.**
⑷남은 오탐 3형태: ⒜절 제목 꼬리 오독(`## human-step / 후속`) ⒝「이 회차에는 없음, 단 후속의 전제가 human-step」 ⒞「새로 만든 human-step = 0」.

★**그러므로 이 목록은 «자동 카드 생성»에 쓰면 안 되고 «사람이 훑을 목록»으로만 써라** — 제안 자신의 경고 그대로다.
