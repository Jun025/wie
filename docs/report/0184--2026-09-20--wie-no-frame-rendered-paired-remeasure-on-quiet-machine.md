## [2026-09-20] 저부하 재측을 «하지 않았다» — 그 창이 이 기계에 **없다**는 것이 이 회차의 산출이다 (wie-no-frame-rendered-paired-remeasure-on-quiet-machine)

인용 트리: `origin/main` = `716e9319` · 생성기 최종커밋 `3b73c63c` · 러너 `game-lab-recensus.sh`(PR #209 착지분) ·
검증기 바이너리 `target/debug/wie_validate` mtime **2026-09-19 13:11**. 조회 2026-09-19T14:09Z ~ 2026-09-20T00:0xZ.

### ⒜ 대상 집합 — ★「32」는 **뽑을 수 없다**. 내 계수는 **1**이다

디스크에 존재하는 유일한 reports 입력은 **7월 기준선**(`game_lab/reports/` · 452건 · mtime 2026-06-25..07-01)이다.
그 입력에서 «화면이 안 그려진» 계열을 실측하면:

| 이유(첫 줄) | 건수 |
|---|---|
| `only blank/uniform frames (black screen)` | 11 |
| `hang: SIGKILLed after 50s (infinite tick loop)` | 6 |
| ★`no frame rendered (hang/black screen)` | **1** |

★**32 는 2026-09-18 인구조사의 «오늘» 열이고, 그 per-game 행은 `mktemp` 스크래치와 함께 지워졌다.**
이것은 내 추정이 아니라 **이 저장소가 이미 적어 둔 사실**이다 —
`docs/worklog/2026-09-18-census-per-game-mapping-generator.json`: 「오늘의 no frame rendered 32건은 **어떤 매핑으로도 못 뽑는다**(7월 입력에선 1건)」.
⇒ ★**채택 제안의 전제(「이제 한 줄로 부분 재측이 된다」)는 그 32 에 대해서는 성립하지 않는다.**
`--bucket`/`--from-stdin` 배관은 **작동한다**(확인했다) — 없는 것은 배관이 아니라 **«그 32를 가리키는 입력»**이다.

⇒ 그래서 모집단을 **7월 기준선의 블랭크 계열 18건**(ktf 9 · lgt 7 · skt 2 · 전건 실재 확인)으로 잡았다.
★**7월은 «선택자»로만 쓰고 «한 팔»로 쓰지 않는다** — 오늘 엔진은 7월보다 147 커밋 앞서 있어,
7월↔오늘 비교는 «부하»와 «엔진 변화»를 섞는다. 두 팔은 **둘 다 오늘**이어야 한다.

### ⒝ 한산 술어 — ★**측정 전에 수로 선언했고, 열리지 않았다**

사전 등록(`00-preregistration.md` · **2026-09-19T14:12:33Z** 작성 = 측정 개시 «전»):
`load1 ≤ 40` ∧ `idle ≥ 80%` ∧ `sys ≤ 15%` ∧ `plv == 1` ∧ **러너 점유 0**. 상한 = 5분 간격 **10회 프로브**.

결과: **10/10 busy.** 어느 프로브도 통과하지 못했다(14:13 → 15:00 UTC · 47분).

★**그리고 「운이 나빴다」가 아니다 — 호스트 베이스라인으로 소급 채점했다**(`~/logs/perf-sample.csv` · **690표본** · 5분 간격 · 2026-09-17T12:43 ~ 2026-09-20T00:03):

| 사전 등록 조건 | 충족 | 비율 |
|---|---|---|
| `load1 ≤ 40` | 55/690 | 8.0% |
| ★`idle ≥ 80%` | **0/690** | **0.0%** |
| ★`sys ≤ 15%` | 1/690 | 0.1% |
| `plv == 1` | 434/690 | 62.9% |
| ★**세 CPU/부하 조건 동시** | **0/690** | **0.0%** |

분포: `idle` p50 **26.3%** · p90 50.2% · ★**max 58.9%**(2.5일 동안 80%를 **한 번도** 넘지 않았다) ·
`sys` min **14.7%** · `load1` p50 **136.1**.
시간대별 평균 `idle` 최고는 01시 **30.5%** · 22시 30.4% · 03시 30.3% ⇒ ★**«조용한 시간»이 없다**(최고와 최저가 3%p 차).

⇒ ★★**사전 등록 술어는 이 기계에서 «드문» 것이 아니라 «관측된 적 없는» 것이다.**
제안이 스스로 적은 tradeoff(「한산한 창이 드물다」)는 **낙관이었다** — 실측은 «드물다»가 아니라 «0» 이다.

### ⒞ 짝짓기 설계 — 선언했고, **돌리지 않았다**

`AGENTS.md` §Definition of Done 의 keydraw 선례를 인용해 선언했다 —
「the two fixtures **alternating inside one loop**, so both see **the same load minute**」.
★**사전 등록 «수정 1»(14:33:46Z · 데이터 0건 상태)**: 초판이 적은 「정순/역순 2회 순회」는 한 쌍을 **수십 분** 떼어놓아
그 선례가 이름 붙인 unpaired trap 그대로였다 ⇒ **게임별 두 팔을 연속(back-to-back)** 으로 바꿨다.
표본 = 18 × rep 2. 집행 스크립트까지 준비했고(`run-paired.sh`), ★**창이 안 열려 실행하지 않았다.**
★미리 적어 둔 한계: 두 팔이 같은 처치의 반복이라 **2회차가 캐시 이득을 본다** — 이 설계는 그것을 제거하지 못하므로
일치가 나와도 「완전히 안정」으로 읽지 않기로 했었다.

### ⒟ 판정 — ★**⒜도 ⒝도 아니다. 「측정하지 않았다」이다**

착수 전에 두 갈래를 못박아 두었다: ⒜재현 ⇒ 진짜 결함(모집단 유지) · ⒝불일치 ⇒ **«부하 인공물»이 아니라 «판정 불가»**.
★**이 회차는 그 어느 쪽도 주장하지 않는다** — 표본이 **0**이다. 모집단은 **줄이지 않았다**(Non-goal 준수).
★**그리고 ⒝를 「좋은 소식」으로 쓰지 않는다**는 규율은 여기서도 유효하다: 「못 쟀다」를 「괜찮다」로 읽지 마라.

※다만 «이미 알려진 것»은 그대로 선다 — `game-lab-recensus.sh` 머리주석이 적은 대로,
loadavg 170~202 에서 같은 3게임이 **UNCLASSIFIED×3 → only-blank / panic-unwrap / no-frame** 으로 갈렸고,
2026-09-18 회차의 게이트② 검수도 독립 표본에서 **고부하 2/2 FAIL ↔ 저부하 3/3 PASS** 를 재현했다.
★**즉 「부하가 버킷을 움직인다」는 이미 관측돼 있고, 이 회차가 답하려던 것은 «그래서 그 32가 무엇인가» 였다.**

### ⒠ 재개 조건 — ★수로

이 실험을 이 기계에서 되살리려면 **둘 중 하나**가 필요하다:
1. ★**드레인 창에 얹어라** — 이 맥이 관측상 조용해지는 유일한 상태는 `~/orchestrator-live/bin/uptime-guard` 의
   계획 재부팅 드레인(전 estate `running 0`)이다. 그 창은 **예고되고**, 러너 점유 0 이 **보장된다**.
   ⇒ 조건: 「`uptime-guard` 가 `UPTIME_RESTART_READY` 를 낸 시점 ~ 재부팅 사이」에 18×2 쌍(예상 **25~35분**)을 돌린다.
   ★**단 2026-09-02 드레인 직후 실측 idle 이 38%** 였다 ⇒ **`idle ≥ 80%` 는 그때도 못 선다.** 그래서 2 가 필요하다.
2. ★**술어를 «다시 선언»하라 — 완화가 아니라 재정의다.** 베이스라인이 주는 수로 고른다면
   `load1 ≤ 45`(p10) ∧ `idle ≥ 50%`(p90) ∧ `sys ≤ 23%`(p10) ∧ `plv == 1` ∧ 러너 0 —
   ★**이 조합의 실현율도 «먼저» 채점하고 나서** 돌려야 한다(이 회차가 0.0% 를 놓친 방식으로 다시 놓치지 않으려면).
   ★**그리고 그 수로 잰 결과는 «저부하»가 아니라 «중부하»의 답이라고 적어야 한다.**

⇒ **지금 이 저장소가 할 수 있는 정직한 선택은 셋이다**: ⑴다른 기계에서 잰다 ⑵드레인 창 + 재정의된 술어로 잰다
⑶★**버킷 자체를 부하에 둔감하게 만든다**(아래 제안) — 그러면 「조용한 창」이 애초에 필요 없어진다.

### ⒡ 부수 관측 — 형제 PR 이 **스택**돼 있다(순서 위험)

측정 준비 중 실측: **PR #219** (`wie-census-generator-input-precedence-and-signature-lookup`)의 base 는 `main` 인데
**내 PR #218 의 커밋 `6b0de1bc` 를 조상으로 품는다**(`git merge-base --is-ancestor` 확인) — 즉 스택 PR 이고,
`docs/report/0182` 는 **#218 의 파일이 #219 에도 들어 있는 것**이다(같은 내용 · 진짜 연번 충돌 아님).
⇒ ★**#219 가 먼저 착지하면 #218 의 diff 가 비어 게이트③이 「이미 착지」 형상을 만난다.** 착지 순서는 **#218 → #219** 가 맞다.
이 회차의 산출은 아니고, **보고만 한다**.
