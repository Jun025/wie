## [2026-09-18] `STATE.md` §완료 의 «삽입 지점»을 없앴다 — 착지가 이제 `STATE.md` 를 만지지 않는다 (wie-remove-state-md-completed-insertion-point)

**무엇을**: `§완료 (최근)` 의 항목 **149건 · 1,940줄**을 걷어내고 `docs/report/` 를 가리키는 **고정 안내**로 바꿨다
(2026-09-08 에 `§진행중` 이 받은 것과 **같은 처방**). 부수로 `AGENTS.md` §Landing paperwork 의 지시와
그 절의 «반대 결정»에 SUPERSEDED 배너, 그리고 **유일한 코드 파일 소비자**의 `STATE.md:<line>` 인용을 고쳤다.
★제품 코드 **0줄**(고친 `.rs` 는 시험 파일의 **doc 주석**) · 새 검사기·잡 **0** · 열린 PR **무접촉**.

## 왜 — 이것이 열린 PR 을 묶어 둔 «단일» 원인이었다
`§진행중` 을 고친 뒤에도 남은 절반이 이 절이었다. 회차마다 **맨 위 한 자리**에 항목을 끼워 넣으므로
형제 회차가 전부 같은 지점을 다투고, ★**착지 1회가 열린 PR 전건의 충돌로 증폭**된다.
★**«맨 아래 추가»로도 안 고쳐진다** — `§진행중` 절이 이미 실측으로 적었듯 **«지점이 하나»인 것**이 문제다
(0줄 간격 → CONFLICT · 1줄 → clean). ⇒ 파일을 바꾸는 것이 아니라 **지점을 없애는** 것이 처방이고,
`docs/report/NNNN--…` 는 **회차마다 새 파일**이라 공유 지점이 구조적으로 없다.

## ★기각 사유가 사라졌음을 «내가 다시» 쟀다 — 149/149
이 처방은 2026-09-08 에 **기각**됐었다(`AGENTS.md`). 사유는 하나 — 「§완료 에만 있는 항목이 있어 포인터화하면 소실된다」.
```
§완료 항목 149 · docs/report 사본 확인 149 · ★사본 없음 0
```
술어: 항목 블록에서 백틱 토큰을 뽑아 `~/orchestrator/tasks/<id>.md` 로 존재하는 것만 티켓 id 로 인정 →
`docs/report/NNNN--DATE--<id>.md` 매칭. ★**id 가 없는 옛 3줄**(`.direnv` PR #51 · KB 경로 PR #46 · 2026-07-22 4PR 번들)은
제목을 프로브로 써서 **본문 인용**으로 찾았다 — 각각 `0092`·`0094`·`0159` 가 **원문을 그대로 싣고 있다**.
⇒ ★**포인터화로 잃는 고유 내용 0.** (2026-09-08 이 「7건이 여기만 있다」로 기각했고, PR #199 가 백필로 3 → 0 을 만들었다.)

## ★성공 정의 — ★**티켓이 시킨 «착지 직후 rc=0 계수»는 이 처방을 «반대로» 판정한다. 그대로 적는다.**
| | rc=0 | rc=1 |
|---|---:|---:|
| 착지 **전**(`origin/main` `c30f8632`) | **3** / 6 (`#195`·`#201`·`#202`) | 3 (`#162`·`#180`·`#190` · ★충돌 파일 전건 `STATE.md` 하나) |
| 착지 **직후**(이 PR 을 병합한 트리) | ★**1** / 6 (`#202` 만) | 5 |

⇒ ★★**3 → 1 로 «줄었다».** 티켓 Acceptance 는 「한 건도 안 늘면 처방이 ⓒ 를 못 맞춘 것」이라 적었지만,
★**그 자로는 이 마이그레이션의 «어떤» 판본도 통과할 수 없다** — §완료 를 없애는 일은 그 절에 줄을 더한 **모든** PR 과
원리적으로 충돌하기 때문이다. 같은 티켓의 계약 2⒝ 가 그것을 **이미 예고**했다(「이 PR 자신이 열린 PR 전건을 한 번 더 무효화한다」).
⇒ ★**두 문장이 서로 어긋나고, 옳은 자는 «전향(forward) 재발률»이다** — 아래 ⒜ 가 그 자다.
★**그리고 그 비용은 «크지 않다»**: 5건 중 3건은 **이 PR 이 없어도 이미 rc=1** 이었고, 새로 깨지는 것은 **2건(`#195`·`#201`)** 이며
해소는 **base 당김 1회**(§완료 를 만지던 회차는 그 항목을 `docs/report/` 로 옮기면 된다).

## ★★양방향 ⒜ — 본체: «한 번 푸는 것»이 아니라 «다시 안 깨지는 것»
동시 회차 둘(A·B)을 **로컬로 합성**해(원격 무접촉) *A 가 착지한 뒤 B 가 살아남는가*를 두 규약에서 각각 쟀다.
A·B 는 각각 **전형적인 한 회차**다 — 새 규약: `docs/report/` 새 파일 + `docs/worklog/` 새 파일 · 옛 규약: `§완료` 맨 위 항목 삽입.
```
★신 규약(이 PR 착지 후 main)          ★대조군 — 옛 규약(포인터화 «이전» main c30f8632)
  main1 × A            rc=0             old × A2            rc=0
  main1 × B            rc=0             old × B2            rc=0
★ A 착지 후 main × B   rc=0             ★ A2 착지 후 old × B2  rc=1
```
⇒ ★★**같은 실험에서 옛 규약은 «1/1 로 깨지고» 새 규약은 «0/1 로 안 깨진다».**
`AGENTS.md` 가 2026-09-08 에 기록한 **전향 재발률 1/1**(「임의의 동시 회차 둘은 여기서 충돌한다」)이 **0** 이 됐다.
★**깨뜨리던 것은 「착지」가 아니라 「§완료 를 만지는 착지」였고, 그 경로를 없앴다.**
※부수 확인: 이 PR 병합본 위에 새 규약 착지를 1건 더 얹어도 열린 PR 6건의 rc 가 **한 건도 움직이지 않았다**(`STATE.md` 접촉 0).

## ⒝ 포인터를 따라가면 실제로 나오는가 — 소실 0
```sh
grep -H '^## \[' docs/report/*.md | sort -r     # ← 「무엇이 착지했나」의 정본(연번 내림차순)
```
`-H` 가 없으면 정렬키가 «제목 속 날짜»가 되어 어긋난다(`AGENTS.md` 가 그 실측을 적는다) — 그래서 안내에 `-H` 를 박았다.

## 대가 — 「잃는 것이 없다」가 아니다
⑴★**한 홉 멀어진다** — `STATE.md` 한 파일만 열면 보이던 «최근 착지 요약»이 이제 `docs/report/` 를 읽어야 보인다.
오프라인에서도 읽히지만 **한 번에 한 파일**이고, 「최근 5건을 훑는다」가 한 줄 명령 + 파일 열기로 바뀐다.
⑵**정렬이 «날짜»가 아니라 «연번»** 이다 — 대체로 일치하나 **보장이 아니다**(연번은 claim 순서라 날짜와 어긋날 수 있다).
⑶★**이 PR 자신이 열린 PR 전건을 한 번 더 무효화한다** — ★**비용 비교가 이 회차의 논거다**:
   **지금 한 번(3건 재해소)** ↔ **안 하면 착지마다 계속**(오늘만 해도 `#199`→`#196`→`#197` 세 착지가 형제를 연쇄로 깼다).
⑷**`STATE.md` 가 «현재 상태»만 남는다** — 이력이 사라진 것이 아니라 **다른 파일로 옮겨졌다**. 그 차이를 모르는 사람은
「이력이 지워졌다」로 읽을 수 있어, 고정 안내가 **되돌리는 법**까지 함께 적는다.

## 소비자 — 기계 0, 그러나 «코드 파일 1건»은 고쳤다
추적 코드·설정 전수에서 `## 완료` **파서 0** · `STATE.md` 언급 **1**
(`wie_midp/tests/create_image_missing_name_message.rs` 의 **doc 주석**이 `STATE.md:349` 를 인용).
★그 인용은 **이미 썩어 있었고**(그 줄은 무관한 항목 · 뜻한 내용은 510행으로 이동 — `AGENTS.md` 기록),
이 회차 이후엔 **가리킬 절 자체가 없다** ⇒ `docs/report/0047--…` 로 **repoint** 했다.
★**덤으로 하나 정정**: 그 주석은 세 곳을 «독립 기록 셋»으로 열거했는데 `STATE.md:349` 와 `0047` 은
★**같은 회차의 원장 사본 ↔ 회차 파일**이었다 ⇒ 「three times」를 **둘**로 고쳤다(수를 줄인 것이 아니라 **중복을 지운 것**).

## 범위
`STATE.md` §완료 1절 · `AGENTS.md`(지시 1줄 + SUPERSEDED 배너 + 줄인용 규칙) · 그 doc 주석 · 이 파일 + worklog.
★**백필 재실행 0**(#199 가 했다 · 나는 0 을 «확인»만 했다) · ★**열린 PR 무접촉** · 제품 코드 **0줄** ·
새 검사기·launchd **0** · ★`check-audit-warnings` 의 **선재 red 는 손대지 않았다**(별 티켓).
