## [2026-09-20] 연번 가드와 스키마 검사기 «사이»의 빈칸을 메웠다 — ★기존 검사기에 얹었고, 오탐이 실재해 «경고»로 두었다 (wie-serial-guard-and-worklog-json-leave-a-gap-p2)

채택 제안 `2026-09-20-price-cargo-audit-at-pr-stage#p2` 의 이행.
결함의 형태: 가드 A 는 «파일명이 겹치나», 가드 B 는 «JSON 이 스키마에 맞나»를 본다 ⇒ ★**«그 JSON 이 가리키는 번호가 이 PR 의 보고서인가»를 아무도 보지 않는다.**

### ⒝ 빈칸이 «실재»하는가 — ★내 출력으로 보였다

`origin/main` 의 두 검사기를 그대로 두고, 개악 상태를 만들었다: `docs/report/0200--…md` 를 더하고 같은 PR 의 worklog `changes[0]` 이 **`docs/report/0184`**(어디에도 없는 번호)를 가리키게 했다.

```
--- check-docs-report-serial (수정 전) ---  OK — 중복 연번 0 · 내가 더한 1건 ↔ 열린 PR claim 충돌 0   rc=0
--- check-worklog-json       (수정 전) ---  OK (160 file(s) in docs/worklog/)                      rc=0
```

⇒ ★**둘 다 rc=0.** 제안의 전제는 참이다(둘 중 하나라도 잡았으면 여기서 닫았을 것이다).

### ⒞ live 에 «지금» 몇 건인가 — ★1건이고, 그 1건이 오탐이다

전수(`docs/worklog/*.json` **159** · 그중 report 를 인용하는 **85** · 인용 출현 **123**):

| | 값 |
|---|---|
| 존재하지 않는 연번을 가리키는 인용 | ★**1개 연번**(2회 출현) |
| 그 자리 | `2026-09-20-price-cargo-audit-at-pr-stage.json` 의 ★**`proposals[2].why` · `proposals[2].tradeoff`** |

★**그 두 곳은 «정당하다»** — 제안이 결함을 **서술하려고** 사라진 번호를 인용한 것이다.
★★**그리고 그것은 제안 자신이 미리 적은 자기지시 함정 그대로다**: 「이 제안 자신의 `why` 가 결함을 «서술하려고» `docs/report/0184` 를 인용한다 ⇒ 위 술어를 그대로 구현하면 첫 대상이 이 파일이다. **예외를 두든 술어를 좁히든, 그 한 줄을 먼저 정해야 한다**」.
⇒ ★**이 회차가 정한 그 한 줄 = 「`proposals[]` 는 대상이 아니다」**(아래).

★그리고 이 수가 판단의 근거다: ★**진탐 1**(2026-09-20 에 실제로 난 `changes[0]` — 사람이 잡았다) · ★**오탐 1**. 표본이 그것뿐이다.

### ⒟ 형제 repo 에 답이 있나 — ★없다(포팅할 것이 없다)

`otterpebble`/`scripts/docs-report-serial-check.mjs`(140줄)는 wie 가 **이미 포팅한 그 축**(파일명 유일성)이고,
★**`worklog` 문자열이 0건**이다. 형제 리니지(`allocation-races` · `disk-only-answer-on-timeout` · `guidance-still-points-at-disk-only-recipe`)도 **할당·조회·타임아웃** 축이다.
⇒ ★**이 빈칸을 다룬 선례는 형제에도 없다.** 발명이 아니라 «없어서 만든 것»이고, 그 사실을 적는다.

### 계약 1 — «어느 축에 얹을지»와 그 이유 (★측정으로 골랐다)

★**`check-docs-report-serial.mjs` 에 얹었다.** 새 스크립트·새 CI 잡 **0**.

| | `check-docs-report-serial.mjs` | `check-worklog-json.mjs` |
|---|---|---|
| `child_process` 사용 | ★**4곳**(이미 git 을 읽는다) | ★**0** |
| 「이 브랜치가 더한 report」 집합 | ★**`myAddedNames()` 가 이미 만든다** | 없다 |
| 연번 정규식·`REPORT_DIR` | 이미 있다 | 없다 |
| 배선 | 이미 `contract`(상시 실행) | 이미 `contract` |

제안이 「둘 다 현행 책임 경계를 넘는다」고 적은 것은 맞다. ⇒ ★**«덜 넘는 쪽»을 수로 골랐다**: 스키마 검사기에 얹으면 **git 을 0에서 도입**해야 하고, 이쪽은 **이미 있는 git·이미 있는 집합**에 `JSON.parse` 하나를 더한다.

### 계약 3 — 오탐을 재고 «경고»로 두었다

★**rc 를 올리지 않는다.** 티켓이 「오탐이 하나라도 있으면 rc 를 올리지 말고 경고로 두어라」 했고, ⒞ 가 **오탐 1건을 실측**했다.
★그 위에 **구조적 좁힘**을 하나 더했다 — **`proposals[]` 가지는 읽지 않는다**:

- 근거: 제안은 «다른 회차·앞으로 할 일»을 이야기하는 자리라 **남의(때로는 사라진) 번호를 정당하게** 가리킨다.
- ★**실측이 그 모양을 지지한다**: 살아 있는 유일한 오탐 **2회가 전부** `proposals[]` 안이었다.
- ★**그 가지만 걷어도 «진짜» 결함은 남는다** — 2026-09-20 의 진탐은 `changes[0]` 에 있었다.

★**그래도 rc 를 안 올린 이유**: 제외를 넣으면 «오늘 아는» 오탐은 0 이 되지만, **표본이 1건**이다. 이 저장소가 반복해 규탄한 「표본 1건에 굳히면 오탐이 굳는다」를 그대로 받는다.
⇒ ★**값은 «막는 것»이 아니라 «사람의 눈보다 먼저 화면에 올리는 것»** 이다. 2026-09-20 의 진탐을 잡은 것은 게이트② 검수자의 눈이었다.

### 계약 2 — 개악 양방향 ★넷을 «돌려서»

| | 무엇을 했나 | 결과 |
|---|---|---|
| ⒜ | worklog 가 **어디에도 없는** `0184` 를 가리킴 | ★**`::warning` 출력** · `worklog 인용 1건 중 «없는 연번» 1건(경고)` · rc=0(설계) |
| ⒝ | 같은 파일이 **내가 더한** `0200` 을 가리킴 | **green** · `인용 1건 전건 실재` |
| ⒝′ | **존재하는 남의** 번호(`0109`·`0185`) 교차인용 | **green** · `인용 3건 전건 실재` |
| ⒞ | 같은 `0184` 가 **`proposals[]` 안**에 있음 | **green** — ★오탐 모양이 통과한다 |

그리고 그 판별력을 **저장소 안에 고정**했다 — `--selftest` 에 **8케이스 추가**(전 **27케이스 전건 일치**), 그중:
- ★**「본체가 `::warning` 을 내고 exit 를 올리지 «않는다»」를 호출부 슬라이스로 단언** — 나중에 누가 red 로 올리면 그 축이 운다.
- ★**「실 저장소에서 제외를 걷어 내면 오탐 1건이 다시 보인다」** — 제외가 **일을 한다**는 것을 공허하지 않게 고정했다(`counted === 0 && exempt === 1`).

### ★형제 단언 하나를 «고쳤다» — 숨기지 않는다

기존 selftest `★★성공 줄이 «미대조» 를 «충돌 0» 으로 적지 않는다` 가 성공 줄의 **끝**을 `${crossNote}`);` 로 고정하고 있었다. 새 축이 그 뒤에 `${citeNote}` 를 붙이자 ★**옳은 추가가 red** 가 됐다.
⇒ 「끝 고정」을 ★**「성공 줄 «안»에 `${crossNote}` 가 있는가」**로 풀었다. **지키려던 뜻은 그대로**이고, 이유를 그 자리에 주석으로 적었다. ★이 저장소가 `check-parity-lock-wired`(2026-09-06)에서 치른 값과 같은 형태다.

### 왜 지금인가 · 대가 (각 한 줄)

- **왜 지금인가**: ★**가설이 아니라 2026-09-20 에 실제로 났고**, 착지했으면 **원장 항목이 남의 문서를 가리킨 채 영구화**된다. 잡은 것이 사람의 눈뿐이었다는 것이 그 빈칸의 증거다.
- **대가**: ★**발화 빈도가 낮다** — rename 이 일어나는 회차에서만 값한다. 그리고 ★**경고라 무시할 수 있다**(그것이 오탐 1건과 맞바꾼 값이다). 실행 비용은 `JSON.parse` × 이 PR 의 worklog 수 ≈ **ms**.

### 한계 — 숨기지 않는다

1. ★**경고다. 막지 않는다.** 아무도 안 보면 값이 0 이다 — 이 저장소가 `check-worklog-coverage`(2026-09-07)에서 본 형태다. rc 로 올리는 것은 **표본이 더 쌓인 뒤**의 결정이다.
2. ★**`proposals[]` 제외는 «구조적»이라 그 안의 «진짜» 오타를 놓친다** — 제안이 남의 번호를 잘못 적어도 조용하다. 오탐 1건과 맞바꾼 값이고, 되돌리려면 표본이 필요하다.
3. ★**`docs/report/NNNN` 꼴 인용만 본다.** 「보고서 0184」처럼 경로 없이 쓰면 안 보인다 — 넓히면 임의의 네 자리 수를 물게 된다.
4. ★**«존재하는가»만 본다. «맞는 보고서인가»는 못 본다** — 내가 `0109` 를 인용하면 그 파일이 정말 그 내용인지는 사람이 본다.
5. ★**연번 «할당» 규칙은 건드리지 않았다**(티켓 경계). 이 회차는 «대조»를 더한 것이다.
