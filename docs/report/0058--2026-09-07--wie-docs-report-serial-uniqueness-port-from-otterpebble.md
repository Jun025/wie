## [2026-09-07] `docs/report` 연번 유일성 축 — «디스크 최대 + 1」의 디스크는 «머지된 트리»다 (wie-docs-report-serial-uniqueness-port-from-otterpebble)

**무엇을**: `scripts/check-docs-report-serial.mjs` 신설(세 모드) + `engine-contract.yml` `contract` 잡에 **스텝 1개** +
`AGENTS.md` 규약 스니펫이 `ls docs/report` 대신 **도구에게 묻도록** 교체. ★**형제 저장소 `otterpebble` 의 착지분
(`otterpebble-docs-report-serial-uniqueness-unguarded` · approve)의 포팅이고, 새 방식을 발명하지 않았다.**

**왜**: 이관이 정한 「연번 = 디스크 최대 + 1」에서 ★**«디스크»는 머지된 트리**다 ⇒ 동시에 열린 두 PR 이 각자
계산하면 **같은 번호**를 잡는데, ★**파일명이 달라 git 충돌도 안 나고 어떤 검사도 울지 않는다.**
★**이관이 파일 «내용»에서 없앤 충돌 형태가 파일 «이름»에서 그대로 재발한다.**

**★오늘 이미 나 있었다(가설 아님)**: `main` 이 `0056--…-ci-wire-or-document-three-never-run-checks`(#109 착지)를
들고 있는데 ★**열린 PR #112 도 `0056--…` 을 추가**한다(형태 ⑵ — 내가 번호를 잡은 뒤 남이 착지). ★**#112 는 이 레인이
직전 회차에 연 PR 이다** — 즉 규약을 따랐는데도 났다. ★그리고 #111 이 `0057` 을 들고 있어 디스크 답(`0057`)도 이미 막혀 있다.
⇒ 도구가 **`0058`** 을 주었고 ★**이 회차 자신이 그 번호를 썼다**(자기 도구를 먼저 먹였다).

**사용자 영향**: 없다(원장·CI 축만 · 엔진·웹 산출물 무접촉).

**검산**: 사후 축 종단 red — 트리에 같은 번호 2건을 실제로 만들자 `::error` **1줄 · rc=1**, 제거 후 rc=0 · 잔재 0.
개악 **M0**(열린 PR 조회 제거 = 포팅 «전» 형상) → `--next-serial` 이 다시 **`0057`** 을 준다(#111 과 충돌) ↔ 원복 **`0058`**.
개악 **M1~M3**(제품 호출부·순수 함수) → selftest 가 각각 **1·1·2건 불일치**로 잡는다. 원복은 **바이트 동일** · selftest **9케이스 전건 일치**.

**★남는 것(고치지 않았다)**: `REPORT.md` §어떻게 쓰나 의 같은 스니펫은 **범위 밖(무접촉)** 이라 옛 형태로 남았다.
그 절이 스스로 「정본 규약 = `AGENTS.md` §Landing paperwork」라 적으므로 정본은 갈리지 않지만, ★**복사할 수 있는
낡은 한 줄이 남는다** — 후속 제안으로 등재했다.
