## [2026-09-13] keydraw 의 paint 수는 «하한»이다 — 그리고 「55 가 낡았다」는 재현되지 않았다 (wie-agents-md-paints-55-is-a-stale-single-value)

- **무엇을**: `AGENTS.md` §Definition of Done 의 keydraw 절(`paints 1` ↔ `paints 55` 대비 문단 바로 뒤)에
  **한 문단**을 더했다 — 판정은 `PASS`·`content true`·rc=0 이지 **수가 아니다** · 1↔수십의 **간극이 신호**다 ·
  55 는 재현되나 **보장은 아니다**(45 도 관측) ⇒ **낮은 수 자체는 회귀가 아니다**.
  ★**기전 = 경합에 의한 틱 기아**(고정 ~20.0s 예산 안에 드는 틱 수 · 아래 「정정」 절의 12·30병렬 실측).
  ★**단일 값을 다른 단일 값으로 갈아치우지 않았다** · **검사기·게이트 축 0** · 코드·픽스처·러너 **무접촉**.

- **★★대전제 ⓐ 가 반증됐다 — 제안(내 직전 회차의 `#p1`)의 전제는 «거짓»이었다.**
  제안 문면은 「`AGENTS.md` 는 55 를 적었고 **2026-09-13 같은 명령이 45 를 낸다**」였다. ★**오늘 다시 재니 55 다.**

  | # | 형상 | paints | 판정 3축 |
  |---|---|---:|---|
  | 1–3 | `keydraw_ktf` 순차 3회 | **55 · 55 · 55** | 전건 PASS · content true · rc=0 |
  | 4–6 | `keydraw_lgt` 순차 3회 | **55 · 55 · 55** | 전건 PASS · content true · rc=0 |
  | 7–9 | `keydraw_lgt` **3병렬** | **55 · 55 · 55** | rc=0 ×3 |
  | 10 | 병렬 직후 단독 대조군 | **55** | PASS · content true |
  | 11 | ★**CPU 버너 20개 / 10코어**(load 17.8 → 26.98) | **55** | PASS · content true |

  ⇒ ★**11/11 이 55 다. 「55 는 낡지 않았다」**(wall time 22~24s = 문서의 20.1~26.1s 범위 안).

- ★★★**[정정 · 게이트② request-changes 승계 `-fix` 회차] 초판이 적은 「CPU 포화로는 그 수가 움직이지 않는다
  ⇒ 기전 미규명」은 «거짓»이었다 — 실험이 «덜 셌다».**
  CPU 버너 20개는 그저 도는 스피너이고, 경합해야 할 상대는 ★**같은 일을 하는 에뮬레이터 여러 개**였다.
  ★**검수자 설계로 다시 돌려 재현했다**(`keydraw_lgt` · 이 `-fix` 회차 직접 실측):

  | 형상 | paints | 판정 3축 |
  |---|---|---|
  | 유휴 단독(회차 시작) | **48** | PASS · content true · rc=0 |
  | ★**12병렬**(n=12) | ★**38 · 39×3 · 40×4 · 41×4** | **12/12** PASS · content true · rc=0 |
  | ★**30병렬**(n=30) | ★**28×3 · 29×3 · 31 · 32×5 · 33×3 · 34×4 · 35×10 · 36** | **30/30** PASS · content true · rc=0 |
  | 부하 해제 후 단독(load 164) | **50** | — |

  ⇒ ★**기전은 «경합에 의한 틱 기아»다** — 예산이 **시간 고정(~20.0s)** 이고 `paints` 는 그 안에 드는 **틱 수**다.
  ★★**그리고 `AGENTS.md` 자신이 이미 그렇게 적고 있었다**(「the spread above 20.0 is tick overrun under load」 ·
  「`--boot-secs 1.0` → paints 55 → 37」) ⇒ ★**초판 문단은 같은 문서의 아래 문단과 «모순»이었다.**
  ⇒ 원 제안의 「부하 의존」은 **옳았고**, 45 는 **이 스윙의 한가운데**다.
  ★**절대값은 회차마다 다르다**(검수자 12병렬 54~55 ↔ 이 회차 38~41) — ★**그 불일치가 곧 논지다**:
  수는 머신 상태에 딸리고, ★**흔들리지 않은 것은 판정 3축뿐**(합 42회 전건 PASS · content true · rc=0).
  ⇒ ★**「보장되지 않는 수가 등식처럼 읽히는 것」**이 결함이라는 판정은 **그대로 유효**하고, 근거가
  «미규명»에서 ★**«측정된 기전»**으로 바뀌었다(더 세졌다).

- **★대가(계약 2) 보존**: 「범위로 적으면 `--inject` 를 빼면 1 · 넣으면 수십이라는 정보가 흐려진다」는 제안의 경고를
  받아, 새 문단이 ★**그 간극을 «신호»로 명시**한다(「what the 1-vs-dozens gap proves is that the flag reached the
  guest — that gap is the signal, and it is enormous」). 앞 문단의 `paints 1` ↔ `paints 55` 원문은 **그대로 둔다**.

- **ⓑ 판정 기준으로 쓰이는 자리 — 없다**: `scripts/`·`.github/`·`wie_cli/tests/` 전수에서 이 수를 **임계로 읽는
  코드 0건**(일치하는 것은 전부 산문·주석·다른 맥락의 `paints` 필드). ⇒ **문면 1문단으로 족하다**(계약 3 과 정합).

- **★손대지 않은 세 자리 — 빠뜨린 것이 아니라 «고르지 않은» 것이다**(계약 4: 일제 점검은 범위 밖).
  ★★**줄번호로 지목하지 «않는다» — 문구로 지목한다**: 초판이 `:151`·`:184`·`:218` 로 적었는데
  ★**이 PR 자신이 만든 9줄 밀림** 때문에 착지 후 실제는 `:160`·`:193`·`:227` 이었고,
  ★**`:151` 은 «새 문단 안»을 가리켜 독자를 정확히 반대 자리로 보냈다**. ⇒ 갱신해도 다음 회차에 또 밀린다.
  ⒜「`paints` went *up* (55 → 83, the blank MIDP overpaint)」= 2026-09-05 사건 **서술**
  ⒝「measured 2026-09-07: `paints` 55 on both carriers」= 27스텝 논거의 **방증**
  ⒞「`--boot-secs 1.0` → 18.7 s, paints 55 → 37」= **노브 대비**(★이 회차의 기전 서술이 이것을 «근거로» 쓴다).
  셋 다 «그때의 비교»이지 «지금의 등식»이 아니고, 새 문단이 **그 수들을 읽는 법**을 앞에서 정한다.

- **게이트**: 문서 회차라 4게이트 전량은 불요(러스트 무접촉). 상시 PR 검사 로컬 선행 **전건 통과** —
  `check-doc-liveness-parity` **OK(25줄 전건 커버 · fenced `sh` 무접촉)** · `check-worklog-json` **OK(88)** ·
  `check-docs-report-serial` OK(중복 0) · `check-worklog-coverage` **9/10 · 약속 최신**(landed 86 / 기록 83) ·
  `npm run audit` **PASSED**.

- **채택 기록**: worklog `adoptedProposals: ["2026-09-13-lgt-entrypoint-jar-name-contract#p1"]`.
