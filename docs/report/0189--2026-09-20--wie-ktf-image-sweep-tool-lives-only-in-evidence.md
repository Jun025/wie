## [2026-09-20] KTF 이미지 스윕 도구를 repo 로 옮겼다 — ★**세 벌을 한 벌로 합치고, 「바이트 동일」로 그것을 증명했다** (wie-ktf-image-sweep-tool-lives-only-in-evidence)

인용 트리: `origin/main` = `6774d16c` · 조회 2026-09-20. 채택 ref `2026-09-19-ktf-db-slot8-token-dir-or-extension-unsettled#p1`.
산출: `scripts/ktf-image-sweep.py`(신규) · `scripts/checker-census.mjs`(+`py` 2자리) · `AGENTS.md` 등재 1블록.

### ⒜ 제안 원문(그 파일에서 읽었다) · ⒝ 해소되지 않았음을 «수»로 확인했다

`why` 축자: 「지금 스윕 도구(`slots.py` · `refs2.py` · `trace.py`)는 `reports/evidence/…-p0/` 에만 있고, 그 도구가 쥔 것은 재사용 가치가 큰 세 가지다: **동기화 Thumb 디코드**(임의 짝수 주소에서 시작하면 BL 4바이트 쌍 안으로 들어가 이후 전부 오디코드된다) · **베니어 표** · **sl 상대 전역 역추적**. 형제 도구 `scripts/game-lab-census-map.mjs` 가 이미 같은 이유로 `scripts/` 에 있다.」
`tradeoff` 축자: 「파이썬 + capstone 의존이 생긴다 … **`checker-census` 의 «호출자 0» 바구니가 또 하나 늘고**, 썩어도 red 가 나지 않는다 … 세 번째 이미지에서 깨질 여지가 그대로 있고, **그 한계를 머리주석에 적어야 한다**.」

**미해소 실측**: repo 의 **추적 `.py` 파일 0건**(`git ls-files '*.py'`) · `scripts/` 는 node+셸 **28항목**뿐 · 그 ref 를 `adoptedProposals` 로 가진 worklog **0건** · `capstone|__call_via|veneer` 를 아는 추적 파일은 **`wie-wipi-c/src/api/database.rs` 하나**(그것도 산문이지 도구가 아니다). ⇒ **열려 있다.**

### ⒞ ★왜 지금인가 — **이 비용을 내가 한 회차 전에 실제로 냈다**

제안의 `plainSummary` 는 「다음 사람이 저장소를 뒤지면 못 찾는다」인데, 실제로 일어난 일은 그보다 나쁘다:
★**형제 제안 `#p0` 을 이행한 직전 회차(2026-09-20)는 증적 디렉터리를 «찾았고» `slots.py` 를 복사해 썼으면서, `trace.py` 가 거기 있다는 것을 알아채지 못하고 그 기능을 «처음부터 다시 구현했다»**(임시 `win_dump.py`).
⇒ 한 회차에 도구 하나를 다시 만들었다. **이것이 이 제안의 근거 전부이고, 가설이 아니라 실측이다.**

### ⒟ ★세 벌이 아니라 «한 벌»로 옮겼다 — 제안 자신의 문면이 그렇게 말한다

제안의 tradeoff 는 바구니가 「**또 하나**」 는다고 적는다(단수). 그리고 세 파일은 **같은 전문(preamble)을 세 번** 갖는다(capstone Thumb 디코더 · `IMAGE_BASE` · `u16/u32/cstr` · `preceding`) — 이 저장소가 반복해 금지하는 «두 벌이 갈리는» 형태다.
⇒ **서브명령 3개를 가진 한 파일**로 합쳤다: `slots` · `refs` · `window`.

★★**그런데 «합치면서 바꾸지 않는 것»이 이 회차의 실제 난점이었다** — 세 원본의 헬퍼가 **미묘하게 다르다**:

| 헬퍼 | `slots.py` | `refs2.py` | `trace.py` |
|---|---|---|---|
| `cstr` 반환 | **bytes** · 인쇄가능 필수 · NUL 없으면 `None` | **str**(`.decode()`) · 인쇄가능 필수 | **raw bytes** · 필터 없음 · NUL 없으면 **n바이트로 자름** |
| `preceding` 기본 `back` | 40 | 30 | 인자 |

★**헬퍼를 «하나»로 뭉갰으면 출력이 조용히 바뀐다.** ⇒ 공유는 «완전히 같은 것»(`u16`/`u32`/`preceding`(back 명시)/이미지 적재)만 하고, 문자열 규칙은 **`cstr_raw(a, n, pad)` 한 겹** 위에서 각 서브명령이 **자기 규칙을 그대로** 적용하게 뒀다. 그 이유를 클래스 주석에 적었다.

### ⒠ ★검증 — 「합쳐도 같다」를 **바이트 동일**로 보였다(주장 아님)

두 이미지를 코퍼스에서 스크래치로 꺼내(`mktemp -d` · repo 밖) **원본 세 스크립트와 새 서브명령을 같은 인자로 나란히** 돌렸다:

| 서브명령 | 대조 대상 | 결과 |
|---|---|---|
| `slots` | 선행 회차 **증적 출력**(`slots-tears.txt`·`slots-getamped.txt`) | ★**본문 바이트 동일**(머리줄은 경로라 제외) · 머리 수치도 동일: `veneers=87 hits=19` · `veneers=74 hits=60` |
| `window` | `trace.py`(01031C0A `0x128f60` back 48 · 0103451A `0x105730` back 40) | ★**바이트 동일**(45줄 · 34줄) |
| `refs` | `refs2.py`(0103451A · `0x150,0x170,0x174,0x18c`) | ★**본문 바이트 동일** · `229 reference(s)` |

⇒ ★**합침은 행동 보존적이다** — 「같을 것이다」가 아니라 **같음을 실행해서 보였다**.

### ⒡ ★제안의 tradeoff 한 줄이 «틀렸고», 고치니 참이 됐다

제안은 「`checker-census` 의 «호출자 0» 바구니가 또 하나 늘고」라고 적었다. **실제로는 늘지 않는다** — 그 census 의 모집단이 `scripts/*.{sh,mjs,js}` 라 ★**`.py` 를 아예 보지 않는다.**
⇒ 그대로 뒀다면 도구가 `scripts/` 에 있으면서 **repo 자신의 «여기 뭐가 있고 누가 부르나» 답에서는 보이지 않는** 상태가 된다 — 0-caller 로 세어지는 것보다 **나쁘다**.
**두 자리를 고쳤다**(모집단 정규식 2개 + 출력 라벨 1줄).

**양방향 실측**(★이 census 는 **실패 상태가 없는** 도구라 「red/green」 축이 없다 — 그래서 **보이는가/안 보이는가**로 보였다):

| | 결과 |
|---|---|
| ⒝ **대조군**(`py` 미포함 = 현행 main) | `44 artifacts · callers 0 = 10` · `ktf-image-sweep` 등장 **0회** |
| ⒜ **실험군**(`py` 포함) | `45 artifacts · callers 0 = 11` · **`callers 0` 바구니에 실재** |

⇒ ★**제안이 예측한 「또 하나 늘고」가 «이제» 참이다.**
★라벨 줄도 함께 고쳤다 — 그 줄은 정규식과 **별개의 하드코딩 문자열**이라, 안 고쳤으면 «모집단을 거짓으로 광고하는» 상태가 됐다.

### ⒢ 의존성 — ★**capstone 은 이 맥에 «없다». 그래서 도구가 그 사실을 말한다**

실측: `/opt/homebrew/bin/python3`·`/usr/bin/python3` **전건 import 실패**. ⇒ 맨손 호출은 import 에서 죽는다.
그대로 두지 않고 **rc=2 + 처방 한 줄**로 바꿨다(`uv run --with capstone python3 …`). 이 저장소 관용(`exit 2` = 「못 쟀다」이지 「없다」가 아니다)을 그대로 따른다.
**rc 전수(파이프 없이 실측)**: capstone 없음 **2** · 알 수 없는 서브명령 **2** · 없는 이미지 **2** · 인자 부족 **2** · `--help` **0** · 정상 실행 **0**.
※★첫 측정 때 `| tail -1` 뒤의 `$?` 를 읽어 **전부 rc=0 으로 보였다** — 파이프의 마지막 명령 상태였다. 파이프 없이 다시 재서 고쳤다. **그 오측을 적어 둔다**(이 저장소가 반복해 이름 붙인 「부재를 통과로 읽는다」의 한 변종이다).

### ⒣ 등재 · 모드

`AGENTS.md` §Web-surface 「local only」 목록에 등재했다 — ★**펜스 `sh` 블록으로 넣지 않았다**(그러면 `doc-liveness.yml` 동기 의무가 붙는데 이 명령은 코퍼스가 필요해 주 러너가 못 돌린다 · 선례 `0173`·`0187` 이 같은 이유로 같은 선택). 확인: `check-doc-liveness-parity` **rc=0 · 26 documented line(s)**(불변).
파일 모드 **644** — ★제안이 선례로 지목한 `scripts/game-lab-census-map.mjs` 와 **같다**(shebang 있음 · 644). `old mode/new mode` **0건**.

### 안 한 것 · 대가

- ★**`repro.py` 는 옮기지 않았다** — 그것은 «그 회차의 독립 재현»이지 재사용 도구가 아니다(제안도 세 개만 지목했다).
- ★**동작을 «개선»하지 않았다** — 합치면서 로직을 한 줄도 바꾸지 않았다. 바이트 동일이 그 증거이고, 개선은 별건이다.
- ★**세 번째 이미지에서 깨질 여지는 그대로다** — 제안이 요구한 대로 **머리주석에 적었다**(베니어 탐지·`table = *(*global)` 이중 역참조·sl 역추적이 전부 두 이미지에서 읽은 패턴이고, 실패 양식은 «크래시»가 아니라 «그럴듯한 오답»이다).
- ★**재배치 델타는 이 도구가 «모른다»** — 정적 워드를 찍는다. 이미지마다 유도하고 독립값으로 교차검증하라고 머리주석이 못박는다(델타 재사용 금지).
- ★**`slots` 인자 요약 열의 오류를 머리주석에 «주소와 함께»** 적었다(`01031C0A:0x128f4a` — 열은 토큰을, 실제는 경로를 싣는다). 선행 회차가 `limits` 에만 적어 둔 한계를 **도구 자신으로** 옮긴 것이다.
- ★**CI 는 이 도구를 돌릴 수 없다**(코퍼스 = git-ignored 게임 바이트 · Constraint 9). 썩어도 red 가 나지 않는다 — `smoke_gate.sh`·`game-lab-recensus.sh` 와 같은 계급이고, 그 사실을 머리주석과 `AGENTS.md` 양쪽에 적었다.
- ★**게임 파일명 유입 — 도구를 «실행해서» 적는다**: `origin/main...HEAD` 3파일에서 **BOUNDED 0회/0쌍 · PREFIX-EMBEDDED 0회/0쌍 · SUFFIX-ATTACHED 0회/0쌍** ⇒ SUFFIX 가 비었으므로 「**유입 0건**」을 그 도구의 규칙대로 주장할 수 있다.
