## [2026-09-19] SVC selector 5 function 0 — ★**이름·크기·플래그를 받는 «열기»다**(`f("SaveData", 0xeec, 0, 1)`). ★그런데 «어떤 API 의 열기인가»는 확정하지 못했다 (2026-09-18-repair-campaign-pilot-unimpl-stub-p1)

### ⒜ 원문 대조 — 브리프와 원문은 «글자 그대로» 같다

정본 = `docs/worklog/2026-09-18-repair-campaign-pilot-unimpl-stub.json` 의 `proposals[1]`.
브리프가 인용한 `title`·`plainSummary` 두 줄은 원문과 **바이트 동일** — 어긋난 곳 **0**.

★★**단 원문 `tradeoff` 의 «방법 처방»은 따르지 않았고, 그 편이 나았다 — 적어 둔다.**
원문: 「★명세가 없으므로 «역공학»이다 — **호출 시 인자·복귀값을 로깅해** 세 게임의 사용 패턴을 맞춰 보는
수밖에 없다」. ⇒ ★**«수밖에 없다»가 아니었다.** 형제 회차(`…-p0`)가 세운 정적 방법
(동기화 Thumb 디스어셈블 + sl 상대 포인터 표의 재배치 델타)으로 **에뮬레이터를 한 번도 돌리지 않고**
아래 ⒝ 의 사실 전부를 얻었다. ★로깅은 **확인용으로 마지막에** 한 번 썼고, 그 결과가 정적 결론과
**레지스터 단위로 일치**했다(⒞).
★**원문의 다른 절반은 그대로 지켰다**: 「«0 을 돌려주면 지나간다»는 유혹 … 그것은 추측이다」 ⇒
★**구현하지 않았다.** `Unimplemented` 그대로다.

### ⒝ 무엇을 쟀는가 — ★**이 함수의 «모양»이 섰다**

**⑴ arity 는 4이고, 디스어셈블로 확정했다.** 세 호출부 전부에서 `r0`~`r3` 이 `bl` **직전에** 세팅되고
피호출자 주소는 **`r4`** 에 있다(`__call_via_r4` 베니어). 스택 인자 **0**(창 안에 `str …,[sp]` 없음).
```
0103451A 급 tycoon  0x10169e:  r0=*[SL+0x20]  r1=#0xeec  r2=#0      r3=#1   callee r4=***(SL+0x1c)
sega                0x12327c:  r0=<호출자 인자> r1=#0x80  r2=#1      r3=#1   callee r4=***(SL+0x77c)
golf                0x10172a:  r0=*[SL+0xa8]  r1=#0x80   r2=#0      r3=#1   callee r4=***(SL+0xa4)
```
★**인터페이스는 «세 번» 역참조해야 닿는다**(`***전역`) — 형제 회차가 DB 표에서 찾은 «두 번»보다 한 겹 깊다.

**⑵ ★★`r0` 은 «이름»이다.** 형제 회차의 재배치 델타 방법으로 정적 해석했다
(`0103451A` 급 `+0xE2C` · golf `+0xFA0` — 각각 덤프의 `R0` 로 유도하고 이웃 전역으로 교차검증):
**`"SaveData"`** · **`"FG_102"`** · (sega 는 호출자 인자라 정적으로는 못 읽었고, 실행이 **`"SPORTS"`** 라 답했다).

**⑶ ★`r1` 은 «크기»다** — `0xeec`(3,820) · `0x80`(128) · `0x80`(128). `r2` 는 플래그(0·1·0),
`r3` 은 **세 게임 모두 `1`**.

**⑷ ★★반환값은 «핸들»이다 — 호출부가 그렇게 쓴다.**
- tycoon: `cmp r0,#0 ; bgt` ⇒ ★**양수일 때만** 진행.
- golf: `movs r2,#0xc ; rsbs r2,r2,#0 ; cmp r0,r2 ; bne` ⇒ ★**`-12` 과 비교한다.**
  ★**`-12` 은 이 저장소가 이미 `M_E_NOENT` 로 적어 둔 그 값이다**(`database.rs` 의 `open_database`
  가 없을 때 돌려주는 값). ⇒ **「그 이름이 없다」 분기가 실재한다.**
- sega: 반환값을 그대로 **같은 표의 slot 10** 에 첫 인자로 넘긴다.

⇒ ★★**종합: `f(이름, 크기, 플래그, 1)` → 성공 시 양수 핸들 · 부재 시 `M_E_NOENT` · 그 핸들을 같은 표의
다른 슬롯이 받는다.** 이것은 **이름·크기로 여는 저장소 API** 의 모양이다.

**⑸ 이 표는 «한 함수»만 쓰이는 게 아니다.** 같은 전역을 통한 호출 전수(전방창 12):
tycoon `{0×3, 4×1}` · golf `{0×8, 4×4, 7×6}` · sega `{0×2, 1×2, 4×1, 5×1}` ⇒
★**function 0 호출만 13곳**이고 슬롯 **0·1·4·5·7** 이 쓰인다 — 다음 회차가 걸 표적 목록이다.

### ⒞ 무엇을 바꿨는가 — 수로 · ★**의미 이름은 0글자**

**1파일 `wie-ktf/src/runtime/wipi_c/method_table.rs` +168/-6.** 제품 동작 변경은 **진단 문자열 하나**다.

| 무엇 | 전 | 후 |
|---|---|---|
| 미식별 표 스텁의 메시지 | selector·slot·function id | **+ `r0`~`r3` 실값 + `r0` 이 가리키는 토큰 인용** |
| 그 스텁의 인자 | 0개(컨텍스트만) | 4개 — ★**arity 주장이 «아니다»** 라고 메시지가 스스로 말한다 |
| 순수 술어 | 없음 | `token_from_bytes`(경계) · `unnamed_table_message`(문면) — **시험 2건** |

★**clippy 가 설계를 한 번 고쳤다 — 적어 둔다.** 초판 `unnamed_table_message` 는 레지스터 넷을 `a0..a3`
**이름 붙은 파라미터**로 받아 `too_many_arguments`(8/7)로 red 였다. 고치면서 `[WIPICWord; 4]` **배열**로
바꿨는데, ★**그 모양이 원래 말하려던 것에 더 가깝다** — 「SVC 가 본 `r0`~`r3`」이지 「이 함수의 네 인자」가
아니다. `a0..a3` 라는 이름 자체가 둘째 주장을 조용히 하고 있었다.

★**실행으로 확인**(세 게임 모두, 에뮬레이터 실제 구동):
```
5: … function 0; entry registers r0=0x146568 ("SaveData") r1=0xeec r2=0x0 r3=0x1 (…NOT a claim…)
5: … function 0; entry registers r0=0x12fa18 ("SPORTS")   r1=0x80  r2=0x1 r3=0x1 (…)
5: … function 0; entry registers r0=0x15f218 ("FG_102")   r1=0x80  r2=0x0 r3=0x1 (…)
```
★★**그 레지스터 값이 «7월 덤프»와 전건 일치한다** — 정적 분석과 실행이 서로를 독립으로 확인했다.
그리고 ★**sega 의 `r0` 은 정적으로 못 읽던 값**인데 실행이 `"SPORTS"` 로 답했다 ⇒ **두 방법이 상보적이다.**

### ⒟ ★★확정하지 «못한» 것

★**«어떤 API 인가»는 확정하지 못했다.** 모양은 「이름·크기로 여는 저장소」인데, **그것이 DB(표 7)와
어떻게 다른지**는 못 갈랐다 — 표 7 의 열기는 `(name, mode, type)` 로 **크기를 받지 않는다**는 구조적
차이만 확인했다. ★**이름을 붙이지 않았고 `Unimplemented` 도 그대로다** — 원문이 「0 을 돌려주면
지나간다는 유혹 … 그것은 추측」이라 못박은 자리이고, 이 리니지는 그 길에서 **이미 두 번** 틀렸다.
★**가를 수 있는 다음 관측은 후속 제안에 «설계까지» 적었다.**

### ⒠ 양방향 개악 — ★제품 호출부, **4축**(증적 `evidence/…-p1/`)

```
1 토큰 인용        원형 green → 메시지에서 {quoted} 제거              → ★red → 원복 green
2 경계            원형 green → 출력가능 검사 + 길이 상한 제거          → ★red → 원복 green
3 «arity 아님» 문구 원형 green → 그 절을 제거                          → ★red → 원복 green
4 ★★끝에서 끝까지  원형: 실게임이 ("SaveData") 를 낸다
                  개악: unnamed_table_token 이 항상 None → ★그 이름이 «사라진다»
                  원복: 다시 ("SaveData")                              ← 실제 게스트로 잰 3상태
```

### ⒡ 대가 — 「잃는 것이 없다」가 아니다

1. ★★**게스트 문자열이 오류 메시지에 들어간다.** `r0` 은 게스트가 그 레지스터에 남긴 값이고 이 코드가
   그것이 경로가 아님을 증명할 수 없다(`wie_validate --guest-stdout` 이 opt-in 인 이유).
   **16바이트 상한 + 출력가능 ASCII**로 좁혔고 실측 이름은 6~8바이트지만 ★**0 은 아니다.**
2. ★★**스텁이 레지스터 4개를 읽는다 — 그 수는 «이 함수»에서만 잰 것이다.** 같은 스텁이
   **64슬롯 × 8표**를 받치고 그쪽 arity 는 아무도 모른다. ⇒ 메시지에 「arity 주장이 아니다」를 박았지만
   ★**읽는 사람이 그 줄을 무시하면 오해할 수 있다.** 이 저장소의 「문서가 코드보다 넓게 약속한다」와
   **반대 방향의 같은 위험**이다.
3. ★**술어가 두 벌이 됐다.** `wie-wipi-c` 의 `slot8_token` 과 같은 판정인데, 그쪽은 **열린 형제 PR**
   (`#211`)에 있어 지금 합치면 둘 다 단독으로 머지 불가가 된다 ⇒ **일부러 복제**하고 주석에 적었다.
   ★두 PR 이 모두 착지한 뒤 합쳐야 한다(후속).
4. ★**세 게임은 여전히 FAIL 이다** — PASS 전환 **0건**. 바뀐 것은 다음 회차의 비용뿐이다.
5. ★**메시지가 길어졌다** — 이 줄은 실패 전사에 그대로 실린다.
6. ★**정적 방법의 한계는 그대로다** — 재배치 델타의 «출처»를 여전히 모르고(형제 회차의 그 한계),
   스윕은 직선 모델이라 레지스터 생존을 추적하지 않는다. 본문 인용은 **손으로 읽은 창**에서만 왔다.
7. ★**전 코퍼스 재실행 0** — 다른 버킷이 움직이지 않았다는 주장은 **하지 않는다**.

### ⒢ 게이트

`evidence/gates.txt` 참조(rc + 소요 초). ★이 회차는 **제품 Rust 를 실제로 바꿨으므로** 게이트가
«이 diff 를 겨눈다».

### 범위 판정 — 제안자 추정 `M` ↔ ★내 실측도 `M`

한 회차에 들어갔다. **좁히지 않았다** — 원문이 요구한 것(「그 하나만 알아내면 3개가 함께 풀릴 수 있다」의
전제인 «그 하나가 무엇인가»)은 ⒝ 의 다섯 사실로 섰다. **넓히지도 않았다** — 구현은 원문이 명시적으로
경계한 자리라 후속으로 남겼다. ★**반쪽 착지 위험 없음**: 다음 게임이 **어느 미식별 표에 닿든** 자기
레지스터 4개와 이름을 스스로 말한다.

### 범위·경계

`game_lab/` **쓰기 0**(읽기 전용 · `git status --porcelain game_lab/` 무출력) · 게스트 메모리 쓰기 **0** ·
분류 술어·`classify.sh`·`.gitignore`·`STATE.md`·`AGENTS.md` **무접촉** · 새 검사기·새 워크플로 **0** ·
스크래치는 `mktemp -d` → 디렉터리째 삭제.
★**게임 파일명 유입** — 이 diff 의 추가 줄에 코퍼스 stem **0건**(NFC 완전일치 대조).
★**게임 «내부»의 문자열**(`"SaveData"`·`"SPORTS"`·`"FG_102"`)은 코드 주석·이 기록에 들어갔다 — 그것은
코퍼스 **파일명이 아니라 이미지 안의 리터럴**이고 게임 **바이트는 0**이다(Constraint 9 는 bytes 다).
