## [2026-09-17] 연번 0113 이중 claim — «이미 해소됐다»를 **처분 기록으로** 남긴다 (wie-adopt-slice-d-blocked-on-lgt-abi-decision-p1-fix)

**무엇을**: 제안 `2026-09-16-slice-d-blocked-on-lgt-abi-decision#p1` 의 처분을
`docs/worklog/2026-09-17-serial-0113-collision-already-resolved.json` 의 ★**`adoptedProposals` 한 줄**로 남겼다.
★**제품 코드 0줄** · 사실 판정 **불변**. ★**PR 1건 close(비가역 아님)** — 아래 «표제 정정».

**왜**: 직전 회차(`-p1`)가 「이미 해소됐다」를 **정확히** 판정하고도 ★**그 처분을 «기록»하지 않았다.**
게이트②가 **critical** 로 반려했고, 그 지적이 옳다.

---

## ★반려 사유 ① — 내 논거가 «순환»이었다

직전 회신은 Acceptance ⑶(`adoptedProposals` 기록)을 이렇게 접었다:

> 「`adoptedProposals` 기록은 **worklog 를 낳는 회차**의 의무인데 이 회차는 worklog 를 만들지 않는다(변경 0)」

★**이것이 순환이다** — 「기록을 안 남겼으니 기록 의무가 없다」. 그리고 ★**같은 리니지가 «같은 날» 그것을 반증했다**(검수자 실측):

| 회차 | 결론 | `adoptedProposals` | 결과 |
|---|---|---|---|
| `#p0` | 「이미 났다」 | ★**기록함** · `proposals: []` | PR #172 — ★**착지** |
| `#p2` | 「이미 지켜졌다」 | ★**기록함** | ★**착지** |
| ★`#p1`(나) | 「이미 해소됐다」 | ★**없음** | — |

★★**그리고 `#p0` 회차가 자기 리포트에 이유를 명시해 두었다**:
「★**이 회차의 진짜 산출물은 worklog 의 `adoptedProposals` 한 줄**이다 … 그 기록만이 **내일 또 채택되는 것**을 막는다」.
⇒ ★**「변경이 없어 산출물이 없다」가 아니라 «그 한 줄이 산출물»이라는 것이 이 리니지의 이미 확립된 판정이었다.**
★**Contract 2(「이미 해소됐으면 코드 0줄로 그 사실만 적고 나오라」)와 충돌하지 않는다** —
★**«코드 0줄»은 «파일 0개»가 아니고, 처분 기록은 코드가 아니다.**

### ★파생 결함 — 내가 만든 전제로 나를 면제했다

직전 회신은 ⑷(PR head)·⑸(`ci-presence`)도 「PR 이 없으니 해당 없음」으로 접었다.
★**그런데 PR 이 없던 이유가 «⑶을 건너뛴 것»이다** ⇒ ★**자기가 만든 전제로 자기를 면제한 형태**다.
이 회차가 ⑶을 이행하니 ★**PR 이 생기고 ⑷⑸ 칸이 실제로 성립한다.**

## ★표제 정정 ② — 「PR 0」이 덮은 것

직전 회신 표제는 「**코드 0줄 · PR 0 · 커밋 0**」이었다. ★**그 회차는 PR **#174** 를 `CLOSED` 로 닫았다**
— 저장소 상태 변경이다. ⇒ 정확한 표제는 ★**「코드 0줄 · 커밋 0 · **PR 1건 close(비가역 아님)**」**이다.
★**은폐는 아니었다**(본문 「부수 발견」 절에 사유·근거를 전문 공개했고 검수자도 그렇게 판정했다) ·
★**브랜치 `wie-worklog-coverage-remeasure-104` 가 원격에 보존**(`9dc95560`)돼 있어 **reopen 가능**하다.
★**#174 를 되살리지 않았다** — 그 파일의 소유자는 총괄이 `-p0-merge`·`-p2-merge` 에 명시한 **게이트③ 회차**다.

---

## 처분한 사실 — ★**바뀌지 않았다**(검수자가 「전건 참」으로 확인 · 재조사 금지)

제안: 「`--next-serial` 이 `열린 PR claim [0113(#159) 0113(#160)]` 을 낸다 ⇒ 둘 다 착지하면 main red」.

★**이미 풀렸고, 푼 주체는 «옮겨야 할 쪽» 자신이다**:
커밋 **`39ffb2ef`** `[wie-p3-slice-a-keydraw-lgt-breaks-on-upstream-base-fix]`
**「연번 0113 → 0115 — 조각 C 가 0113 을 가지고 착지했다」**
⇒ #159(조각 C)가 `0113` 으로 **먼저 착지**(`01:20:39Z`)하자 #160(조각 A)의 `-fix` 회차가 **착지 전에** 자기 번호를 옮겼다
(`02:04:45Z` · 실제 `0115`). ★**`AGENTS.md` 의 「착지하지 «않은» 쪽을 옮겨라」 그대로**이고,
★**제안이 예측한 번호(`0115`)까지 맞았다.**

현재 상태: `main` 의 `0113` ★**1개** · 열린 PR 중 `0113` claim ★**0건** · `check-docs-report-serial` ★**rc=0**.

## 이 회차가 한 일

- ★**`adoptedProposals: ["2026-09-16-slice-d-blocked-on-lgt-abi-decision#p1"]`** 1줄(`proposals: []`).
  ★**형제 `#p0` 의 착지분 worklog 형상을 그대로 복제**했다 — 발명 0.
- ★**「채택」을 골랐다**(`declined` 아님): 제안의 관측·위험·처방·예측 번호가 ★**전부 맞았기** 때문이다.
  기각은 「제안이 틀렸다」는 뜻인데 그것은 사실이 아니다. ★**낡은 것은 «판단»이 아니라 «시점»뿐**이다.
  ※검수자 문면대로 «둘 중 어느 쪽이든» 추천 패널에서 그 ref 를 내린다 — 중요한 것은 **처분했다는 기록**이다.
- 이 리포트 + `STATE.md` 완료 1건. ★**연번은 `--next-serial` 을 «작성 직전»에 재조회**해 `0132` 를 잡았다.

## ★대가

⑴★**열린 PR 이 하나 늘고 게이트③이 하나 더 생긴다** — 「PR 0」이던 회차가 PR 을 만든다.
⑵★**이 repo 는 오늘 연번으로 «두 번» 막혔다**(0122·0123) ⇒ 새 보고서가 또 충돌을 만들 수 있다.
  그래서 ★**`--next-serial` 을 작성 직전에 재조회**했고(0132), ★**착지 시점에 다시 볼 일**이다.
⑶**안 하면**: 같은 제안이 내일 또 채택되고 ★**그 회차도 「이미 해소됐다」로 끝난다** — 무한히 싸 보이는 비용이다.

## 사용자 영향

**없다.** 원장 3파일 · 제품 코드 0줄.
