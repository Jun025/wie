## [2026-09-21] 조각 D 의 재개 게이트는 «결정»이다 — 죽은 항목 둘을 처분하고 ⒜/⒝ 를 «가역성»으로 쟀다 (wie-p3-slice-d-resume-gate-is-a-decision-and-the-next-section-carries-two-dead-items)

**무엇을** — 문서·측정 전용(**제품 코드 0줄**). ⓐ `STATE.md` `## 다음` 의 죽은 항목 둘을 처분했고,
ⓑ 조각 D 의 미결 결정 ⒜/⒝ 를 **정확성이 아니라 «되돌리는 비용»** 축으로 재서 권고 한 줄을 냈다.

**왜** — 유휴 레인 오라클의 처방이 「`## 다음` 을 읽고 발권하라」인데, 그 절이 **이미 닫힌 일 둘**을 이고
있었다. 깨진 것은 코드가 아니라 ★**«다음 절»의 신뢰**다.

**사용자 영향** — 없음(제품 코드 0 · 게이트 무접촉). 파이프라인 영향: 다음 유휴 회차가 닫힌 일을 집어 들지 않는다.

---

## ⓐ 죽은 항목 둘 — 처분

### ⑴ 연번 `0113` 이중 claim — 「총괄 판단 대기」로 적혀 있었으나 «이미 해소»

`STATE.md`(mtime 2026-09-20 04:15)가 #159·#160 을 **둘 다 미착지**로, 충돌을 **미해결**로 적었다.
총괄 실측을 이 회차가 **독립으로 재확인**했다(`gh` + `git ls-tree origin/main`):

| 축 | STATE.md | 실측 2026-09-21 |
|---|---|---|
| #159 | 미착지 | **MERGED `2026-09-16T01:20:39Z`** · 머지 `3744f84e` |
| #160 | 미착지 | **MERGED `2026-09-16T02:04:45Z`** · 머지 `d70b93f8` |
| 연번 충돌 | 「둘 다 착지하면 main red」 | ★**red 아니다** — `check-docs-report-serial` rc=0 · 중복 연번 **0**(194건 중) |

`origin/main` 실재: `docs/report/0113--…-slice-c-…md` · `0114--…-slice-d-…md` · `0115--…-slice-a-…md` ·
처분 기록 `docs/worklog/2026-09-17-serial-0113-collision-already-resolved.json`.

★**지우지 않고 «해소»로 고쳐 썼다 — 그 줄의 예측이 맞았기 때문이다.** STATE.md 는
「옮기는 쪽은 **0115** 를 잡아야 한다(0114 는 이 회차가 썼다)」고 적었고, 실제로 **#160 의 `-fix` 회차가
커밋 `39ffb2ef` 로 0113 → 0115** 를 집행했다. ★**맞은 예측의 기록은 다음 회차의 자산이다.**

★**대가를 정확히 적는다 — 이것은 «무증상»이었다.** 잘못 배차된 회차도, red 도 없었다.

### ⑵ A~E 요약 줄 — A·D 를 같은 절의 「조각 A 돌았다」·「조각 D 시도 → 멈췄다」 항목과 정합

★**그 요약이 «틀린» 것이 아니라 «B·C 에만 취소선이 그어져» A·D 가 미착수로 읽혔다.** 같은 절의
「조각 A 돌았다」 항목과 「조각 D 시도 → 멈췄다」 항목이 그 둘을 **이미** 적는다. 두 서술을 맞췄다.
★**줄번호로 인용하지 않았다** — `AGENTS.md` 가 그 부식을 명시로 경고하고, 실제로 이 회차의 편집만으로도
재개 조건 블록이 **211 → 218행**으로 밀렸다. ⇒ 앵커는 «내용»이다.

| 조각 | 종전 표시 | 정합 후 | 근거 |
|---|---|---|---|
| **A** | 표시 없음(미착수로 읽힘) | ~~취소선~~ + **돌았다** | 「조각 A 돌았다」 항목 · `0115` |
| **B** | ~~취소선~~ 끝났다 | 불변 | — |
| **C** | ~~취소선~~ 부분 착지 | 불변 | — |
| **D** | 표시 없음(미착수로 읽힘) | ~~취소선~~ + **시도했고 멈췄다** | 「조각 D 시도 → 멈췄다」 항목 · `0114` |
| **E** | 표시 없음 | **미착수**(명시) | 선행 D 미완 |

★**부수로 «텍스트 파손» 1건을 함께 고쳤다**(사실 추가 0 · 분량 감소): 그 항목은 **B 를 두 번**
서술하고 있었고(`② 51건 hunk 분류`가 두 블록 · 폐기21/재적용24/발신6 이 중복), C 의 괄호가
그 중복과 C 자신의 취소선 항목을 삼켜 **괄호가 11줄 뒤에서 닫히고** 있었다. 반복 편집이 남긴
자국이다. ⇒ ★**사실은 전건 보존**하고(A~E 각각의 M/L·위험·선행 · C 의 기대값 `53 ≤ N < 74`
및 실측 74→67 · 「그룹 B·C 는 D 안에서 처분」) 구조만 폈다.

### ⑶ 순서 선행 — 「#159 착지 뒤」는 «열렸다»

위 「조각 D 시도 → 멈췄다」 항목의 재개 조건에 적힌 `★순서는 셋 중 무엇이든 #159 착지 «뒤»다` 가 **2026-09-16 01:20 에 충족**됐다.
★**다섯 달이 아니라 «닷새»째 열려 있었다.** ⑴코퍼스·⑶upstream SDK 는 이 레인 밖이므로
★**이 레인이 움직일 수 있는 것은 ⑵ 하나이고, 그것은 «측정»이 아니라 «결정»이다.**

---

## ⓑ ⒜/⒝ 를 «가역성»으로 재다 — 권고

★**«옳다»를 증명하려 하지 않았다.** 「코퍼스 없이 어느 쪽도 옳다를 증명 못 한다」는 STATE.md 의
판정은 **유효하고 이 회차가 건드리지 않는다.** 잰 축은 **정확성이 아니라 «되돌리는 비용»** 하나다.

### 측정 ① — 배선의 크기는 «대칭»이다

`wie-lgt/src/runtime/wipi_c.rs` 의 `=> graphics::` 를 `=> wie_wipi_c::api::graphics::` 로 치환:

| 축 | 값 |
|---|---|
| 변경 줄 | **27 added / 27 removed** |
| 변경 파일 | **1** (`wie-lgt/src/runtime/wipi_c.rs`) |
| 역방향(⒝→⒜) | **같은 파일 · 같은 27줄**(치환의 역) |

★**표면은 `44fbf265`(조각 A 가 잰 ref)와 오늘 `upstream/main`(`3e203809` · 2026-09-20)에서 «동일»하다** —
`=> graphics::` **27** · `graphics.rs` **1,095줄**, 양쪽 다. ⇒ ★**결정 표면은 닷새간 드리프트하지 않았다.**

### 측정 ② — ★⒝ 는 그 1,095줄을 «버리지 않는다»(STATE.md 의 전제 정정)

STATE.md 는 ⒝ 를 「그 1,095줄을 **버려** benefit 을 스스로 취소한다」로 적는다. ★**과장이다.**
27줄은 `match` 팔이고, 그 밖에 **진입점 3개가 계속 산다**:

| 진입점 | 호출처 |
|---|---|
| `graphics::init_process_state` | `wie-lgt/src/runtime/wipi_c.rs:199` |
| `graphics::set_use_annunciator` | `wie-lgt/src/runtime/wipi_c.rs:200` |
| `graphics::set_display_property` | `wie-lgt/src/runtime/init.rs:32` |

⇒ 모듈이 통째로 고아가 되지 않고 **파일이 트리에 남는다.**
★**단 공짜도 아니다**: `graphics.rs` 의 `pub fn` **30개 중 27개**가 미참조가 된다
(⒜ 에서는 30/30 이 참조된다 — 27 팔 + 위 3개, 슬랙 0). 그리고 `mod runtime;` 이 크레이트 루트에서
**비공개**이므로(`wie-lgt/src/lib.rs:5`) 그 27개는 `dead_code` 다.

★★**이것을 «추론»으로 두지 않고 «실측»으로 바꿨다** — 같은 형상(사설 모듈 사슬 · 미참조 `pub fn`)의
최소 재현을 만들어 돌렸다: `warning: function ... is never used` → `RUSTFLAGS=-D warnings` 에서
★**`error: ... is never used` · 컴파일 실패**. ⇒ ⒝ 의 추가 비용은 **`#![allow(dead_code)]` 1줄**
또는 **그 27개 삭제**다.

⇒ ★**정확한 문장은 「버린다」가 아니라 「«주차»한다」이고, ⒜ 로 되돌리는 값은 27줄이다.**

### 측정 ③ — ★게이트 비용은 «일방적»이다 (여기가 비대칭이 사는 곳)

⒜ 에서 `keydraw_lgt` 는 FAIL 이다(paints 0 · 게스트 SDK 가 `framebuffer.rs:149` 에서 스스로 패닉).
그 픽스처를 **실행으로 소비하는** 자리를 전수했다:

| 소비자 | 단언 축 | ⒜ 에서 | 어디서 도나 |
|---|---|---|---|
| `scripts/contract-roundtrip.mjs` **Scenario F** | ★**정확 픽셀 수**(`runF.pixels === k.expectPixels` · 키 3개) | ★**확실히 깨진다**(paints 0) | `contract` — ★**main 의 required check** |
| 같은 파일 **F-res** | 게스트 콘솔 `res:` 줄 | 미측정 | 같음 |
| `wie-lgt/tests/test_key_reach.rs` | ★**게스트 stdout** `key:<code>` | ★**미측정**(아래) | `rust_ci` ×6 legs — **required ×3** |
| `wie-lgt/tests/test_resource_reach.rs` | ★**게스트 stdout** `res.bin` 크기·합 | ★**미측정** | 같음 |
| `wie_cli/src/bin/wie_validate.rs` | 픽스처별 기대표 | 재분류 필요 | 로컬 · 주간 |
| `AGENTS.md` 러너 블록 + `.github/workflows/doc-liveness.yml` | `--expect-last-frame` rc | 재분류 필요 + ★**parity 검사기가 둘을 묶는다** | 주간 |

★★**미측정을 «없음»으로 적지 않는다** — LGT 쪽 `cargo test` 둘은 **픽셀이 아니라 stdout** 에 단언한다.
⒜ 에서 게스트가 `key:` / `res:` 를 **패닉 «전»에 찍는지**는 base swap 을 실제로 돌려야 알 수 있고,
★**그것은 이 티켓이 금지한 조각 D 의 실행**이다. ⇒ ★**⒜ 의 게이트 비용은 «최소 Scenario F»이고 상한은 열려 있다.**

★**반대로 ⒝ 의 게이트 비용은 «0»이다** — `keydraw_lgt` 를 소비하는 어느 자리도 바뀌지 않는다(지금 전건 green).

★★**그리고 ⒜ 쪽 비용은 «되돌릴 수 없는 종류»를 하나 품는다**: STATE.md 가 이미 잰 대로
게스트 SDK 에 **`wipi/src/framebuffer.rs` 의 `lgt` 분기가 0건**이라 ★**⒜ 를 고르면 그 테스트를 다시 만들 수도 없다.**
⇒ required check 를 **약화시키는** 변경인데 **신호를 복원할 경로가 없다.**

★**부수 발견 1건(범위 밖 · 고치지 않았다)**: `AGENTS.md` 는 「`keydraw_lgt.zip` has no `cargo test`
coverage at all … `wie-lgt/tests/` holds only `test_helloworld.rs`」라고 적는데 ★**낡았다** —
`origin/main` 에 `wie-lgt/tests/test_key_reach.rs`·`test_resource_reach.rs` 가 **실재**하고
전자의 머리주석이 「Why this file had to be written twice」로 복원 경위를 적는다. 별건 발권감이다.

### 권고 — ★**«비긴다»가 아니다. ⒝ 다.**

| 축 | ⒜ upstream LGT 유지 | ⒝ 공용 복귀 |
|---|---|---|
| 반대쪽으로 넘어가는 diff | **27줄 / 1파일** | **27줄 / 1파일** ⇒ ★**비김** |
| 1,095줄의 운명 | 사용 중 | ★**트리에 남는다**(+ `allow(dead_code)` 1줄 또는 27 fn 삭제) |
| 착지에 드는 게이트 변경 | ★**required check 약화**(최소 Scenario F 3단언 · 상한 미측정) | ★**0** |
| 약화한 신호의 복원 가능성 | ★**없다**(SDK 에 `lgt` 분기 0건) | 해당 없음 |

⇒ ★★**권고 한 줄: base swap 의 착지 위치로 ⒝ 를 골라라 — «옳아서»가 아니라 «되돌리기가 싸서»다.**
코드 축은 **27줄로 대칭**이라 비기지만, **게이트 축이 일방적**이다. ⒝ 는 27줄로 언제든 ⒜ 가 되고
그동안 benefit(1,095줄)은 트리에서 **주차**된 채 보존된다. ⒜ 는 required check 를 **복원 경로 없이**
약화시킨 뒤라야 서고, 되돌리려면 그 약화를 **다시 걷어내야** 한다.

★★**이 권고가 «주장하지 않는» 것 — 흐리지 마라.**
⑴**어느 쪽이 실제 clet 에 옳은지는 여전히 모른다.** `docs/lgt_abi.md:930` 의 놈ZERO 관측이 우리 픽스처와
같은 모양이라 위험이 「픽스처 편향」에 한정될 가능성은 낮아졌지만 ★**확정이 아니다.**
⑵★**코퍼스가 생기면 LGT 52건을 먼저 돌려라** — 그 답이 이 권고를 **뒤집을 수 있고, 뒤집어야 한다.**
⑶이것은 **권고이지 집행이 아니다.** 조각 D 는 돌리지 않았고 제품 코드는 0줄이다.

---

## 검증

- `gh pr view 159/160` · `git ls-tree -r origin/main` · `check-docs-report-serial` rc=0(중복 0)
- `=> graphics::` 계수 · `graphics.rs` 줄 수를 **`44fbf265` 와 `upstream/main` 양쪽**에서 측정(동일)
- 27줄 치환 diff `--numstat` = 27/27 · 치환 후 잔존 `graphics::` 참조 2건(+`init.rs` 1건) 실측
- `pub fn` 30 / ⒜ 참조 30 / ⒝ 참조 3 / 미참조 **27** — `comm` 으로 계산
- `dead_code` + `-D warnings` = **최소 재현 크레이트를 실제로 빌드**해 확인(추론 아님)
- ★`grep` 은 `/usr/bin/grep` 사용(에이전트 셸의 `ugrep` 그림자가 `-E` 에서 실제로 실패했다)

## 한계

- ★**⒜ 에서 LGT `cargo test` 둘(stdout 축)이 깨지는지는 «재지 않았다»** — base swap 실행이 필요하고 티켓이 금지한다.
  ⇒ ⒜ 의 게이트 비용은 **하한만** 적었다.
- ★코퍼스 축(⑴)·upstream SDK 축(⑶)은 이 레인 밖이라 손대지 않았다.
- ★`AGENTS.md` 의 「LGT 에 cargo test 없음」 낡은 서술은 **발견만 하고 고치지 않았다**(범위 밖 · 별건).
- 제품 코드 diff **0** · 게이트 무접촉 · 머지 0.
