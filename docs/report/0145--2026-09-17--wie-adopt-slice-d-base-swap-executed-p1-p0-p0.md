## [2026-09-17] 「부하에서 verdict 가 뒤집힌다」를 «일화»에서 «측정»으로 올렸다 — 무변경 트리에서 4/6 FAIL (wie-adopt-slice-d-base-swap-executed-p1-p0-p0)

**무엇을**: 제안 `2026-09-17-adopt-slice-d-base-swap-executed-p1-p0#p0` 채택.
`AGENTS.md` 의 keydraw floor 블록에 ⒜**불변식의 천장** ⒝**기전** ⒞**반증 절차 4단계** ⒟**틀린 휴리스틱 1건**을 넣었다.
★**제품 코드 0줄 · Rust 0줄** · 접촉 = `AGENTS.md` 1곳(+31/−1).

### ⓐ 전제 재측 — 제안은 참이고, ★**증거가 «훨씬» 강해졌다**
그 블록은 지금도 「42/42 … **A `FAIL`, a blank last frame, or a non-zero rc is [a regression]**」로
**무조건**으로 읽힌다. 제안이 든 근거는 **1회 일화**(load 160.84)였다. 이 회차가 그것을 **반복 측정**으로 바꿨다:

★**무변경 `origin/main`(`75ca3451`) · 문서가 적은 그 명령 · 플래그 추가 0 · load 121~150**:
| 픽스처 | 결과 | paints |
|---|---|---|
| `keydraw_ktf` | ★**PASS 2 / FAIL 4** | 11 18 15 17 33 23 |
| `keydraw_lgt` | PASS 6 / FAIL 0 | 55 55 44 55 55 55 |

★**고친 것이 «아무것도 없으므로» 회귀할 것도 없었다.** 42/42 는 **동시 30**에서 잰 값이고 이 구간은 그 몇 배다.

### ★기전을 찾았다 — 「수」가 아니라 「왜」를 적었다
`paints` 는 ★**고정 벽시계 예산** 안에 드는 tick 을 센다. 부하는 «초당 tick»을 깎지만 **마감은 그대로**다
⇒ 충분히 부하가 걸리면 ★**마지막 키의 paint 가 마감 전에 도달하지 못한다** ⇒ `last_frame_content=false`
⇒ `--expect-last-frame` 이 **rc=1**. ⇒ ★**count 와 verdict 는 «독립이 아니다»** — floor 규칙은
「tick 이 아직 충분히 든다」는 동안에만 성립한다.

★★**그리고 그 예산은 «반대편»에서도 굶길 수 있다 — 부하 없이.** 실측:
`--action-secs 0.02` → `keydraw_lgt` ★**5/5 FAIL**(paints 4~11) · `--action-secs 0.01` → **PASS**
⇒ ★**임계가 아니라 «레이스»다.** ★**이것이 제안의 tradeoff 를 해소한다** — 제안은 「동시 실행을 30 너머로
올려야 하고 그 측정이 머신 전체를 느리게 만든다」고 적었는데, ★**노브로 재현하면 그 대가가 없다.**

### ★내 제안의 휴리스틱이 «틀렸다» — 정정해서 넣었다
제안의 tradeoff 는 반증 절차로 「**다른 캐리어도 함께 FAIL 인가**」를 제시했다. ★**측정이 그것을 기각한다**:
같은 몇 분 안에 **LGT 는 6/6 깨끗한데 KTF 가 4/6 실패**했다 ⇒ ★**한쪽만 실패하는 것이 부하 기아의 «평범한» 서명**이고
「다른 쪽이 통과했으니 진짜 버그」는 **틀린 추론**이다. 그래서 그 문장을 **그대로 넣지 않고** 아래로 대체했다.

### 넣은 반증 절차(블록인용 4단계)
⑴**여러 번 다시 돌려라** — 기아는 재현되지 않고 진짜 회귀는 재현된다.
⑵★**`paints` 를 유휴 범위(LGT 48~55)와 대조하라** — 11 에서의 FAIL 은 기아다. ★**«건강한 수»에서의 FAIL 이 위험한 쪽**이다
  (2026-09-05 LGT 회귀는 paints 가 **55 → 83 으로 «올라가며»** 화면이 비었다).
⑶**무변경 트리에서 재현하라** — 유일한 결정적 단계.
⑷**부하를 읽어라**(`uptime`) — §Host performance 대로 **load 수치가 아니라 `idle`·`sys`**.

★**「FAIL 을 부하 탓으로 돌려라」로 읽히지 않게** 한 장치가 ⑵⑶이다 — 둘 다 **진짜 회귀를 «남겨 두는»** 축이다.

### 개악 대조(양방향) — ★**무엇을 물었고 무엇을 «못» 물었는지 가른다**
| 상태 | `check-doc-liveness-parity` |
|---|---|
| 정상(전건 **인라인**) | **rc=0** |
| 개악: 재현 명령을 ```` ```sh ```` **펜스**로 추가 | ★**rc=1** |
| 복원 | **rc=0** |

★**정직하게**: 이 쌍이 무는 것은 **«펜스 불변식»**이고, 증명하는 것은 ★**내 편집(+31/−1)이 그것을 깨지 않았다**는 것이다.
★**내가 적은 «측정·기전·절차»에는 기계 소비자가 없다** ⇒ 그 축은 **못 쟀다**.
★**그 대신 주장 자체를 양방향으로 실증했다** — 예산 정상 → PASS · 예산 기아 → **FAIL** · 다시 완화 → PASS(위 노브 표).

### 전 스위트
`fmt --check` OK · `clippy --all -D warnings` rc=0 · wasm clippy rc=0 · `+beta clippy` rc=0 ·
`cargo test --all` **전건 합산 385 passed / 0 failed** · 검사기 전건 rc=0(아래 done 회신에 목록).

### 잃는 것 / 안 하면 무엇이 나쁜가
**잃는 것**: ⑴★**블록이 길어졌다**(+31줄) — 이 파일에서 길이는 실비용이다. 그래서 «수»보다 **기전과 절차**를 넣었다
(수는 낡지만 기전은 안 낡는다). ⑵★**오용 위험**: 「부하 탓」이 회귀의 변명이 될 수 있다 — ⑵⑶이 그것을 막지만
**완전히는 못 막는다.** ★**「잃는 것이 없다」고 적지 않는다** — ⑵가 이 변경의 진짜 위험이다.
**안 하면**: 문면이 「`FAIL` = 회귀」를 **무조건**으로 가르치므로, 다음 회차가 **무변경 트리에서도 4/6 으로 나는 FAIL**을
자기 회귀로 읽고 **멀쩡한 변경을 되돌린다**. ★그 블록 자신이 「한 회차가 정확히 그 오독으로 멈췄다」를 이미 적고 있다.

### 열어 둔 것
- **기계 강제 0** — 「부하에서 난 FAIL 을 자동으로 가르는」 축은 만들지 않았다(제안 범위 밖이고, 그 판정은 ⑶ 없이는 불가능하다).
- **KTF 가 왜 LGT 보다 취약한가**는 **세지 않았다** — 관측만 했다(후속 소재).
