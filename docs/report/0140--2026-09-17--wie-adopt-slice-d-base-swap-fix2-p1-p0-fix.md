## [2026-09-17] 「대조 안 했음」이 「대조했고 깨끗함」으로 읽혔다 — 강등을 성공 줄이 말하게 했다 (wie-adopt-slice-d-base-swap-fix2-p1-p0-fix)

**무엇을**: 게이트② `request-changes` 승계. 블로킹 **F1** 하나와 비블로킹 **F2·F3·F4** 를 전부 받았다.
접촉 = `scripts/check-docs-report-serial.mjs` · `AGENTS.md` · `.github/workflows/engine-contract.yml`(주석 선언) + 원장.
★**fail-open 은 그대로 유지**했다(경고 · red 아님) — 고친 것은 **판정이 아니라 «무엇을 말하는가»**다.

**왜(F1)**: 열린 PR 조회가 **실패**했는데 성공 줄이 「열린 PR claim 충돌 0」이라고 **단언**했다.
접미사 조건이 `mine.ok && mine.out.length` 뿐이고 ★**`claims.ok` 를 안 봤다** ⇒ «건너뛰었다»가 stdout 에서 «대조했고 0이다»가 된다.
★**직전 회차 자신의 worklog `limits[0]` 이 그것을 금지한다** — 「**「검사가 있다」를 「검사가 돌았다」로 읽지 마라**」.

---

## ⓐ F1 — 재현하고 고쳤다(★검수자 형상 그대로 · `gh` 실패 shim + **실제 충돌 있는** 형상)
| 형상 | rc | stdout |
|---|---|---|
| 수정 «전» | 0 | `… 중복 연번 0 · 내가 더한 1건 ↔ 열린 PR claim 충돌 0` ← ★**거짓** |
| 수정 «후» | 0 | `… 중복 연번 0 · ★내가 더한 1건은 «미대조»(열린 PR 을 못 물었다 — 위 경고)` |

★**`!mine.ok` 갈래도 대칭으로** 접미사를 세운다(종전엔 그쪽만 «가드돼» 있어 **비대칭**이었다 — 검수 지적).
★**관용은 발명이 아니다** — 같은 파일의 `--next-serial` 이 이미 「디스크만 보고 답한다」로 강등을 말한다.

## ⓑ 대조군 — **fail-open 도 판정력도 그대로**
`gh` 정상 · 충돌 없음 → **문안·rc 불변**(`충돌 0` · rc=0) / `gh` 정상 · 실제 충돌(`0135` ↔ **#178**) → **rc=1 + `::error` 1건**.

### ★★대조군이 «이 회차가 새로 넣은 버그»를 잡았다 — 숨기지 않는다
F2 를 위해 claim 에 `headRefName` 을 실으며 jq 템플릿 `"\(.number)\t\(.headRefName)"` 을 **JS 문자열**에 넣었더니
★**JS 가 `\(`·`\t` 를 먼저 먹어** jq 가 상수를 받았고 `gh api repos/…/pulls/(.number)/files` 로 죽었다 ⇒ **모든 조회가 실패**.
★**증상이 정확히 F1 의 새 문안(「미대조」)으로 나타나 곧바로 보였다** — 고치기 전이었다면 「충돌 0」으로 조용히 지나갔다.
처방: jq 템플릿을 버리고 **`JSON.parse`**. ⇒ ★이 회차의 F1 수정이 **그 자리에서 자기 버그를 잡은** 셈이다.

## ⓒ F2 — 자기 제외 축을 «파일명»에서 «브랜치»로(파일명은 2차 폴백)
재현(연번 `0136` 고정 + 슬러그만 변경): 수정 «전» ★**rc=1 이고 상대가 «#179 = 자기 자신»**(적용 불능 규칙을 찍는다) ↔ 수정 «후» **rc=0**.
`selfHeadRef()` = `GITHUB_HEAD_REF` → `git rev-parse --abbrev-ref HEAD`(detached 면 `""`).
★**브랜치를 모르면 2차 축만 남는다** — 오답이 아니라 «덜 걸러짐»이고, 그 사실을 함수 주석과 셀프테스트가 못박는다.

## ⓓ F3 — **규칙의 축을 「PR 번호」에서 「claim 시각」으로 바꿨다**(문안만이 아니다)
검수 논거: 체크런은 커밋에 묶이므로 ★**늦게 claim 한 쪽이 먼저 빨개지고 먼저 claim 한 쪽은 자기 CI 가 다시 돌기 전까지 모른다.**
「번호가 큰 쪽이 옮긴다」면 ★**통보받은 쪽은 움직이면 안 되고 움직여야 할 쪽은 아직 모른다.**
⇒ **나중에 claim 한 쪽이 옮긴다**로 바꿨고, 「두 PR 이 다 red」는 **「각자 다음 run 부터」**로 고쳤다(`AGENTS.md`·`STATE.md`·`docs/report/0136`).
★★**두 축이 실제로 «다른 답»을 준 사례가 이미 있다**: `0134` 충돌에서 **#177 이 먼저 claim** 했고 PR 번호는 **#176 이 작다** —
게이트③은 **#176(나중 claim)** 을 옮겼다(`0134 → 0139`). ⇒ ★**옛 문안대로였다면 반대로 지시했을 것이다.**

## ⓔ F4 — `origin/main` remote-tracking ref 의존을 **워크플로에 선언**했다
`myAddedNames` 는 `git ls-tree origin/main` 에 기댄다. 그 ref 를 `fetch-depth: 0` 과 앞선 coverage 스텝의 명시 `fetch` 가
공급하는데 **어디에도 적혀 있지 않았다** ⇒ 스텝 순서를 바꾸면 조용히 강등된다. 주석 한 문단으로 선언했다(구조 변경 0).
★**이제 강등돼도 «조용하지 않다»** — F1 덕에 성공 줄이 「미대조」라고 말한다.

## ⓕ 대가 — **green 처럼 보이던 회차가 「못 쟀다」로 바뀐다**
그것이 **목적이지 부작용이 아니다**. 회차가 이 줄을 보면 ⑴네트워크/권한을 고치고 다시 돌리거나
⑵「이 회차는 cross-PR 축을 못 쟀다」를 회신에 적어야 한다 — 종전엔 둘 다 하지 않고도 green 으로 읽혔다.

## ⓖ 회귀 0(전건 합산 · `tail` 미사용)
`--selftest` **19케이스 전건 일치**(신규 4) · 검사기 8종 **rc=0** ·
`fmt`·`clippy --all -D warnings`·wasm·**`+beta`** 전건 rc=0 · `RUST_MIN_STACK=4194304 cargo test --all` **rc=0**
(`test result: ok` **42줄 · 합산 384 passed · 0 failed**). ★제품 Rust **0줄** · 워크플로 구조 변경 **0**(주석만).

**사용자 영향**: 연번 대조가 **실패했을 때 그렇게 말한다**. 종전에는 조회가 죽어도 성공 줄이 「충돌 0」이라 적어,
이 축이 **자기 실패 모드에서만** 조용히 무력했다.
